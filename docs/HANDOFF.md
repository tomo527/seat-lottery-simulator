# Current save state

Updated: 2026-08-21 (Asia/Tokyo)

## Current state

- Harness Phase 1+2A is checkpointed locally at `1bbd7cdfa0f0bdc24559508052b0f29404a34ed4` and Phase 2B at `542dbcd643a959d64bf0d8d00aac63b74510f257`; Phase 2C acceptance is closed in the current authorized checkpoint diff. `origin/main` remains `9b9d1460711ea9e984d24e264d10d55dd38598cb`.
- Production data is **94 venues / 96 selectable configurations / 151,748 configuration-seat records**. Tokyo user-visible production coverage is **38/76 (50.0%)**, MUST **24/44 (54.5%)**, SHOULD **14/28 (50.0%)**; `venues:release:coverage` reports **RELEASE READY: yes**.
- `TOKYO-WAVE-12` is complete and deployed. `nissay-theatre-standard/current-official-block-numbered-seat-map` is production with 1,490 mapped seats; the other three bounded targets remain non-production handoffs.
- The Phase 1+2A checkpoint contains agent-neutral project knowledge, proportional `verify:*` entry points, the canonical venue workflow, and thin Codex/Claude instruction and Skill adapters. It changes no application behavior, venue data, generated venue artifact, or production meaning.
- Harness Phase 2C closes the provider-neutral optional researcher/reviewer contract: `docs/VENUE_SUBAGENT_ROLES.md` is the sole role contract, and thin read-only adapters in `.codex/agents/` and `.claude/agents/` point to it without fixing models. A trusted new root session completed live discovery of both custom roles and the IINO HALL researcher plus two-stage reviewer acceptance. The main agent remains the only repository integration owner; no application behavior, venue data, generated artifact, or production meaning changed.

## Unresolved items

- `tokyo-geigeki-playhouse-standard`: current official material has an internal 845-versus-829 numbered-seat contradiction; the repository readiness record also requires post-renovation/reopening currentness confirmation.
- `shinbashi-enbujo-standard`: current official 2F/3F side groups lack issuer-defined row/area ownership.
- `bunkyo-civic-main-standard`: current official 1F side cells lack unique row ownership; the visual attachment totals 1,258 against the stated 1,242.
- `iino-hall-standard` is a separate venue-data review follow-up, not a Harness failure. The successful bounded read-only acceptance used the live official download page and its linked seat-map PDF: first pass extracted 500 numbered seats, while the frozen independent extraction and Stage 2 comparison extracted 497. The reviewer reports G2–G30 (G2/G30 marked `可動席`) and H3–H29, versus the existing production source's G1–G30 and H2–H30. Neither permitted official source printed a total or configuration name; the PDF's old modification signals also leave currentness/supersession and movable-seat availability unresolved. Existing production data, generated artifacts, fingerprints, and inventory metadata remain unchanged. Do not begin the re-investigation in this closure.
- The installed WindowsApps CLI still fails to start with `EPERM`, and Claude Code is unavailable in this environment. Static contracts and the Phase 2C live custom-role acceptance pass; local checkpoints only, with push/deploy not authorized.

## Exact next action

For the separate IINO HALL venue-data follow-up, obtain current first-party evidence that resolves the G/H row numbering, G2/G30 movable-seat operating rule, and applicable configuration before changing `data/venue-sources/iino-hall-standard.json` or any generated/fingerprint/inventory data. Do not push or deploy the Phase 2C checkpoint without separate authorization.

## Recent completed work

- 2026-08-21: Phase 2C live acceptance passed in a trusted new root session. `venue_researcher` and `independent_venue_reviewer` were live-discovered, the bounded IINO HALL `STANDARD` researcher report completed, and the same reviewer completed frozen independent extraction followed by Stage 2 comparison. No `unknown agent_type` occurred and no acceptance task edited repository data. The reviewer found a separate 500-versus-497 venue-data discrepancy, which is preserved above without changing the existing production source.
- 2026-08-21: Harness Phase 2C added one provider-neutral venue subagent role contract, optional read-only researcher/reviewer adapters for Codex and Claude Code, two-stage reviewer independence, main-only repository integration, bounded read-only parallelism, and the fresh-pass fallback. Final docs/harness validation passed; no venue-data change is in scope.
- 2026-08-21: Harness Phase 2B added shared read-only SessionStart restoration plus equivalent Codex/Claude approval intent for commits, normal pushes, force pushes, destructive Git operations, and local production deploys. Root, subdirectory, Windows adapter, no-side-effect, Claude Bash/PowerShell matching, JSON, syntax, docs, and lint checks passed. Claude Code live acceptance also confirmed SessionStart, shared instructions, `/venue-wave`, project Hook/permissions, and `verify:docs`. Codex live Hook/Rules loading remains unconfirmed because this environment cannot execute the installed WindowsApps CLI (`EPERM`); static checks, native inline Rules cases, and the Windows hook command passed.
- 2026-08-21: Harness Phase 2A added the shared Codex/Claude instruction adapter structure and canonical venue-wave workflow without changing application or venue semantics. `verify:docs` passed all 20 Markdown files plus the CLAUDE import/frontmatter/canonical-link/adapter-drift contracts; `node --check scripts/verify-docs.mjs`, ESLint, and provider-neutral new-session self-audits also passed.
- 2026-08-21: Harness engineering Phase 1 reorganized operational documentation and added proportional `verify:*` commands. Inventory/readiness/release reports, venue build/check/validate/report, lint, typecheck, unit tests (**20 files / 212 tests**), production build, local Markdown links, and diff checks passed; E2E passed **16/16** in the approved run after the known sandbox Wrangler-registry `EPERM`. Venue generation produced no tracked source/generated-artifact diff. The 24 existing venue validation warnings remain review information, not errors.
- 2026-08-20: Tokyo Wave 12 added the Nissay Theatre representative configuration, synchronized the bounded batch/readiness/coverage/generated artifacts/fingerprint, passed the full venue and app validation set, and was verified on the public Workers deployment with no browser console errors.
