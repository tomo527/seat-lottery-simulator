# Current save state

Updated: 2026-09-04 (Asia/Tokyo)

## Current state

Production is **144 venues / 146 selectable configurations / 218,983 configuration-seat records**, confirmed for the current source database by `npm run venues:validate`. The most recent completed work is the **SAITAMA-CITY-CULTURE-MAIN REVISED-TEMPORARY-CLOSURE-POLICY BOUNDED PRODUCTION REIMPLEMENTATION** (2026-09-04), described in the next paragraph. Before it, the **KANAGAWA-PACIFICO-NATIONAL-HALL BOUNDED PRODUCTION IMPLEMENTATION (USER-AUTHORIZED CURRENTNESS-POLICY OVERRIDE)** (2026-09-04), now **RELEASE VERIFIED / CLOSED** (2026-09-04). Before it, the **GUNMA-KIRYU-CULTURE-MAIN BOUNDED PRODUCTION IMPLEMENTATION** (2026-09-04), now **RELEASE VERIFIED / CLOSED** (2026-09-04). Before it, the **CHIBA-ICHIHARA-CULTURE-MAIN BOUNDED PRODUCTION IMPLEMENTATION** (2026-09-02), now **RELEASE VERIFIED / CLOSED** (2026-09-04), the **KAAT-KANAGAWA-ARTS-THEATRE-HALL BOUNDED PRODUCTION IMPLEMENTATION** (2026-09-01), and before that the **RAIBOC + CHIBA PRODUCTION DISPOSITION RECONCILIATION AUDIT** (2026-08-31), which re-judged the two most recent releases against the cross-document reconciliation policy and changed both dispositions. `chiba-pref-culture-main` 千葉県文化会館〔大ホール〕 (canonical `chiba-pref-culture-large-hall` / configuration `standard`) **stays production** with corrected data at **1,724 mapped seats** (was 1,727). `saitama-raiboc-main` RaiBoC Hall〔大ホール〕 (`raiboc-hall-large-standard`) is **no longer production**: it is back to a blocked/draft state pending a corrected re-transcription. The narrative sections that follow record earlier waves with the production totals that were current at the time; those historical figures are deliberately preserved and are not the current state, and any implementation action named inside them is history, not scope.

The `SAITAMA-CITY-CULTURE-MAIN REVISED-TEMPORARY-CLOSURE-POLICY BOUNDED PRODUCTION REIMPLEMENTATION` (2026-09-04) is complete and closes with **PRODUCTION**. [`data/venue-reports/saitama-city-culture-main-bounded-reimplementation-2026-09-04.json`](../data/venue-reports/saitama-city-culture-main-bounded-reimplementation-2026-09-04.json) is the machine-readable record. It was a separately authorized single-venue wave starting from clean `main`/`origin/main` `e8848c0df628f45a9b600eb04de2e410b3a2ca31` with production at 143/145/216,985 and the Kanto ledger at 60 production / 3 ADOPT / 61 formal blocked / 30 inventory-unresearched. さいたま市文化センター〔大ホール〕 is now schema v2 source `saitama-city-culture-main` / configuration `a-spec` (`scope.kind: official-variant`, `definitionAuthority: issuer`, `confidence: representative`), **1,998 mapped seats across 212 ranges** over areas `floor1` 1階客席 (1列〜30列, 座席番号1〜56, 1,320席, 134 ranges), `floor2` 2階客席 (1列〜6列 plus the printed side-box rows L1列〜L13列 and R1列〜R13列, 座席番号1〜36, 330席, 44 ranges) and `floor3` 3階客席 (1列〜8列, 座席番号1〜48, 348席, 34 ranges). The registered configuration is the issuer's **A仕様（2,006席）**, the only one of the two published specs for which a numbered chart exists; **B仕様1,658席 is aggregate-only** — the issuer publishes no B chart, no B fixed/movable split and no statement of which numbered positions B removes — so it is neither registered nor mixed in, and the `2,006 − 1,658 = 348` identity with the printed 3階客席(348席) subtotal is explicitly **not** adopted as evidence. 156 of the mapped seats (1階客席1列〜5列) are the issuer's 移動席(オケピ) and are disclosed as movable; the other 1,842 are 固定席.

**This wave applied the 2026-09-04 user-authorized revised temporary-closure policy to a record that the previous policy had blocked.** The `SAITAMA-CITY-CULTURE-MAIN BOUNDED PRODUCTION IMPLEMENTATION` (2026-08-29) stopped at the currentness gate and returned RENOVATION / CURRENTNESS HOLD. That decision was correct under the policy then in force and its report is **preserved unchanged** at [`data/venue-reports/saitama-city-culture-main-bounded-implementation-2026-08-29.json`](../data/venue-reports/saitama-city-culture-main-bounded-implementation-2026-08-29.json); it is recorded as **superseded**, not deleted or amended. Because that wave recorded `visualTwoPass.performed: false`, the seat set here was transcribed **from zero**; the old capacity figures were treated as historical comparison information and never as a transcription target or answer key. One factual correction to the historical record: it described the chart as a scanned image, but the PDF contains **no raster image at all** — it is 8,772 vector drawing operations with zero extractable text, so the numbers are vector outlines. The operational conclusion (visual transcription) is unchanged, and the asset bytes are identical to 2026-08-29.

**Closure metadata for さいたま市文化センター〔大ホール〕.** The facility is **ACTIVE as of 2026-09-04** (current programme announcements up to 2026.09.04, 令和8年9月分の催し物案内, current 料金表 and 開館時間 9:00〜21:30). The operator and the City of Saitama both publish, in a 2025-11-28 notice, 「令和9年度から中規模修繕工事を実施する予定です。本工事に伴い令和9年6月1日から休館となります」 and 「※工事期間については、2年間程度を予定しておりますが、今後設計や工事の状況等により変更となる場合があります」. Closure starts **2027-06-01** for **approximately two years**; **no reopening date is published** and none was inferred. This is a **temporary renovation** of the same facility — no 閉館, 解体, 建替え or replacement facility — so facility-space identity is maintained. Unlike the Pacifico precedent there is **no seating-work statement at all**: the issuer names the work only as 中規模修繕工事 and never says whether 客席 is in scope, and it states **no** change to seat count, row structure, numbering or wheelchair provision. That absence is recorded as an absence and explicitly **not** reasoned into proof that nothing changes: the registered data is valid only against the pre-renovation current official chart, and the inventory record carries **`recheckNotBefore: 2029-06-01`**, which is a conservative discovery gate derived from 2027-06-01 plus the stated 2年間程度 — **not** a claimed reopening date. If the issuer later publishes an exact reopening date, that date takes precedence. After reopening, fresh currentness, a re-fetched chart, a fresh two-pass and structural checksums are mandatory.

Verification used the **default fresh independent human visual two-pass**, not DENSE-VECTOR. DENSE-VECTOR was assessed and **rejected on the evidence, not on venue size**: it requires every identifier to be printed as extractable text, and this chart's identifiers are vector outlines with zero extractable text, so the two independent identifier-reconstruction paths it demands do not exist. Pass 1 read 1階客席 front 1列→15列 then rear 16列→30列, then 2階客席 (main 1列→6列, then L1〜L13, then R1〜R13), then 3階客席 1列→8列, low-number side first, from upright panel crops at 320–1200 dpi. Pass 2 re-fetched the PDF independently (byte-identical SHA-256, `cmp` equal), then read 3階客席 8列→1列, 2階客席 6列→1列, side boxes R before L, 1階客席 rear 30列→16列 and front 15列→1列, **high-number side first via horizontally mirrored crops**, at different DPI and with rotation-deskewed renders at the measured 24.8° rake for the steeply raked side blocks. Both passes were frozen before comparison and matched with **zero unresolved mismatch** (212/212 ranges, expanded seat-set symmetric difference 0). One construction-stage error is recorded rather than hidden: while building Pass 1 a phantom 15列 side extension was read on both sides of the 1階客席 front section (14 seats), was detected by the disagreement with the issuer block subtotal 586 and the machine run structure, and was resolved by returning to the chart with a deskewed re-render before Pass 1 was frozen; Pass 2 reproduced the corrected structure independently.

**Three independent machine structural checksums** were run, none of them fed by the human range tables, and none used to generate or complete a seat identifier. (1) PDF vector-geometry cell extraction (drawing operators only, no text read): **1,998 cells, 212 runs**, run-length multiset `{2:2, 3:14, 4:8, 5:6, 6:30, 7:8, 8:12, 9:4, 10:6, 11:2, 12:120}`. (2) An independent raster path — 500 dpi rasterisation, binarisation, closed interior (hole) contour detection: **1,998 cells, 212 runs**, identical multiset, with 31 non-seat rectangles excluded by size and each confirmed by drawing position (28 boxed row-number labels of 1階客席16列〜29列, 2 stepped rectangles in the side-box area, the ■ title glyph). (3) Fill classification of every cell interior: perfectly bimodal at **1,842 white / 156 grey with zero intermediate values**, and all 156 grey cells lie in 1階客席1列〜5列. The human range table's own run-length multiset is identical to both machine paths. A **left-right mirror-symmetry invariant** was also applied (1階客席 n ↔ 57−n, 2階客席 main n ↔ 37−n, 3階客席 n ↔ 49−n, side boxes L ↔ R under n ↔ 13−n): the final 1,998-seat set is invariant under all four with **zero asymmetric seats**, which is a genuine cross-check because the two halves were read from separate crops.

Issuer checksum reconciliation closes. The chart prints 固定席 1,842席 ／ 移動席 156席 ／ 車椅子席 8席　計2,006席 and 1階客席(1328席) / 2階客席(330席) / 3階客席(348席), and the issuer's own **大ホール　客席図** (1:250, page 11 of the current 大ホール舞台資料) overlays colour-coded block subtotals on the same layout: 1階客席前方 586 / 後方 734, 2階客席 中央206・左サイド62・右サイド62, 3階客席前方 240 / 後方 108, 移動席 120 + 36. The mapped set reproduces 586 / 734 / 240 / 108 / 120 / 36 / 156 / 1,842 exactly, gives 2階客席 330 and 3階客席 348 exactly, and gives 1階客席 1,320 which equals the printed 1,328 once the **8 unnumbered 車椅子席** are added. Grand total 1,998 + 8 = 2,006. **Capacity was never a transcription target**: the identifier set was fixed first and reconciled afterwards, and the −8 was not pre-assumed. One residual difference is left visible in `verification.unresolvedIssues`: within 2階客席 the 客席図 labels its sub-blocks 206 / 62 / 62 while the seats actually drawn — counted identically by both human passes and all three machine paths — are 208 / 61 / 61. Both splits total 330, so the floor subtotal and the grand total are unaffected; this is an issuer-side sub-block annotation difference, not a seat-set difference, and **no range was stretched, deleted, added or moved between floors** to match it. The same 客席図 colour grouping is what places the printed side-box rows L1〜L13 / R1〜R13 in 2階客席 rather than 1階客席, so no repository-invented area name was needed. The **8 wheelchair positions** are drawn as two groups of four unnumbered dashed rectangles labelled 車椅子席（4席）, are structurally distinguishable by machine (8.3×9.86pt versus the 8.14×5.48pt numbered cells), sit inside the published 2,006 (the City page states 大ホール（2,006席／車いす席8を含む）), and carry no published replacement relation, so they are registered as `wheelchairSemantics.status: not-reflected` with `accessibilityConversionNotReflected: true` and are the entire source of `rangeDiff: -8`. Confidence is `representative`, following the repository convention that a mapped/published count difference never carries `verified`; the −8 is fully issuer-explained rather than an evidence gap, and the strength of the transcription is recorded in `verification.method` and `unresolvedIssues`.

Validation for this wave: `npm run venues:review -- --id saitama-city-culture-main` reports no production blockers, no errors and one expected warning (official total 2,006 differs from mapped 1,998). `npm run verify:venues` passes and `npm run venues:validate` confirms **144 production venues / 146 selectable configurations / 218,983 configuration-seat records**; the seat-record delta of +1,998 was computed from the fresh mapped set only. Venue-specific machine assertions all pass: expanded mapped total 1,998, unique runtime keys 1,998, duplicate runtime keys 0, source set == runtime set (symmetric difference 0), runtime `totalSeatCount` 1,998, per-floor 1,320 / 330 / 348, issuer-absent numbers undrawable, unnumbered wheelchair positions undrawable, no seat outside the printed row/number ranges, and no B仕様 or pit-layout position present. The Kanto >=1,000 ledger was machine-recomputed from `records[]` and is now **61 production / 3 ADOPT / 60 formal blocked / 30 inventory-unresearched over 154** with `missingFromInventory: 0`; that matches the expected check values without any target fitting. The three PREFLIGHT 4 ADOPT records were not touched, and Pacifico, RaiBoC, Tokyo Priority D and the remaining Kanto research pool were not opened. No soft-limit threshold was changed and no compaction or unrelated refactor was made.

The `KANAGAWA-PACIFICO-NATIONAL-HALL BOUNDED PRODUCTION IMPLEMENTATION (USER-AUTHORIZED CURRENTNESS-POLICY OVERRIDE)` (2026-09-04) is complete and closes with **PRODUCTION**. [`data/venue-reports/kanagawa-pacifico-national-hall-bounded-implementation-2026-09-04.json`](../data/venue-reports/kanagawa-pacifico-national-hall-bounded-implementation-2026-09-04.json) is the machine-readable record. It was a separately authorized single-venue wave starting from clean `main`/`origin/main` `ffe8f455b84a2dd32e8c68ab834b282490759b87` with production at 142/144/211,983 and the Kanto ledger at 59 production / 3 ADOPT / 62 formal blocked / 30 inventory-unresearched. パシフィコ横浜〔国立大ホール〕 is now schema v2 source `pacifico-yokohama-national-hall` / configuration `basic-stage` (`scope.kind: official-variant`, `confidence: verified`), **5,002 mapped seats across 502 ranges** over areas `floor1` 1階席 (1列〜47列, 座席番号1〜80, 3,260席, 310 ranges), `floor2` 2階席 (1列〜18列, 座席番号1〜82, 994席, 109 ranges) and `floor3` 3階席 (1列〜17列, 座席番号1〜84, 748席, 83 ranges). The registered configuration is the issuer's **パターン1 基本舞台（最大5,002席）**, the only one of the issuer's four published usage patterns for which a numbered chart exists; パターン2 4,838席, パターン3 4,718席, パターン4 4,538席 and the 1階席 backrest-folded 1,670席 spec are aggregate-only and are neither registered nor mixed in. 770 of the mapped seats (1階席710 / 2階席60) are issuer-marked 可動席 and are disclosed as such; 3階席 is all fixed. Wheelchair seats stay `not-reflected` because the issuer publishes only that they exist.

**This wave applied a user-authorized currentness-policy revision, recorded in [`VENUE_DATA_GUIDE.md`](VENUE_DATA_GUIDE.md) as `currentness / closure policy（2026-09-04 user-authorized revision）`.** The superseded rule made any formally announced long closure within 12 months of review a production blocker. **From 2026-09-04 onward, a temporary closure or temporary renovation — including one scheduled for the future — is not by itself a production blocker.** A venue may be a production candidate while closed or scheduled to close when facility-space identity is maintained, it is not a closure/demolition/rebuild that replaces the space, a current or issuer-maintained official seat chart exists, a complete numbered representative configuration can be reproduced, the issuer has **not** stated that the refurbishment changes the seating structure, and the ordinary seat-evidence, configuration and cross-check policies are all satisfied. Permanent closure, demolition, rebuild-as-replacement, an explicit issuer statement of seat change, or first-party evidence that the current chart becomes invalid after the works remain HOLD. Absence of a seating-change statement must never be reasoned into proof that nothing changes, and a temporary closure alone must never be used to stop chart research or implementation. **The revision relaxes no seat-evidence rule**: capacity fitting, invented IDs, geometry completion, configuration mixing and third-party chart infill stay forbidden, and the human/machine verification requirements are unchanged. Historical records are preserved: the 2026-09-04 PREFLIGHT 4 HOLD on this venue was correct under the policy in force at the time, and is recorded as **superseded** by this revision rather than deleted.

**Closure metadata for パシフィコ横浜〔国立大ホール〕.** The hall is **ACTIVE as of 2026-09** (2026-08-28 issuer notice of a 2026-09-26 国立大ホール stained-glass opening, a 2026年8月〜 price list, and current seat-chart / usage-pattern / capacity material). The issuer's 2026-06-04 press release announces a **temporary refurbishment closure from 2027-05-17 through 2028-03-31 with reopening planned 2028-04-01**; the other Pacifico spaces stay open and the hall reopens as itself, so this is not a facility-space replacement. **Seating-change evidence was found but is not a seat-structure statement**: the release lists 客席 among the 主な工事箇所（予定） and says 客席・カーペットの更新やバリアフリー環境の整備, yet the issuer nowhere states that the seat count, row structure or numbering changes, and no post-refurbishment chart is published. The registered data is therefore valid only against the pre-refurbishment current official chart, and the inventory record carries **`recheckNotBefore: 2028-04-01`**; after reopening, fresh currentness, a re-fetched chart, a fresh two-pass and structural checksums are mandatory, and the old ranges must not be used as an answer key.

Verification for this wave was split by floor because the chart encodes the two pages differently. Page 1 (1階席) carries its seat numbers as PDF text, so **DENSE-VECTOR verification** was applied there with two independent machine paths: vector cell-quad geometry alone (9,965 duplicated quads, 3,260 unique cells, no text read) and digit-glyph containment identifier reconstruction (3,260 printed number spans, 6,286 digit glyphs, 0 unattached glyphs, 0 cells without a number, 310 strictly ascending runs), followed by a rendered-chart visual audit of both halves, the AA/A/B boundaries, the movable-seat boundaries and the truncated first/last rows. Page 2 (2階席・3階席) carries its numbers as vector outlines rather than text, so DENSE-VECTOR was **not** applied and the default **fresh independent human visual two-pass** was used instead: Pass 1 read 2F then 3F in vertical left/centre/right panels, rows top to bottom, ranges ascending; Pass 2 re-fetched the PDF independently (byte-identical SHA-256), then read 3F before 2F, right to left, bottom row upward, using deskewed horizontal row-band strips instead of vertical panels. Both passes were frozen before comparison and matched with zero unresolved mismatch, and the human range structure matched the machine geometric run structure exactly (2F 109/109, 3F 83/83 runs, identical run-length multisets). A machine glyph-recognition second path for 2F/3F was attempted and **not adopted** — normalized cell-image clustering could not separate adjacent two-digit numbers and vector-path signature matching was defeated by rake-angle estimation error — so the machine paths were used only as structural checksums and never to generate or complete a seat ID; that is recorded in `verification.unresolvedIssues`.

Every issuer checksum reconciles exactly. The issuer publishes 【1階席】3,260席（内可動席710席）, 【2階席】994席（内可動席60席）, 【3階席】748席 and 【全体】5,002席（内可動席770席）, and the press release repeats 最大5,002席（1階席 3,260席、2階席 994席、3階席 748席）. The mapped set is 3,260 / 994 / 748 / 5,002 with `rangeDiff: 0`, and machine movable-seat detection gives 710 / 60 / 0 = 770. The chart also prints per-block subtotals in a reduced block diagram on every floor: the 1階席 34-block multiset (88·96·96·96·88 / 18·5·12·12·12·12·12·5·18 / 15·48·15 / 21·20·21 movable, 118·228·228·180·228·228·118 / 113·192·204·204·204·192·113 fixed) matched the machine block decomposition exactly, as did 2階席 18·37·32·87·104·153 plus 60 movable and 72 fixed, and 3階席 59·46·30·36·77·72 plus 60·60·60. An additional independent invariant was used: the chart is left-right mirror-symmetric under n ↔ 81−n (1階席), n ↔ 83−n (2階席) and n ↔ 85−n (3階席), and the final 5,002-seat set is invariant under all three with **zero asymmetric seats** — which is a genuine cross-check for 2F/3F because their left and right halves were read from separate panels. Pacifico-specific machine assertions also passed: expanded mapped total 5,002, unique runtime keys 5,002, duplicate runtime keys 0, source set == verified set (symmetric difference 0), runtime JSON set == source set, issuer-absent numbers undrawable, and no seat number or row outside the printed per-floor ranges.

Validation for this wave: `npm run venues:review -- --id pacifico-yokohama-national-hall` reports no production blockers, no errors and no warnings. `npm run venues:validate` confirms **143 production venues / 145 selectable configurations / 216,985 configuration-seat records**; the venue contributes no new warning because its mapped count equals the official total, and the pre-existing soft-limit and official-total warnings elsewhere were left untouched. The Kanto >=1,000 ledger was machine-recomputed from `records[]` and is now **60 production / 3 ADOPT / 61 formal blocked / 30 inventory-unresearched over 154** with `missingFromInventory: 0`; that matches the expected check values without any target fitting. The three PREFLIGHT 4 ADOPT records were not touched and remain ADOPT, and RaiBoC, Tokyo Priority D and the remaining Kanto research pool were not opened.

**Release verification for this wave is complete (2026-09-04) and パシフィコ横浜〔国立大ホール〕 is now PRODUCTION + RELEASE VERIFIED / CLOSED.** The production commit is `6bed0971eab9c8c2f20c7b8dd1d5370497d84fbf`. The GitHub check run **Workers Builds: seat-lottery-simulator** completed **successfully on the first attempt** (build `039e2613-04e6-4864-b670-ac36ffacd8b3`) — no retry, no empty commit and no local production deploy. At `https://seat-lottery-simulator.studiotomo.workers.dev/`, searching 「パシフィコ横浜」 and searching 「国立大ホール」 each return exactly one result, パシフィコ横浜 国立大ホール (神奈川県 横浜市, 抽選対象 5,002席), and the selected-venue panel reads 5,002席から今日の1席を抽選します, so the displayed seat count equals the mapped count. The lazy-loaded runtime detail `GET /venue-db/venues/pacifico-yokohama-national-hall--basic-stage.json` returns **HTTP 200** (application/json, 32,090 bytes) with `totalSeatCount` 5,002, 502 ranges and areas 1階席 3,260 / 2階席 994 / 3階席 748. Repeated draws returned seats from **all three areas** (1階席 29列 68番, 2階席 12列 28番, 3階席 17列 22番, 1階席 3列 28番, 1階席 41列 39番, 1階席 26列 11番, 1階席 19列 45番), each present in a registered range, and the result card shows 会場, 座席配置 パターン1 基本舞台（最大5,002席）, エリア, 列 and 座席番号 correctly. An exhaustive sweep of all 5,002 draw offsets against the **deployed** runtime JSON produced exactly the verified seat set — 5,002 distinct seats, 0 duplicates, 0 unverified — so every issuer-absent number and every excluded configuration position is undrawable. No console warning or error appeared at any stage (initial load, venue search, venue selection, draw).

**Future currentness normalization.** From this revision onward, Kanto and Tokyo research must not drop a high-importance venue from the candidate pool solely because of a temporary closure or temporary renovation. Existing formal HOLD records whose only blocker is a temporary closure or renovation, which have a complete current or issuer-maintained numbered chart, and which carry high performance/live/event importance, are eligible for a separate bounded reopen audit. The known immediate reopen candidate is **`saitama-city-culture-main` さいたま市文化センター〔大ホール〕**: earlier research had already established A仕様 2,006席 (fixed 1,842 / movable 156 / wheelchair 8; 1F 1,328 / 2F 330 / 3F 348) against a current official numbered chart, with B仕様 1,658席 available only as an aggregate, and production was stopped mainly by a temporary renovation closure of about two years from 2027-06-01. Under the revised policy that closure alone is not a blocker. Its next implementation must nevertheless use fresh currentness, a re-fetched chart, a fresh two-pass and structural checksums, and must not use the old ranges as an answer key.


The `GUNMA-KIRYU-CULTURE-MAIN BOUNDED PRODUCTION IMPLEMENTATION` (2026-09-04) is complete and closes with **PRODUCTION**. [`data/venue-reports/gunma-kiryu-culture-main-bounded-implementation-2026-09-04.json`](../data/venue-reports/gunma-kiryu-culture-main-bounded-implementation-2026-09-04.json) is the machine-readable record. 美喜仁桐生文化会館〔シルクホール〕 is now schema v2 source `kiryu-culture-silk-hall` / configuration `proscenium` (`scope.kind: official-variant`, `confidence: verified`), **1,517 mapped seats across 57 ranges** over areas `floor1` 1階 (1列〜29列, 1,139席) and `floor2` 2階 (30列〜44列, 378席). Production totals move to **142 venues / 144 selectable configurations / 211,983 configuration-seat records**, and the Kanto >=1,000 ledger recomputes from `records[]` to **59 production / 0 ADOPT / 61 formal blocked / 34 inventory-unresearched over 154**, which empties the PREFLIGHT 3 ADOPT queue. Lane was `JUDGMENT`: the issuer publishes three stage forms with three capacities but only one numbered chart, and the relation between the chart headline 1,527 and the facility page 1,517 had to be settled from issuer evidence before any transcription could be trusted. Currentness PASS on fresh 2026-09-04 fetches — current シルクホール ticketed programme through 2026-10 plus the ワンコインコンサート2026-2027 series, the 2026-09-01 notice accepting シルクホール bookings 令和9年9月30日まで (later than the horizon PREFLIGHT 3 recorded), a 12-month booking window, and a keyword sweep of eleven issuer pages returning only the weekly Tuesday closing day and a routine 設備保守点検 note. The chart link was re-derived on the day from both the event page (シルクホール客席図) and the facility page (ホール座席表): `seat_silk.pdf`, SHA-256 `8d8c70a83160534e075700fecf2f7d61209d3884d386d1484ce458e0e0d853d2`, 2,035,010 bytes, 1 page, HTTP Last-Modified 2026-08-17, byte-identical across four independent fetches. Verification is seven-way and exact. A fresh independent human visual two-pass (Pass 2 on a re-fetched asset, 2階→1階, banks right-to-left, rows descending, numbers descending, each bank rendered de-skewed by its own measured rake angle, different crops and dpi) matched on all 1,517 expanded (floor, row, seat) IDs with zero unresolved mismatch. The machine-expanded source range set is set-equal to both human sets in both directions with **0 duplicate runtime keys**. Three independent machine structural checksums that never read the human table agreed: PDF vector cell geometry (429 blocks, 1,517 cells, 1階1,139 / 2階378), a glyph-containment identifier reconstruction with constrained block chaining (44 rows, a 1,517-member set identical to the human set), and a 600 dpi raster contour hierarchy (1,515 classified + the 2 cells 1階23列32番・33番 whose interiors are bisected by the printed evacuation-route dashes = 1,517). **PREFLIGHT 3 was corrected on two points by fresh evidence.** First, its "no alternate movable/standing configuration" is wrong: the current facility page publishes プロセニアムステージ 1,517席（1F 1,139席、2F 378席）, オーケストラピット使用時（フロントステージ使用時）1,401席 and スラストステージ 1,353席, plus 立見90人 and 親子室（3組分、6席）. Only プロセニアムステージ has a published numbered chart, so only that configuration was registered and nothing was mixed or inferred for the others. Second, 1,517 was **not** a stale superseded inventory value: it is the current issuer figure for this configuration's numbered seats, published with a per-floor decomposition PREFLIGHT 3 recorded as absent. The mapped total matches that issuer subtotal at floor level and grand level with **rangeDiff 0**, and the chart headline 「１５２７席（車椅子席１０席を含む）」 is satisfied as 1,517 + 10 = 1,527 as a post-transcription reconciliation only. 1,527, 1,517 and 1,527 − 10 were never transcription targets and the stale-inventory inference was never used. The ten wheelchair spaces are unnumbered: the chart draws four pictogram squares along 1階16列 (outside 左サイド6番, inside 左ブロック15番, inside 右ブロック34番, outside 右サイド43番), none containing a digit glyph, and four pictograms cannot locate ten positions. The issuer publishes no replacement relation and no companion positions, so `wheelchairSemantics.status` is `not-reflected` with `accessibilityConversionNotReflected: true`; no wheelchair ID was invented and no adjacent numbered seat was deleted. Area names `1階` / `2階` are printed on the issuer chart, so no repository-invented block names were needed.

The `KANTO >=1000 HIGH-IMPACT UNRESEARCHED PREFLIGHT 4` (2026-09-04) is complete: [`data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-4-2026-09-04.json`](../data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-4-2026-09-04.json). It reranked only the then-current **34** `inventory-unresearched` Kanto >=1,000 records, used first-party currentness before seat assessment, and opened exactly four records. `kanagawa-pacifico-national-hall` パシフィコ横浜〔国立大ホール〕 is **HOLD** because the issuer formally announces hall-specific large-scale refurbishment closure from 2027-05-17 through 2028-03-31 (reopening 2028-04-01), within the 12-month policy horizon. `saitama-koshigaya-suncity-hall` サンシティホール〔大ホール〕, `gunma-ota-culture-main` 太田市民会館〔ホール〕, and `ibaraki-hitachinaka-culture-main` ひたちなか市文化会館〔大ホール〕 are **ADOPT / implementation-ready** only for their current issuer-linked standard numbered charts. The report records configuration variants, capacity semantics, issuer checksum structures and independent structural-count plans; it does not transcribe any seat. The third ADOPT stopped the wave at four researched candidates, leaving **59 production / 3 ADOPT / 62 formal blocked / 30 inventory-unresearched over 154** (`missingFromInventory: 0`). Production remains **142 venues / 144 selectable configurations / 211,983 configuration-seat records**. RaiBoC stays separate and blocked; Tokyo Priority D remains lower priority while the 30 Kanto records remain.

## Current exact next action

separately authorized bounded production implementation of **`saitama-koshigaya-suncity-hall` サンシティホール〔大ホール〕**。fresh currentness / chart re-fetch / fresh human two-pass / independent machine structural checksum / issuer subtotal reconciliation を必須gateとし、capacityをtranscription targetにしない。queueは **1. `saitama-koshigaya-suncity-hall`、2. `gunma-ota-culture-main`、3. `ibaraki-hitachinaka-culture-main`** の順（PREFLIGHT 4のADOPT 3件）で、**1 venueずつ別のClaude thread**で扱う。RaiBoC re-transcriptionは別queueのまま。`saitama-city-culture-main` さいたま市文化センター〔大ホール〕 は 2026-09-04 に **PRODUCTION** になったため、この next action からは外れており、再研究・再オープンの対象ではない。これらが終わった後、Codexで **`KANTO >=1000 TEMPORARY-CLOSURE REOPEN AUDIT / PREFLIGHT 5`** を実施し、残りのinventory-unresearched 30件と、既存のtemporary-closure-onlyなHOLDのうち高重要度venueを改訂後policyで再評価する。Tokyo Priority Dは Kanto >=1000 formal accounting の後。以下は旧記述（PREFLIGHT 4 ADOPT 3件を先に処理する計画）で、上記により順序が更新されている: Claudeによる separately authorized bounded implementationを、importance / production-yield順に **`saitama-koshigaya-suncity-hall`、`gunma-ota-culture-main`、`ibaraki-hitachinaka-culture-main`** の最大3件について実施する。**1 venueずつ別のClaude threadで**扱い、この [`PREFLIGHT 4 report`](../data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-4-2026-09-04.json) の各 `implementationVerificationPlan` をmandatory gateとする：fresh independent human visual Pass 1 / Pass 2、machine-expanded source setとのtwo-way equality、issuer subtotal reconciliation、実用的な場合のindependent machine structural checksum、material mismatch時のproduction停止。RaiBoC re-implementationは別queueのまま維持する。30件のKanto >=1000 inventory-unresearchedがformal accountingされるまでTokyo Priority Dへは戻らない。

**Release verification for that wave is complete (2026-09-04) and 美喜仁桐生文化会館〔シルクホール〕 is now PRODUCTION + RELEASE VERIFIED / CLOSED.** The GitHub check run **Workers Builds: seat-lottery-simulator** for production commit `a8a1529` completed **successfully on the first attempt** — no retry, no empty commit and no local production deploy. At `https://seat-lottery-simulator.studiotomo.workers.dev/`, searching 「美喜仁桐生文化会館」 and searching 「シルクホール」 each return exactly one result, 美喜仁桐生文化会館 シルクホール (群馬県 桐生市), displaying **抽選対象 1,517席** — identical to the mapped total — and 「1,517席から今日の1席を抽選します」. The runtime detail lazy-loaded over HTTP 200 (5,314 bytes, SHA-256 `51350541294261ff780cbbcb29f032f5bd35dc14d922cef3090859d853966770`, byte-identical to the committed artifact) and eight draws completed across **both** registered areas with area-qualified labels: `2階 35列 5番`, `1階 27列 11番`, `1階 8列 41番`, `1階 23列 17番`, `1階 7列 25番`, `2階 43列 23番`, `1階 8列 26番`, `2階 40列 34番`. All eight were machine-checked against the live payload and all eight are registered. The deployed payload was machine-checked in the browser session: 1,517 expanded seats, 1,517 unique `(area, row, seat)` keys, **0 duplicates**, per-area totals 1階 1,139 / 2階 378, only the two registered areas, and no source/checked-at/verification/URL leak. Gap probes confirmed the issuer's own omissions are not drawable: none of 1階16列 1〜5番・16〜18番・31〜33番・44番 (the four wheelchair areas) is present, and 1階1列18番・31番, 1階29列17・18・30・32・46番, 2階30列3番・50番, 2階42列1番・52番 and 2階44列2番・51番 are all absent, while 1階23列32番・33番 — the two cells the raster checksum could not classify — are present and drawable. No browser console log, warning or error was produced. 市原市市民会館 was checked as the non-regression venue: still searchable, still 抽選対象 1,527席, runtime JSON status 200, one draw returned the valid 7列 8番.

The `CHIBA-ICHIHARA-CULTURE-MAIN BOUNDED PRODUCTION IMPLEMENTATION` (2026-09-02) is complete and closes with **PRODUCTION**. [`data/venue-reports/chiba-ichihara-culture-main-bounded-implementation-2026-09-02.json`](../data/venue-reports/chiba-ichihara-culture-main-bounded-implementation-2026-09-02.json) records it. It was a separately authorized single-venue wave starting from clean `main`/`origin/main` `f145e12145985de8ec06cad37dddd6329f3a0eff` with production at 140/142/208,939 and the Kanto ledger at 57 production / 2 ADOPT / 61 formal blocked / 34 inventory-unresearched. `chiba-ichihara-culture-main` 市原市市民会館〔大ホール〕 is now **PRODUCTION** as schema v2 source `ichihara-civic-large-hall` / configuration `standard` (`scope.kind: official-variant`), **1,527 mapped seats across 56 ranges over rows 1列〜32列**, `expectedSeatCount` 1,527, `rangeDiff` 0, `confidence: verified`. The hall is a single audience level, so the configuration registers one main area and omits `areaId`/`areaLabel`; each row's numbers run continuously across the aisles, so no repository-invented block names were needed. **Currentness gate PASS** on fresh 2026-09-01/02 fetches (not reused from PREFLIGHT 3): `https://ichiharahall.or.jp/` carries current 大ホール ticketed events (2026-09-06 稲川淳二の怪談ナイト, 2026-10-04 市原フィルハーモニー管弦楽団第47回定期演奏会, 2026-10-09 よしもとお笑いライブin市原2026) and its complete eight-item news list contains no 長期休館・改修・建替・閉館・移転・営業終了 notice. The one related notice, 2026-01-26 「市民会館施設の維持管理に伴う施設貸出休止のお知らせ」, suspends ホール棟 rental only on 2027-02-15〜19, 02-22 and 02-24〜26 with weekends and holidays unaffected; that is routine weekday maintenance, not 改修・長期休館・閉館・運用終了 and not a seating change, so it does not trigger the twelve-month rule and is recorded in the source metadata rather than dropped. **Current chart**: the link was re-derived from both `https://ichiharahall.or.jp/seat/big_hall/` and `https://ichiharahall.or.jp/reserve/big_hall/` rather than trusted by URL, resolving to `https://ichiharahall.or.jp/assets/images/bighall/bighallmap.pdf`; SHA-256 `9d159a6cce7509ace27cbfa89afcf9f3b21cdf014ca8e052751078ad46d32755`, 1,759,043 bytes, 1 page, A3 portrait, Last-Modified 2019-02-21, Adobe Illustrator CS2 / Adobe PDF library 7.77, identical across four independent fetches. Its currency is corroborated by the current 「ホール利用案内（2026.7改訂版）」 PDF (SHA-256 `e98ecae5bc1d1f91cff29f975202d20d6857a912006952cce6469f8d816bb07f`, 4,234,713 bytes, 20 pages, created 2026-07-06), which embeds the identical chart. **Configuration gate**: 劇場型多目的ホール / プロセニアム方式 with exactly one published numbered audience chart; the downloads index publishes only technical drawings, and no movable/removable seat, orchestra pit, stage extension, standing use or event-dependent variant is disclosed anywhere in current issuer material. The chart carries no legend and no PA/見切れ/technical marking of any kind. **Verification is five-way and exact, and 1,527 was never a transcription target** (in particular `1,527 − 4 = 1,523` was never adopted as an expected value). A fresh independent human visual Pass 1 (row-major, rows 1→32, left block→right block, low number first, right wing de-skewed by −18.55° because its seat numbers are drawn at a uniform +18.55°) and a fresh independent Pass 2 on a re-fetched asset (column-major block-by-block instead of row-major, rows 32→18 then 17→1, blocks right-to-left, high number first, different crop boundaries and different dpi) were frozen before comparison and matched on all 1,527 expanded seat IDs with **zero unresolved mismatch**. Three **independent machine structural checksums** that never read the human table then agreed exactly: PDF vector seat-cell geometry returned 1,531 seat-sized quads = **1,527 numbered cells + 4 wheelchair pictogram squares** (plus the four double-width 8.18×16.4pt wheelchair cell outlines); a band-clustered identifier reconstruction (32 axis-aligned bands + 27 rotated bands, row numbers taken from the red label spans, digits assigned to cells by containment) returned a **1,527-member (row, seat) set that is set-equal to the human set with zero difference**, with per-band counts matching the human transcription on all 59 bands; and a 600 dpi raster contour-hierarchy count returned **1,527** glyph-bearing seat cells. A fourth structural check tallied the 108 red row labels (rows 1–3 twice each, row 4 once, rows 5–17 three times each, rows 18–23 five times each, rows 24–28 four times each, rows 29–32 three times each), independently reproducing the block topology and independently confirming that **row 4 has no right-wing block and no right-wing row label** — the wing's printed label sequence is 1, 2, 3, 5, 6 … 17, so 4列 is registered as 8〜31番 only and nothing was interpolated. **Issuer reconciliation is exact and the wheelchair question is resolved from issuer evidence, not inference.** Fresh material yielded a subtotal PREFLIGHT 3 did not have: the current 2026-07 「ホール利用案内」 施設概要 table lists 大ホール as **固定席 1,527席** and **車いす席 4席 as two separate line items** (小ホール is given the same way, 固定席491席 / 車いす席2席). The mapped printed numbered total is **1,527**, so the difference against the official total is **0**, and the four wheelchair spaces sit outside the 1,527 rather than inside it. All four are drawn in 17列 as unnumbered double-width pictogram cells — two at the left end where 1〜4番 would be and two at the right end where 52〜55番 would be — so 17列 prints 5〜51番. The issuer publishes no per-seat replacement relation, so none was inferred: no adjacent number was deleted and no wheelchair seat ID was created (`wheelchairSemantics.status: not-reflected`, `accessibilityConversionNotReflected: true`). No other unnumbered position exists; every one of the 1,527 cells carries a printed number. Source, the inventory record (`chiba-ichihara-culture-main`, now `eligible` / `production` / `venueSourceId: ichihara-civic-large-hall`), the Kanto ledger, the production fingerprint (`count` 1527, offset samples `main/1/10`, `main/18/26`, `main/32/29`) and the generated catalog/runtime were synchronized. The Kanto >=1,000 ledger counts are **recomputed from `records[]`, not fitted**: **58 production / 1 ADOPT / 61 formal blocked / 34 inventory-unresearched** over the unchanged 154-record universe with `missingFromInventory` 0. `gunma-kiryu-culture-main` remains the single ADOPT and was not opened; `saitama-raiboc-main` remains blocked in its separate transcription-remediation queue and was not touched; `kanagawa-kaat-hall` remains production and was not touched. Validation: the canonical `npm run verify:venues` was run and passed end to end; `venues:validate` reports **141 / 143 / 210,466 with 45 warnings**, the same warning count as before this wave because Ichihara's mapped count equals its official total. Catalog and database soft-limit warnings are pre-existing and no threshold was changed. A bounded Ichihara-specific machine check (64 assertions) confirmed the expanded mapped count, unique runtime keys, zero duplicates, source expanded set equal to both human passes and to the machine identifier set, and that every wheelchair position, the row-4 right-wing gap and every printed gap are undrawable while every sampled printed seat is drawable.

**Release verification for that wave is complete (2026-09-04) and 市原市市民会館〔大ホール〕 is now PRODUCTION + RELEASE VERIFIED / CLOSED.** The production commit is `f0ee1a293e196216bc170d63012eda676e875fb5`. Its **first** managed build, `9c5f93bf-e299-4396-807f-8f6e192376e8`, **FAILED and deployed nothing**; that failure is preserved here rather than deleted. The Cloudflare log shows only `Initializing build environment...` and then, about five minutes later, `Build failed to initialize and was timed out` — the Cloning, Installing, Building and Deploying stages were never reached, so the failure stage is **build-environment initialization** and the reason is an **initialization timeout**. It was **not** a code, dependency or build-command failure, which was corroborated independently before the retry: a throwaway clean clone of that exact commit (checked out with `core.autocrlf=false` so the tree byte-matches a Linux runner) ran `npm ci` and then Cloudflare's own `npm run build` to exit 0, with `venues:check` byte-identical and `venues:validate` reporting 141 / 143 / 210,466 and the known 45 warnings, and its `dist` differed from the last successfully deployed commit `f145e12` by exactly one added asset (152 → 153 files, +6,807 bytes) — far under every Workers Static Assets limit. The user then **manually retried the same commit from the Cloudflare Dashboard with no change of any kind**; that retry, build **`5ae7fb87`** on `main` at `f0ee1a2`, reported **SUCCESS / Manually deployed** with Initializing, Cloning, Installing, Building **and Deploying all SUCCESS**. The initial failure is therefore **superseded and resolved** by the successful retry and is recorded as a transient Cloudflare build-environment initialization timeout; no empty commit, no no-op commit and no source change was used to recover. The dashboard retry published no second GitHub check run, so the commit still shows only the original failed check; the deployment was verified directly against the live site instead. The `npm audit` vulnerability count and the `allow-scripts` warning visible in the Cloudflare log did not block the build or the deploy and are deliberately out of scope — no `npm audit fix`, dependency upgrade or package change was made. **Public UI smoke on the deployed production passed.** `GET /venue-db/venues/ichihara-civic-large-hall--standard.json` returns **HTTP 200 `application/json`**, 4,627 bytes, SHA-256 `3615391cada256b87e15268d2225aa3126fb8c40c98954d3633ef2b9b1732d91` — **byte-identical to the committed artifact** and no longer the SPA fallback that path served while the failed build stood; the app was also observed lazy-loading it with status 200. Searching 「市原市市民会館」 returns exactly one venue, 市原市市民会館 / 千葉県 市原市, and after selection the app displays **抽選対象 1,527席** and 「1,527席から今日の1席を抽選します」, with the draw card naming the configuration 通常座席（固定席・標準構成）. **Seven consecutive draws** completed normally over seven distinct rows — 8列27番, 11列12番, 12列13番, 14列27番, 24列24番, 15列17番, 3列10番 — and every one was checked against the live payload's registered ranges and is valid, with correct row/seat labels. The deployed payload expands to **1,527 keys, 1,527 unique, zero duplicate runtime keys**, and leaks no source/URL/verification metadata. **Undrawable as designed and confirmed on the deployed data**: the four unnumbered wheelchair positions (17列 1–4番 and 17列 52–55番), the right-wing block the chart does not print for 4列 (32・40・44・49・55番), and every sampled printed gap (1列9番, 2列8番, 3列40番, 5列39番, 10列35番, 18列38番, 18列45番, 23列40番, 27列41番, 31列34番, 32列30番); every sampled printed seat (1列10番, 1列44番, 4列31番, 16列55番, 17列5番, 17列51番, 28列59番, 32列29番) is present. The browser console produced **no log, warning or error** across the whole smoke. **Non-regression**: KAAT神奈川芸術劇場 is still searchable by its KAAT alias, still shows 抽選対象 1,187席 and 基準勾配（標準的なパターン）, lazy-loaded with status 200, and one draw returned the floor-qualified 1階 4列 25番, a valid registered seat. This release-verification pass changed only `docs/HANDOFF.md` and [`data/venue-reports/chiba-ichihara-culture-main-bounded-implementation-2026-09-02.json`](../data/venue-reports/chiba-ichihara-culture-main-bounded-implementation-2026-09-02.json); the production source, ranges, fingerprint, generated catalog/runtime, inventory record and Kanto ledger were **not** touched, so production stays **141 / 143 / 210,466** and the Kanto ledger stays **58 production / 1 ADOPT / 61 formal blocked / 34 inventory-unresearched**.

In the same wave, [`VENUE_DATA_GUIDE.md`](VENUE_DATA_GUIDE.md) received a **documentation-only policy consistency cleanup** so the 2026-09-01 `転記のcross-check必須要件（production共通）` section is the single current authoring/review contract. That section now states that it applies as a mandatory requirement to new production implementations and to re-implementations of blocked venues from 2026-09-01 onward. The production hard-gate paragraph no longer lists 独立2 generation as mere confidence information and instead makes each item of the cross-check section a hard gate for configurations promoted from 2026-09-01 onward (human visual two-pass for ordinary venues, the defined DENSE-VECTOR verification for DENSE-VECTOR eligible charts, issuer subtotal reconciliation, an independent structural checksum where a machine structural count is practical, and production stop on material mismatch). The `status` list entry for `production` drops the same 独立2 generation wording and points at the cross-check section. The old sentence 「独立generation不足はproduction blockerではありません」 survives only as an explicitly labelled legacy / backward-compatibility statement about pre-2026-09-01 historical production, with its purpose stated — it exists so historical production is not demoted or re-audited en masse merely for lacking today's machine-readable report or two-pass metadata — and is marked as not the current implementation rule. **This changed documentation only.** No schema was changed, no production source was migrated, no validator was modified, and none of the 140 pre-existing production venues was re-verified or demoted. `npm run verify:docs` passed.

The `KAAT-KANAGAWA-ARTS-THEATRE-HALL BOUNDED PRODUCTION IMPLEMENTATION` (2026-09-01) is complete and closes with **PRODUCTION**. [`data/venue-reports/kaat-kanagawa-arts-theatre-hall-bounded-implementation-2026-09-01.json`](../data/venue-reports/kaat-kanagawa-arts-theatre-hall-bounded-implementation-2026-09-01.json) records it. It was a separately authorized single-venue wave starting from clean `main`/`origin/main` `d2b4e3ccdc58b85a83eadd1659505f2929a8006b` with production at 139/141/207,752. `kanagawa-kaat-hall` KAAT神奈川芸術劇場〔ホール〕 is now **PRODUCTION** as schema v2 source `kaat-kanagawa-arts-theatre-hall` / configuration `standard-gradient` (`scope.kind: official-variant`), **1,187 mapped seats: 1階778席・2階189席・3階220席**, `expectedSeatCount` 1,187, `rangeDiff` 0, `confidence: verified`, 120 ranges over `floor1`/`floor2`/`floor3`. **Currentness gate PASS** on fresh 2026-09-01 fetches (not reused from the preflight): `https://www.kaat.jp/` lists a continuous programme from 2026-08 to 2027-01 and `https://www.kaat.jp/news_list` carries no 休館・改修・工事・閉館・建替・営業終了 notice anywhere in its 412 items. **Configuration gate**: the issuer publishes seat charts for only two of its four named seating shapes, and 基準勾配 is the one it labels 標準的なパターン; 急勾配・平土間・鳥屋口・オーケストラピット・張り出し舞台 were disclosed and excluded, never mixed in. The chart `https://www.kaat.jp/assets/pdf/facilities_hall_01.pdf` was re-reached from both the facility page and the download index; SHA-256 `e78ee58074c843d96b2852c9696e1ab7a0d4fdfa13b28241c861c7aeb2242af8`, 2,339,464 bytes, 1 page, Last-Modified 2025-04-28, identical across four independent fetches. Its seat numbers are vector outlines, not extractable text. **Four-way reconciliation is exact.** Human visual Pass 1 (1F→3F, stage-first, low-number-first, per-block de-skew renders) and a fresh independent Pass 2 (re-fetched asset, 3F→1F, back-to-front, high-number-first, different crops, whole chart read 180° upside down) were frozen before comparison and matched on all 1,187 expanded seat IDs with **zero unresolved mismatch**. An **independent machine structural checksum** was then taken by two paths that never read the human table — PDF vector seat-cell geometry, and a 600 dpi raster contour-hierarchy count with colour classification — giving **1,263 printed numbered positions: 本席1,187（1F 778／2F 189／3F 220）・イス付立見席58・立見席18**, which reproduces the issuer's own summary table cell for cell. A block-level machine clustering produced 81/17/22 blocks per floor whose per-block seat-count multiset is identical to the human transcription. Issuer subtotals, both human passes and the machine checksum agree at grand, per-floor and per-block level, so no difference needed an issuer explanation and none was manufactured. **Excluded by issuer definition, not by inference**: イス付立見席 58 (yellow legend class), 立見席（イス無）18 (blue legend class) and 車椅子席 2組 (unnumbered) are all outside the issuer's own 1,187, so none is registered or drawable. The ※PA席・見切れ席(サイドバルコニー) note has no legend and identifies no printed number, and the dashed boxes around 1階25列 中央11・12/24・25 have no legend either, so no printed seat was excluded by inference. Validation: the canonical `npm run verify:venues` was run; `venues:validate` reports **140 / 142 / 208,939 with 45 warnings**, the same warning count as before this wave because KAAT's mapped count equals its official total. Catalog and database soft-limit warnings are pre-existing and no threshold was changed. A bounded KAAT-specific machine check (25 assertions) confirmed the expanded mapped count, unique runtime keys, zero duplicates, expanded source set equal to the verified human set, machine structural checksum equal to the mapped set, per-floor subtotal reconciliation, and that every known gap and every issuer-excluded position is undrawable.

Two bounded metadata-consistency corrections were made in the same wave, against existing repository evidence only and with no new research. (a) In [`data/venue-coverage/kanto-must-coverage-audit-2026-08-27.json`](../data/venue-coverage/kanto-must-coverage-audit-2026-08-27.json), `explicitFindings.kArenaYokohama` still carried the superseded 2026-08-31 HARD-CASE / JUDGMENT HOLD wording while the same file's `records[]` entry, the canonical inventory record and this file all recorded Kアリーナ横浜 as PRODUCTION. Because `explicitFindings` states the current finding, it now records the PRODUCTION disposition and the DENSE-VECTOR result (`k-arena-yokohama` / `upper-stands-fixed`, 15,929 mapped), and the old blocker and reopen condition are preserved verbatim in a new `supersededHistory[]` entry. No production source, range, generated artifact or fingerprint for Kアリーナ横浜 was touched. (b) The ledger bucket for `tochigi-ken-sogo-bunka-center-main` still read `inventory-unresearched` although the canonical inventory record in `data/venue-inventory/release-seed-tochigi.json` already carried `researchStatus: "blocked"` with a concrete SOURCE HOLD reason, which is why PREFLIGHT 3 had already excluded it from the live pool; the ledger is now synchronized to that existing formal blocked disposition. The Kanto >=1,000 ledger counts are **recomputed from `records[]`, not fitted**: after the cleanup and before KAAT they were 56 production / 3 ADOPT / 61 formal blocked / 34 inventory-unresearched, and after KAAT they are **57 production / 2 ADOPT / 61 formal blocked / 34 inventory-unresearched** over the unchanged 154-record universe with `missingFromInventory` 0. `chiba-ichihara-culture-main` and `gunma-kiryu-culture-main` remain ADOPT and were not opened; `saitama-raiboc-main` remains blocked in its separate transcription-remediation queue and was not touched.

A narrow standing policy was added to [`VENUE_DATA_GUIDE.md`](VENUE_DATA_GUIDE.md) as `転記のcross-check必須要件（production共通）`, formalizing the future requirement recorded in PREFLIGHT 3 after the RaiBoC/Chiba audit showed that two matching human visual passes can reproduce the same systematic transcription error. It keeps the human visual two-pass mandatory for ordinary venues, requires a mapped subtotal to match any issuer-published floor/area subtotal or to have an issuer-explained difference, requires an independent machine structural checksum whenever the current official chart is independently machine-countable (and a recorded reason plus an alternative checksum when it is not), states that a machine count is a cross-check and never a basis for generating seat IDs, repeats that ranges must never be fitted to a capacity or subtotal target, and blocks production when a human-human MATCH materially mismatches the issuer or structural checksum. It weakens no existing rule, including the DENSE-VECTOR verification exception.

The `KANTO >=1000 HIGH-IMPACT UNRESEARCHED PREFLIGHT 3` (2026-09-01) is complete: [`data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-3-2026-09-01.json`](../data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-3-2026-09-01.json). Starting from clean `main`/`origin/main` `1788e526aaf60936b1e901760f5ae11aa946beec`, it rebuilt the 39-record ledger `inventory-unresearched` bucket and found 38 live candidates after excluding the already-blocked canonical Tochigi inventory record. It opened only four candidates (currentness first) and stopped immediately when the third ADOPT was reached: **(1) `kanagawa-kaat-hall` KAAT神奈川芸術劇場〔ホール〕** — issuer-named 基準勾配 1,187-seat scope with 1F/2F/3F checksums 778/189/220 and a current linked numbered vector chart; **(2) `chiba-ichihara-culture-main` 市原市市民会館〔大ホール〕** — active 1,527-seat including four wheelchair positions, current linked complete numbered PDF; **(3) `gunma-kiryu-culture-main` 美喜仁桐生文化会館〔シルクホール〕** — active current linked complete numbered PDF, issuer 1,527 including ten wheelchair positions (correcting stale inventory discovery 1,517). **`chiba-lala-arena-tokyo-bay` is formal DEFER**: the issuer expressly makes seating placement and wheelchair position event-specific and publishes no complete issuer-defined representative configuration. No source JSON, transcription, fingerprint, generated artifact, runtime/catalog, or production data changed. The ledger is **56 production / 3 ADOPT / 60 formal blocked / 35 inventory-unresearched** (`missingFromInventory: 0`); the next live pool is 34 after excluding the already-blocked Tochigi record. RaiBoC was not reopened; its existing metadata already makes clear that issuer evidence is sufficient and the blocker is repository-transcription remediation, so no normalization was made. **Future implementation rule:** human visual two-pass MATCH alone is insufficient after the RaiBoC/Chiba audit. Each ADOPT's recorded `implementationVerificationPlan` is mandatory: fresh independent human two-pass, machine-expanded range verification, issuer subtotal reconciliation where available, independent machine structural checksum where feasible, and production stop/return-to-chart on any mismatch. The exact next action is **Claudeによる separately authorized bounded implementation of `kanagawa-kaat-hall`, `chiba-ichihara-culture-main`, and `gunma-kiryu-culture-main` in that order; apply each plan as mandatory gates. Keep RaiBoC re-implementation as a separate queue and do not combine it.**

Validation for this preflight: JSON parse, `npm run venues:inventory:report`, `npm run venues:release:coverage`, and `npm run venues:validate` passed; the latter confirms 139/141/207,752 with existing warnings only. `verify:docs` passed local-link and shared-adapter checks, then hit the known Windows `verify-harness` subprocess failure (`git status failed: undefined`); no production artifact was regenerated. `git diff --check` passed.

The `RAIBOC + CHIBA PRODUCTION DISPOSITION RECONCILIATION AUDIT` (2026-08-31) is complete. [`data/venue-reports/raiboc-chiba-production-disposition-reconciliation-audit-2026-08-31.json`](../data/venue-reports/raiboc-chiba-production-disposition-reconciliation-audit-2026-08-31.json) records it. It was opened to re-judge whether the two most recent releases satisfy the cross-document reconciliation policy, on the stated premise that their fresh visual two-pass seat sets were valid and only the issuer floor-subtotal relations were open. **That premise did not survive the audit.** It started from clean `main`/`origin/main` `b16ea52e39a1ad94822466d23e3cc44892dd2b5a` with production at 140/142/209,139 and the Kanto >=1,000 ledger at 57 production / 0 ADOPT / 58 formal blocked / 39 inventory-unresearched. **Step 1** machine-expanded both production sources and confirmed they reproduce their reports/HANDOFF figures exactly, so there was no source/report drift. **Step 2** re-fetched every current first-party document of both issuers: both seat charts are byte-identical to their recorded provenance (RaiBoC SHA-256 `a0bd5467…ebac81`; Chiba SHA-256 `71ddce2a…3b14c9`, gzip-decoded 1,953,992 bytes, Last-Modified 2025-12-08), so neither chart was revised. Chiba’s other current first-party material (the `seating` page overlay image, the 2025/09 barrier-free map, the building floor plan, the stage plan) and RaiBoC’s (the three 舞台図面 and the current usage-notes PDF) publish **no** additional seating subtotal, legend, or floor-ownership statement; no secondary source was used and the former Chiba 1,787 figure played no part. **Step 3** then counted every drawn seat cell on each current chart by two independent machine paths (rasterised connected-component analysis of the enclosed white seat cells, and PDF vector rectangle/quad extraction) and re-read only the differing rows visually at 11x-20x zoom. **Both charts reconcile losslessly with their own issuers.** Chiba prints 1階固定席 1,332席 / 2階固定席 217席 / 3階固定席 175席 / 大ホール固定席 1,724席 / 車いす席 4席 / 収容人員 1,728名, and the chart draws exactly 1,332 + 217 + 175 = 1,724 numbered cells. RaiBoC prints 1階席938席 ／ 多目的鑑賞室3席 ／ 2階席431席 ／ バルコニー28席　計1400席, and the chart draws exactly 938 + 431 + 28 = 1,397 numbered cells plus the 3 unnumbered 多目的鑑賞室 positions. **The unexplained floor-level differences were therefore repository transcription defects, not unexplained issuer relations**, and the `tokyo-official-0249` Scott Hall pattern (a matching grand total masking an unexplained per-floor subtotal) does not apply to either venue.

**Chiba: PRODUCTION retained, data corrected.** Three transcription errors were found and fixed, each confirmed twice (machine cell count plus a targeted high-zoom re-read of the current official chart): 1階19列 was registered as 3-46 but the chart prints side number blocks only on rows 9-18 — rows 19 and 20 carry no side seats and the four wheelchair pictograms sit in the row-19 side positions, so row 19 is **8-41** (-10); 2階15列 is **6-38**, not 6-37 (+1); and 2階16列’s right block is **28-37**, not 28-31 (+6). The corrected mapped set is **1階1,332席 / 2階217席 / 3階175席 = 1,724**, equal to every printed issuer floor subtotal and to the issuer’s own 固定席 total, with `rangeDiff` now **−4** against `expectedSeatCount` 1,728 — exactly the four unnumbered wheelchair spaces the issuer itemises separately. Three independent issuer-published per-floor checksums now agree, which is a stronger external control than the original two-pass had. No seat ID was invented, no printed identifier was deleted without direct chart evidence that it is not printed, and no count was moved between floors. The source, the generated catalog/runtime, the production fingerprint (count 1,727 → 1,724, sample `floor1/23/9` → `floor1/23/18`, sha256 recomputed) and the inventory record were synchronized. Note that the CHIBA wave never wrote an implementation report JSON; the audit report above now supplies that record.

**RaiBoC: PRODUCTION superseded → blocked.** The registered configuration does not reproduce the current chart’s printed ID set. Four defects are confirmed: (a) 2階 row **2A** is printed on the chart (row label `2A`, left 3-9, right 39-45) and is entirely absent from the registered ranges; (b) a further side-only 2階 row between 2A and 2B (left 2-9, right 39-47, no centre block) is also absent; (c) the 2G-2K side areas are printed as split outer/inner blocks (left 1-6 or 2-6 plus 7-13 or 7-14; right 43-48 or 43-47 plus 36-42 or 35-42) whose row ownership does not match the registered ranges; and (d) 1階 rows **B/D/F/H** are registered as contiguous ranges (7-40, 6-41, 4-43, 3-44) although the chart prints their centre blocks ending at **30**, not 31 (rows A/C/E/G/I end at 31), so four seats numbered 31 are registered that are not printed. This is **not** a HARD-CASE evidence defer: the issuer evidence is complete and sufficient, and 1F 938 and バルコニー 28 are already closed by both machine paths and by row-level visual confirmation. What is not yet established is the row-by-row breakdown of the issuer’s 2階 431席. `data/venue-sources/raiboc-hall-large-standard.json` is now `draft` with its single configuration `draft`/`selectable: false` and `verification.status: pending`; its historical implementation report [`data/venue-reports/saitama-raiboc-main-bounded-implementation-2026-08-31.json`](../data/venue-reports/saitama-raiboc-main-bounded-implementation-2026-08-31.json) is **retained unchanged** — its currentness gate, configuration assessment and evidence derivation stand, but its fresh two-pass seat set is not valid and the production disposition it recorded is superseded by this audit under the cross-document reconciliation policy. The inventory record is `blocked` with `venueSourceId: null`, and the generated catalog entry, the runtime detail file and the production fingerprint entry were removed. **Reopen condition:** a separately authorized bounded re-implementation that re-transcribes the whole 2階 (row 2A included) from the current official 座席表 PDF with two independent passes, settles the 2G-2K outer/inner side-block row ownership from the chart structure alone, removes the non-printed seat 31 from 1階 rows B/D/F/H, and closes with per-area mapped totals of **1F 938 / 2F 431 / balcony 28**; the 多目的鑑賞室 (3 unnumbered positions) stays excluded and the unlegended X-cross (1階 I列) and diagonal-hatch (1階 R列) mark semantics remain an open issuer question that must not be resolved by inference. No capacity fitting, no invented identifiers, and no seat moved between areas to fit a subtotal.

The Kanto >=1,000 ledger is now **56 production / 0 ADOPT / 59 formal blocked / 39 inventory-unresearched**. Validation: the canonical `npm run verify:venues` was run; `venues:validate` reports **45 warnings** (one fewer than before, because RaiBoC’s official-total warning left production and Chiba’s remains as the expected `official total 1728 differs from mapped seat count 1724`). No catalog or database size threshold was changed. The exact next action is **CODEX: KANTO >=1000 HIGH-IMPACT UNRESEARCHED PREFLIGHT 3**; the RaiBoC re-implementation is a separate, separately authorized item and is **not** the next action.

The `CHIBA-PREF-CULTURE-MAIN BOUNDED VISUAL TWO-PASS PRODUCTION IMPLEMENTATION` (2026-08-31) is complete and closes with **PRODUCTION**. It started from clean `main`/`origin/main` `cf10c32d9a97535f2975c6c5c6631775e62198ad` with production at 139/141/207,412 and the Kanto >=1,000 ledger at 56 production / 1 ADOPT / 58 formal blocked / 39 inventory-unresearched, per the 2026-08-30 preflight's ADOPT disposition for this venue (the last remaining ADOPT). **Currentness gate PASS** on fresh fetches (not reused from the prior preflight): `https://www.cbs.or.jp/chiba/` carries an active 2026-08/09 programme (including a 2026-09-01 70th-anniversary ceremony and a 2026-09-12 event) with no closure/renovation/rebuild/cessation notice; `https://www.cbs.or.jp/chiba/archives/4002` confirms the 2025 reopening is a **completed historical closure** (令和5年4月〜令和7年7月12日 休館, 令和7年7月13日 再開, published 2025-05-29 / last updated 2025-07-05), with no newer closure notice found; `https://www.cbs.or.jp/chiba/hallguide-2/large_hall` states current capacity **1,728席（車いす用4席含む）** with no orchestra-pit or movable-seat alternative-configuration note. **Current seat chart**: rather than trusting the preflight-recorded PDF URL directly, the current `https://www.cbs.or.jp/chiba/downloads` page was fetched fresh and independently re-linked to the same `chiba-b-seats.pdf`. That PDF was fetched fresh three times (all byte-identical after gzip decoding: SHA-256 `71ddce2aef072763eb3444866ddb168ede8ab7b651fa6ba77565172c933b14c9`, 1,953,992 bytes, 1 page, Last-Modified 2025-12-08 — five months after the reopening, indicating a maintained current file rather than a stale reopening artifact). **Configuration assessment**: the chart draws a single coherent 1F/2F/3F seating diagram with its own printed header (収容人員1,728名 / 固定席1,724席・♿4席 / 1F固定席1,332席・♿4席 / 2F固定席217席 / 3F固定席175席); no orchestra-pit, movable-seat, or alternate-configuration note appears anywhere on the chart or the hall page, and the former discovery capacity 1,787 was not used for any arithmetic. **Verification method**: the PDF carries both vector seat-box paths and embedded text, but machine extraction was not used as the sole method — the standard human visual two-pass was used (this is an ordinary ~1,728-seat hall chart, not a DENSE-VECTOR-eligible super-high-density diagram). **Pass 1** read 1F front (rows 1→20, each row left-to-right) → 1F rear (rows 21→36) → 2F (side wings rows 1→12, then center curve rows 13→16) → 3F (side wings rows 1→8, then center curve rows 9→12), from fresh renders at 15–24x zoom per region. **Pass 2** independently re-fetched the PDF a third time and re-read 1F bottom-up (row 20→1, each row from its opposite/right-side extension block first) then the rear rows again bottom-up, then 2F and 3F wings and center blocks re-cropped at different zoom/boundaries — without treating Pass 1's ranges as an answer key. **MATCH with zero unresolved mismatch**: the complete printed ID set, row ownership, every range, every gap, and the two wheelchair-icon positions were identical between passes on every row of all three floors. **Per-floor mapped totals**: 1F **1,342** (front 776 + rear 566), 2F **210** (wings 46+45 + center 119), 3F **175**. 3F's mapped total matches the chart's own printed 3F subtotal (175) exactly; grand mapped total is **1,727**. **Reconciliation against the current official 1,728**: the four wheelchair positions sit in an unnumbered side alcove between 1F rows 19/20 (2 icons left, 2 right), consuming no printed seat number and carrying no issuer-defined per-seat replacement relation to any numbered seat — no wheelchair seat ID was created and no numbered seat was deleted. Every row's printed numbers run contiguously across the chart's internal aisle/block divisions except 2F row 16 (7–16 / 19–25 / 28–31) and 3F rows 9, 10, and 12, which carry genuine printed gaps at those block boundaries. The mapped grand total (1,727) differs from the issuer's own fixed-seat total (1,724, i.e. 1,728 − 4 wheelchair) by +3, concentrated as 1F +10 / 2F −7 / 3F 0; this reproduced identically across both independent passes, so it is recorded as verification metadata (`rangeDiff: -1` against `expectedSeatCount: 1728`) rather than closed by inventing, deleting, or arithmetically fitting any seat ID. The former discovery capacity 1,787 played no role in the transcription or reconciliation. Source `data/venue-sources/chiba-pref-culture-large-hall.json` (schema v2, configuration `standard`, 90 ranges), the inventory record (`chiba-pref-culture-main`, now `eligible`/`production`, `venueSourceId: chiba-pref-culture-large-hall`), the Kanto ledger and coverage audit, the production fingerprint (offset samples `floor1/1/13`, `floor1/23/9`, `floor3/12/40`), and the generated catalog/runtime were synchronized. The Kanto >=1,000 ledger is now **57 production / 0 ADOPT / 58 formal blocked / 39 inventory-unresearched** — this closes the current ADOPT implementation queue. Validation: the canonical `npm run verify:venues` passed (inventory/readiness/release-coverage reports, venue build/check/validate/report, lint, typecheck, 224 unit tests in 20 files, production build, docs and harness verification, diff check, and 17 Playwright E2E tests); `venues:validate` reports 46 warnings, one more than before, and the single new warning is the expected `chiba-pref-culture-large-hall.json: configuration 0 official total 1728 differs from mapped seat count 1727` — no capacity was invented or deleted to close that gap, and no catalog/database size threshold was changed. Pushed production commit `83347a4` (`main`/`origin/main` both at `83347a440e1b073caffc042b75cd450ad83068d6`, working tree clean); its managed **Workers Builds: seat-lottery-simulator** check run completed successfully. The public UI at `https://seat-lottery-simulator.studiotomo.workers.dev/` searched and selected 千葉県文化会館, displayed **抽選対象 1,727席** (matching the mapped count), and completed ten draws — all landing on 1F (`1階 32列 23番`, `1階 20列 11番`, `1階 31列 17番`, `1階 11列 40番`, `1階 22列 40番`, `1階 17列 18番`, `1階 6列 25番`, `1階 16列 …`, `1階 11列 …`, `1階 3列 …`), each with a floor/row/seat-qualified result label and no browser console warning or error; landing only on 1F across ten draws is statistically unsurprising (1F holds 1,342 of 1,727 seats, ≈78%) and is independently corroborated for 2F/3F reachability by the passing production unit test, which exercises the identical `seatAtOffset` sampler at the deterministic last offset (1726) and asserts it resolves to `floor3/12/40`. The exact next action is **CODEX: KANTO >=1000 HIGH-IMPACT UNRESEARCHED PREFLIGHT 3** — rerank the remaining 39 inventory-unresearched Kanto >=1,000 records with current operational status as the first gate, then live/event relevance and production yield, stopping at at most 5 researched / 3 ADOPT; do not start that research in this session. Tokyo Priority D remains lower priority while Kanto inventory-unresearched records remain.

The `SAITAMA-RAIBOC-MAIN BOUNDED VISUAL TWO-PASS PRODUCTION IMPLEMENTATION` (2026-08-31) is complete and closes with **PRODUCTION**. [`data/venue-reports/saitama-raiboc-main-bounded-implementation-2026-08-31.json`](../data/venue-reports/saitama-raiboc-main-bounded-implementation-2026-08-31.json) records it. It started from clean `main`/`origin/main` `bb3d081d271e9de204c6fe610ef81dba71f898a9` with production at 138/140/206,028 and the Kanto >=1,000 ledger at 55 production / 2 ADOPT / 58 formal blocked / 39 inventory-unresearched, per the 2026-08-30 preflight's ADOPT disposition for this venue. **Currentness gate PASS**: `https://saitama-culture.jp/raibochall/` (fetched fresh, not reused from the prior preflight) shows active operation with booking into 2027 (RaiBoC Hall New Year Concert 2027, announced 2026-08-29) and no closure/renovation/cessation notice; `https://saitama-culture.jp/raibochall/hall/` confirms current 客席数：1,400席 for 大ホール with no closure notice, and its own `座席表（大ホール）` link was followed fresh rather than trusting the prior preflight's recorded URL as still current-linked — the link resolved to the same `https://saitama-culture.jp/sculwp/wp-content/uploads/omiya-civichall_zaseki01.pdf` the preflight had recorded. That PDF was fetched fresh twice (byte-identical, SHA-256 `a0bd5467ad87d6cc1ce26cf6b56d0cfc8c37f3dc5bdb60e39334003aedebac81`, 1,427,749 bytes, 1 page). **Configuration gate**: the current hall page states a single 1,400-seat capacity with fixed seating (no movable-chair note, unlike 小ホール) and no issuer-defined second configuration from reflector-board setup; a single coherent numbered configuration was confirmed directly from the chart. **Verification method**: the chart's seat numbers are drawn as vector curve paths, not embedded text (machine-inextractable), and the venue is an ordinary ~1,400-seat single-page hall chart, not a super-high-density diagram, so the standard human visual two-pass was used and DENSE-VECTOR was correctly not invoked. **Pass 1** read the chart floor/row order 1F front (A→I) → 1F rear (J→W) → balcony (LA/LB/RA/RB) → 2F (2B→2K), each row left-to-right, from a fresh 6x-zoom render. **Pass 2** independently re-fetched the PDF, re-rendered at a different zoom (7x), and read in reversed floor/row order (2K→2B→balcony→W→J→I→A) with each row read from its opposite end first, without treating Pass 1's ranges as an answer key. **One mismatch was found and resolved against the current chart, not by inference**: Pass 1 and Pass 2 initially disagreed on which of 1F rows H/I carried the wheelchair pictograms and X-cross marks; a wider, label-anchored re-crop spanning rows B–I simultaneously (so every row label directly touched its own numbers) resolved it unambiguously — row H is fully plain 3–44 with no marks, and row I carries both the wheelchair pictograms (replacing the unprinted 4–7 and 40–43 positions) and all X-cross marks (over printed 10–13, 17–20, 23–24, 27–30, 34–37). Every other field (row R's diagonal-hatch on 16–31, row W's isolation to 16–31 only, row J starting at 2, the 2B–2F right-side continuity, row 2K's four-segment structure, and LA/LB/RA/RB) matched between passes with **zero further unresolved mismatch**. **Reconciliation**: mapped total is **1,384** (1F 942 including balcony's own 1F-level boxes counted separately below, 2F 414, balcony LA+LB+RA+RB 28 — the balcony's 3+11+3+11=28 exactly matches the chart's own printed バルコニー 28席, corroborating the area assignment) against the chart's own arithmetic 938(1F)+3(多目的鑑賞室)+431(2F)+28(バルコニー)=1,400. The chart prints X-cross marks over 18 numbers in row I and a diagonal-hatch over all 16 numbers in row R with no legend found anywhere on the one-page chart (all four corners and margins checked); following the precedent in `mito-civic-globis-hall` (2F shaded cells retain their printed numbers rather than being deleted without a legend), these printed-but-marked numbers were kept in the registered range rather than excluded on an unsupported assumption, and this is recorded as an open verification note. Wheelchair positions carry no printed seat ID and are `wheelchairSemantics.status: not-reflected` / `accessibilityConversionNotReflected: true`. The 多目的鑑賞室 (3 unnumbered seats) is excluded from the numbered scope. No capacity fitting, no invented seat IDs, no configuration mixing. Source `data/venue-sources/raiboc-hall-large-standard.json` (schema v2, configuration `standard`, 64 ranges), the inventory record (`saitama-raiboc-main`, now `eligible`/`production`, `venueSourceId: raiboc-hall-large-standard`), the Kanto ledger, the production fingerprint (offset samples `floor1/A/9`, `floor1/Q/43`, `floor2/2K/47`), and the generated catalog/runtime were synchronized. The Kanto >=1,000 ledger is now **56 production / 1 ADOPT / 58 formal blocked / 39 inventory-unresearched**. Validation: the canonical `npm run verify:venues` passed (inventory/readiness/release-coverage reports, venue build/check/validate/report, lint, typecheck, 224 unit tests in 20 files, production build, docs and harness verification, diff check, and 17 Playwright E2E tests); `venues:validate` reports 45 warnings, one more than before, and the single new warning is the expected `raiboc-hall-large-standard.json: configuration 0 official total 1400 differs from mapped seat count 1384` — no capacity was invented or deleted to close that gap, and no catalog/database size threshold was changed. The exact next action is **separately authorized bounded visual two-pass implementation of `chiba-pref-culture-main` 千葉県文化会館〔大ホール〕**.

The `KANAGAWA-K-ARENA-YOKOHAMA DENSE-VECTOR PRODUCTION IMPLEMENTATION` (2026-08-31) is complete and closes with **PRODUCTION**. [`data/venue-reports/kanagawa-k-arena-yokohama-dense-vector-production-2026-08-31.json`](../data/venue-reports/kanagawa-k-arena-yokohama-dense-vector-production-2026-08-31.json) records it. It was opened on an explicit **user decision** that Kアリーナ横浜 is release-critical by scale and usage importance and must reach production, that evidence policy is **not** relaxed (no capacity fitting, no invented seat IDs, no geometry completion, no configuration mixing), and that the previous wave's blocker was **not** missing issuer evidence but **human-only transcription scalability** over roughly 1,100 printed row-ranges — so transcription volume alone could not be used again as a formal blocker. It started from clean `main` / `origin/main` `5c37747e3574f66e55f4a6aa48bb3e8c73f32471` with production at 137 / 139 / 190,099 and the Kanto >=1,000 ledger at 54 production / 2 ADOPT / 59 formal blocked / 39 inventory-unresearched. **Policy change**: [`VENUE_DATA_GUIDE.md`](VENUE_DATA_GUIDE.md) gains one narrow subsection, `### DENSE-VECTOR verification（超高密度図の狭い例外）`, placed immediately after `### fixed-only configuration`; no existing rule was edited or weakened. The production default remains two independent **direct human visual** passes and is unchanged for ordinary venues. DENSE-VECTOR is permitted only when all of these hold: a current first-party issuer **vector** PDF; scope and configuration already settled from issuer evidence; every seat/range identifier explicitly printed; two **independent implementation/acquisition paths** (different access, extraction logic, floor order, block traversal and start/end pairing, no shared generation code, neither used as the other's answer key); machine comparison with **zero unresolved mismatch** across floor, row, every start/end, split segments, gaps, truncated rows and the per-floor and grand expanded seat-ID sets; a **visual audit of the rendered official chart** afterwards; and capacity never used as an extraction target. OCR or machine extraction accepted alone or unverified remains prohibited. **Currentness gate PASS**: the current top page carries an ongoing 2026-08/09 programme and 2026-07/08 news, with no 休館・閉館・改修・建替・営業終了 notice on the top, seat, floor or FAQ pages. **Asset gate UNCHANGED**: `seat-map.pdf` re-fetched fresh at SHA-256 `24a838884437b225bf19f6dbe4f40bf95e80204278f48b0528da4c7b603967ee` / 130,688 bytes — identical to the recorded digest, and a second independent fetch reproduced it byte for byte; the top, seat and floor page digests also match the previous record, so the settled configuration judgement was reused. **Configuration**: `scope.kind: fixed-only` over the issuer's permanent stands LEVEL3 ロアースタンド / LEVEL5 ミドルスタンド (including the issuer-printed バルコニー席) / LEVEL7 アッパースタンド; LEVEL1 アリーナ excluded as issuer-declared performance-dependent, the VIP floor excluded as 契約者専用, the unnumbered 3F 車イス席 platform excluded, LEVEL4 carries no numbered seating. **Pass A** interpreted the PDF content stream with a hand-written tokeniser, attributed every glyph to the issuer's own optical content groups (3F番号/5F番号/7F番号, 3F列/5F列/7F列), computed exact glyph advances from the PDF's own /Widths and /DW tables, and reconstructed baselines and printed ranges from the bimodal along-baseline gap structure (≤1.7 pt inside a label, ≥7.4 pt between segments): **1,033 segments**, 3F 320 / 5F 285 / 7F 428, zero anomalies. **Pass B** re-fetched the asset independently, took text geometry from pdfplumber/pdfminer.six (a different parser), detected the printed seat bars as connected components in a rendered raster (1,253 bars at the chart's 5.1 pt stroke width), identified floors from the chart's own background panels **named by the printed GATE numbers** (101-104 / 301-304 / 501-512 / 701-712), paired each bar's two numbers at its two **rendered ends** rather than by any gap threshold, and numbered rows by **counting rendered bands** between the issuer's printed row markers, traversing 7F→5F→3F and right→left: **1,033 records**, identical per-floor counts, zero problems, one declared discontinuity (the 5F centre has no rows 4-5; the balcony ends at row 3 and the main centre stand starts at row 6). One systematic pairing error was caught by the machine comparison, not papered over: Pass B's first run read some numbers right-to-left because the bar axis came from a sign-ambiguous principal-component fit; the pairing logic was corrected to read along the glyphs' own text direction, Pass B was regenerated, and Pass A was re-run from scratch and confirmed byte-identical. **Comparison MATCH with zero unresolved mismatch**: 1,033 vs 1,033 segments with none unique to either pass, 83 (floor, row) keys with none differing, identical split/gap/truncation structure across the 80 rows carrying internal gaps, and symmetric difference **0** on the per-floor expanded seat-ID sets (3F 4,978 / 5F 4,372 / 7F 6,579) and on the grand expanded set (15,929). **Visual audit**: the PDF was re-rendered from the vector source, and every section of **all 83 printed rows** (3F 1-29, 5F 1-26, 7F 1-28) was deskewed, cropped and read directly against the matched table — 100% coverage rather than sampling — with **zero discrepancies**, including the 車イス席 platform truncations on 3F rows 26-29, the most truncated row 3F 29 (which settles the previous session's unresolved reading: block 9-28 prints 14-24, and block 29-48 splits into 29-36 and 41-48 beside GATE 301), the GATE/stair truncations on 5F rows 11-17 and 7F rows 6-10, the twelve two-seat printed pairs, and the four-seat segments 5F row 17 87-90 and 151-154. **Reconciliation**: 3F **4,978** / 5F **4,372** (balcony 235 + main stand 4,137) / 7F **6,579**, grand mapped **15,929**. The issuer's 20,033席 is the whole-venue figure including LEVEL1 and is **not** a 3F/5F/7F subtotal; the difference was not closed, explained away, or used to add or remove a seat, and 20,033 was never an extraction target. Because the issuer publishes **no** upper-level subtotal, `expectedSeatCount` is `null` rather than an invented nominal figure, while `scope.exactSubtotal` carries the mapped 15,929 as the fixed-only contract requires. Wheelchair semantics are `not-reflected` with `accessibilityConversionNotReflected: true`. Seat and row numbers repeat across floors, so each floor has its own `areaId` (`3f-lower-stand` / `5f-balcony` / `5f-middle-stand` / `7f-upper-stand`); no block name the issuer does not define was created. Source `data/venue-sources/k-arena-yokohama.json` (schema v2, configuration `upper-stands-fixed`, 1,033 ranges), the inventory record (`eligible` / `production`, `venueSourceId: k-arena-yokohama`), the Kanto ledger, the production fingerprint and the generated catalog/runtime were synchronized. Machine verification of the released runtime artifact: 15,929 expanded seats, 15,929 unique runtime keys, **0 duplicates**, expanded set identical to both verified extractions in both directions, and **0** excluded 1F/VIP identifiers. The Kanto >=1,000 ledger is now **55 production / 2 ADOPT / 58 formal blocked / 39 inventory-unresearched**. Validation: the canonical `npm run verify:venues` passed (inventory/readiness/release-coverage reports, venue build/check/validate/report, lint, typecheck, 224 unit tests in 20 files, production build, docs and harness verification, diff check, and 17 Playwright E2E tests); `venues:validate` reports 44 warnings, one more than before, and the single new warning is the known 300-venue database size projection — no threshold was changed and no compaction was performed. Pushed production commit `8d295e4` (`main` / `origin/main` both at `8d295e441d4ba74141f70bbe2b15eb29ce400da9`, working tree clean); its managed **Workers Builds: seat-lottery-simulator** check run completed successfully. **Superseded by the DENSE-VECTOR wave above; retained as historical fact.** The `KANAGAWA-K-ARENA-YOKOHAMA BOUNDED VISUAL TWO-PASS IMPLEMENTATION` (2026-08-31) is complete and closes as a formal **HARD-CASE / JUDGMENT** record, not production. [`data/venue-reports/kanagawa-k-arena-yokohama-bounded-implementation-2026-08-31.json`](../data/venue-reports/kanagawa-k-arena-yokohama-bounded-implementation-2026-08-31.json) records it. It started from clean `main` / `origin/main` `73375a258003697fd7fedcf9e34a12ce0f1fcd4e`. **Baseline integrity**: the stale `venues:check` reported by the previous preflight is *not* a production-source inconsistency — it is line-ending normalization. This clone sets `core.autocrlf=true` and the repository has no `.gitattributes`, so any fresh checkout materializes the generated JSON with CRLF while `venues:build` writes LF, and the byte-comparison in `scripts/venues/check-database.mjs` then reports every generated payload stale while `git status` stays clean. A fresh checkout of this same commit produced `catalog.generated.json` at 106,749 bytes against 104,068 bytes in this working tree — a difference of exactly 2,681 bytes for 2,681 lines, one CR per line, content otherwise identical. At this HEAD `npm run venues:check` reports synchronized, so nothing was rebuilt, and no existing venue's seat set or fingerprint is affected. **Currentness gate PASS**: the issuer's current top page carries an ongoing 2026-08/09 programme and 2026-07/08 news, with no 休館・閉館・改修・建替・営業終了 notice on the top, seat, floor or FAQ pages. **Configuration gate PASS**: `https://k-arena.com/floor/` names LEVEL3 ロアースタンド, LEVEL5 ミドルスタンド and LEVEL7 アッパースタンド as permanent stands (and publishes 座席数：20,033席 for the whole venue), while `https://k-arena.com/seat/` states 「※公演ごとにアリーナ席の形状やトイレの男女別配置が変更になることがございます。」 — so 1F アリーナ is issuer-declared performance-dependent and excluded, the VIP floor is 「契約者専用フロア」 and excluded, and a `scope.kind: fixed-only` upper-level configuration is definable. Crucially, the issuer's linked `seat-map.pdf` (SHA-256 `24a838884437b225bf19f6dbe4f40bf95e80204278f48b0528da4c7b603967ee`, 130,688 bytes, `240808_seatmap`, per-floor 番号/列 layers plus separate 車椅子 and VIP layers) — **not** the web level images — prints an explicit start and end seat number for **every individual row**; the web level/zoom PNGs publish only block-level ranges and each block's first and last row, so they are insufficient on their own. **The fresh visual two-pass was NOT completed.** That page prints 1,357 row-range labels, roughly 1,100 of them on 3F/5F/7F, and each seating block is drawn at its own rotation in a fan projection, so a block must be deskewed before its start and end labels can be paired safely — reading without that step produced demonstrably wrong pairings. Pass 1 covered the whole 3F centre and most of the 3F left wing; the 3F right wing, all of 5F and all of 7F were not started and Pass 2 was not started, so **no MATCH, no per-floor subtotal, and no grand mapped subtotal is claimed** and capacity reconciliation was deliberately not performed. Nothing was inferred, geometry-completed, gap-filled, or fitted to 20,033. **No source JSON, range, fingerprint, or generated artifact was created; production is unchanged at 137 venues / 139 selectable configurations / 190,099 configuration-seat records.** The Kanto >=1,000 ledger is now **54 production / 2 ADOPT / 59 formal blocked / 39 inventory-unresearched**, recomputed from its own records rather than fitted to the pre-authorization expectation of 55 / 2, which was conditional on this venue reaching production. The report's `validatedMethodForReopen` records the working reading recipe (per-wing deskew — 44° for the 3F left wing — then one-row band composites anchored on the issuer's printed row markers) so the reopen does not have to rediscover it.

The `KANTO >=1000 HIGH-IMPACT UNRESEARCHED PREFLIGHT 2` (2026-08-30) is complete: [`data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-2-2026-08-30.json`](../data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-2-2026-08-30.json). It started at clean detached `HEAD` / `origin/main` `70fb941f98b653b10faa9622db71fce4e56fda62` with **54 production / 0 ADOPT / 58 formal blocked / 42 inventory-unresearched** in the 154-record Kanto >=1,000 ledger and production unchanged at **137 / 139 / 190,099**. It reranked only those 42 records and screened current first-party operation, closure/renovation/reconstruction/cessation/reopening, and facility-wide versus space-specific effect **before** seat evidence for every opened candidate. Research stopped at the third ADOPT: (1) `kanagawa-k-arena-yokohama` Kアリーナ横浜 — active current program and a current issuer seat map; 1F is explicitly performance-dependent, so only a later-proven complete upper-level numbered configuration may be considered; (2) `saitama-raiboc-main` RaiBoC Hall〔大ホール〕 — active/current booking through 2027, 1,400-seat purpose-built hall, issuer-linked seat chart; (3) `chiba-pref-culture-main` 千葉県文化会館〔大ホール〕 — active after the completed 2025 reopening, issuer now states 1,728 seats including four wheelchair positions and links the current seating plan. There was no currentness HOLD in these three. **No transcription, source JSON, ranges, fingerprints, generated artifacts, production integration, or production total changed.** The ledger is now **54 production / 3 ADOPT / 58 formal blocked / 39 inventory-unresearched** (`missingFromInventory: 0`); only the three opened inventory records changed to `source-located` / `likely-eligible`, and the former Chiba discovery capacity 1,787 was corrected to the current issuer's 1,728. Validation: JSON parse, `npm run venues:inventory:report`, and `npm run venues:validate` passed; target `venues:review` commands correctly stop because preflight creates no source JSON. `venues:check` reports the repository's generated production payloads stale and `verify:docs` reaches the harness but fails its Windows `git status` subprocess, so neither was used to modify generated artifacts. The exact next action is **Claudeの separately authorized bounded visual two-pass implementation of at most `kanagawa-k-arena-yokohama`, `saitama-raiboc-main`, and `chiba-pref-culture-main`**. Without that authorization, continue the next Kanto inventory-unresearched high-impact preflight; do not reopen known HARD-CASE work or Tokyo Priority D while Kanto inventory-unresearched remains.

The `SAITAMA-CITY-CULTURE-MAIN BOUNDED PRODUCTION IMPLEMENTATION` (2026-08-29) is complete and closes with a formal **RENOVATION / CURRENTNESS HOLD**, not production. [`data/venue-reports/saitama-city-culture-main-bounded-implementation-2026-08-29.json`](../data/venue-reports/saitama-city-culture-main-bounded-implementation-2026-08-29.json) records it. Starting from clean `main`/`origin/main` `9ebcd00d1612eb24d51a5958a0b2dbe53ce99e16`, the mandated currentness re-confirmation failed at the first gate: the operator's own current facility page (`https://saitama-culture.jp/culturecenter/`) publishes 「中規模修繕工事実施に伴う休館のお知らせ ... 令和9年6月1日から休館となります ... 休館中は利用受付及び施設利用並びに敷地内への立入りはできません ... 工事期間については、2年間程度を予定」, and the City of Saitama's own facility page states the same closure independently. The 2027-06-01 facility-wide closure is 9 months after the review date, inside the 12-month threshold in [`VENUE_DATA_GUIDE.md`](VENUE_DATA_GUIDE.md), which blocks such a venue in principle and forbids settling the unstated question of whether the work changes the seating by inference. The 2026-08-27 preflight had missed this notice, which predates it; the ADOPT was therefore corrected on current evidence. The configuration assessment was still completed and preserved: the single linked 座席表（大ホール）(`culturecenter_zaseki01.pdf`) is the **A仕様** chart — its printed header 固定席1,842／移動席156／車椅子席8／計2,006席 and its printed floor subtotals 1階1,328・2階330・3階348 sum to exactly the published A capacity 2,006 — while **B仕様1,658席** publishes only an aggregate with no numbered set and no statement of which positions it removes, so A is the only issuer-evidenced representative configuration and B is not implementable without invented differences. The 8 車椅子席 are drawn unnumbered in two 1F blocks and sit **inside** the published 2,006 (1,842 + 156 + 8), with no stated replacement relation; the 156 移動席 are identified by the issuer's 断面図/客席図 legend as 移動席(オケピ) inside the same A configuration. The arithmetic identity 2,006 − 1,658 = 348 = the printed 3階客席 subtotal is explicitly **not** adopted as evidence for what B removes. Because the currentness gate failed, **no fresh visual two-pass was performed and none is claimed**, and no source JSON, range, fingerprint, generated artifact, or production integration was created. Production is unchanged at **137 venues / 139 selectable configurations / 190,099 configuration-seat records**. The Kanto >=1,000 ledger is now **54 production / 0 ADOPT / 58 formal blocked / 42 inventory-unresearched**, recomputed from its own records rather than fitted to the pre-authorization expectation of 55 / 0 / 57 / 42, which was conditional on this venue reaching production.

The `IBARAKI-MITO-CIVIC-MAIN BOUNDED PRODUCTION IMPLEMENTATION` (2026-08-29) is complete and closes with **PRODUCTION**. Starting from clean `main`/`origin/main` `3cc8805b1981bd650debc5544c150ef9fd870ef5`, current official evidence was re-verified: `https://www.mito-hall.jp/outline/hall-l.html` (2,000 seats: 1F 1,293 / 2F 333 / 3F 374; up to 26 wheelchair positions; issuer states wheelchair placement varies by event) and `https://www.mito-hall.jp/organizer/download.html` (links to the current `floormap_sheet_1f.pdf` / `_2f.pdf` / `_3f.pdf` / `_wheelchair.pdf`, all `rev.20230616`). All four PDFs were freshly fetched and SHA-256-verified; each floor's printed seat numbers were extracted twice by independent code paths (header-anchored nearest-column assignment vs. header-independent x-coordinate gap clustering, one pass reading 3F→2F→1F with each column bottom-to-top) and the two passes matched on every printed ID, row/column ownership, range, and gap with zero mismatches. Per-floor mapped totals reconcile losslessly to the issuer's published breakdown: 1F 1,293 (main numbered grid 1,277 across columns 1–31, plus the officially printed 親子室A A-5〜A-8, 親子室B B-1〜B-4, and R/L movable-chair boxes 1〜4 each), 2F 333 (main grid 295 across columns 1–7 plus R-row and L-row 1–19 each), 3F 374 (main grid 336 across columns 1–8 plus R-row and L-row 1–19 each); grand total 2,000, exactly matching the issuer's stated capacity. The dedicated wheelchair-configuration PDF was read and found to describe a separate, event-dependent alternate layout (1F 22 / 2F 4 wheelchair positions replacing whole movable-chair row-bands, stated only as aggregate seat-count reductions, not as individual seat-ID replacements) consistent with the outline page's explicit "car​ いす席は催事内容によって図面と異なる位置に設置される場合があるため、主催者への事前確認が必要です" disclosure; per `docs/VENUE_DATA_GUIDE.md`'s event-dependent-wheelchair guidance, no wheelchair configuration or seat-ID replacement was created, and `wheelchairSemantics.status` is recorded as `not-reflected` with `accessibilityConversionNotReflected: true`. The 1F row-letter labels (N–A, R/Q, O/P) were confirmed to be structural/architectural reference marks — their spacing does not match the seat-number row pitch — and are not used as `rowLabel`; registered row labels are the chart's own per-cell column numbers. Source `data/venue-sources/mito-civic-globis-hall.json` (schema v2, configuration `standard`), inventory record `ibaraki-mito-civic-main` (now `eligible` / `production`, `venueSourceId: mito-civic-globis-hall`), the Kanto ledger, the production fingerprint, and the generated catalog/runtime were synchronized; `npm run venues:check`, `npm run venues:validate` (no new warnings attributable to this venue), and the canonical `npm run verify:venues` (224 unit tests, 17 E2E tests) all passed before push. Pushed production commit `fdb0e72` (`main`/`origin/main` both at `fdb0e728f603115ef51d57d84b9254a37f13db8e`, working tree clean); its managed Workers Build (`e18d829c-3610-43ff-85e1-090fae9410c8`) completed successfully. The public UI searched and selected 水戸市民会館〔グロービスホール〕, displayed 抽選対象 2,000席 (matching the mapped count), and completed five draws spanning 1F and 3F (`1階 23列27番`, `1階 7列35番`, `1階 15列5番`, `3階 1列3番`, `3階 5列22番`) with floor-qualified area/row/seat labels and no browser console warning or error. The one exact next action is: **separately authorized bounded two-pass implementation of `saitama-city-culture-main` さいたま市文化センター〔大ホール〕**.

The `IBARAKI-MITO-CIVIC-MAIN PRODUCTION TWO-PASS COMPLIANCE REMEDIATION` (2026-08-29) is complete and closes with **PRODUCTION — DATA UNCHANGED**. It was opened for one reason only: the wave above satisfied its two independent passes with two *programmatic* embedded-text extractions of the same PDFs, whereas the production contract asks for the current official seat chart to be read directly, twice, as visual evidence. Scope was that compliance gap alone; no re-derivation of the released data was attempted. Currentness was re-confirmed against the operator's own current pages (`https://www.mito-hall.jp/outline/hall-l.html`, still 令和5年7月2日現在 with 2,000席 / 1階1,293・2階333・3階374 / 車椅子席最大26席 and the "車いす席は催事の内容によって、図面とは別の場所に設置する可能性" note, and `https://www.mito-hall.jp/organizer/download.html`, which still links the same four charts). All four PDFs were re-fetched (`floormap_sheet_1f.pdf` SHA-256 `c48d2ac60f43489bb9d7553c0bae34477a5ed0639c57579e31eb09635d5ec767`, `_2f` `5088c204c50b357b55ac7a7fd8cf9debe1e88712072c07cc2bfda894807c465a`, `_3f` `b01a0cb70491e1df6bcc6108a3149983d686fe2f7e57854b1b04d4755c602255`, `_wheelchair` `83617f349bcdc64c03c8e1c96610278138f5ad51f39487b239810d095f4d2194`, all `rev.20230616` / CubePDF 2023-06-16), and a second independent fetch reproduced the same four digests byte for byte. **Visual Pass 1** rendered each chart to high-resolution raster tiles and read the printed identifiers directly under zoom and crop, floor order 1F → 2F → 3F, each block read from its high-numbered end; it was written to a frozen record before Pass 2 began. **Visual Pass 2** re-fetched the assets, re-rendered them rotated 180° so the sheets read in the opposite orientation, and re-transcribed them in reverse order 3F → 2F → 1F with every block read from its low-numbered end. Neither pass used embedded-text extraction, the earlier programmatic output, or the existing source ranges as its answer key; the issuer's published per-floor totals were treated only as the issuer's own capacity statement for reconciliation after transcription, never as a target, and no number was fitted to them. The two visual passes **MATCH** on the complete printed identifier set, floor/area ownership, row and column identity, every range, every gap/exclusion (even rows missing the centre-block top number — 1F 32, 2F/3F 33 — 1F rows 1–8 lacking 1–4 and 41–44, 1F row 31 lacking 1–2/11–12/33–34/43–44, and the unnumbered hatched cells in 2F row 7 and 3F rows 7–8), 親子室A A-5〜A-8 and 親子室B B-1〜B-4, the R/L 区画 (1F 1〜4 each; 2F/3F R/L rows 1〜19 each), the floor subtotals **1F 1,293 / 2F 333 / 3F 374**, and the grand total **2,000**. Compared afterwards against the released `data/venue-sources/mito-civic-globis-hall.json`, all 54 ranges and 2,000 mapped seats matched with **zero differences**, so the existing production data was kept exactly as released. The wheelchair chart's own arithmetic corroborates the standard-layout totals independently (1F 車イス席22席 / 一般席1,229席(-64席) / 計1,251席(-42席) reproduces 1,293; 2F 車イス席4席 / 一般席321席(-12席) / 計325席(-8席) reproduces 333; its per-block notes 「一般席(可動式)を2列撤去し…24席減少」 and 「1列撤去…8席減少」/「…6席減少」 match the visually counted 12-seat, 8-seat and 6-seat blocks). The earlier wave's two-method programmatic extraction is retained as **supplementary** verification and now serves as a third, agreeing cross-check; this fresh visual two-pass is the **production compliance verification**. Wheelchair semantics were re-confirmed on fresh evidence: the wheelchair PDF is an event-dependent alternate layout whose 22 (1F) + 4 (2F) positions are lettered A–V and W–Z and are quantified only as whole-row 一般席 reductions, with no issuer-defined per-seat replacement relation, so `wheelchairSemantics.status: not-reflected` and `accessibilityConversionNotReflected: true` stand unchanged and no numbered seat was deleted and no wheelchair seat ID created. One non-material metadata imprecision was recorded but deliberately **not** edited, to avoid touching released production data on a matched outcome: the free-text `wheelchairSemantics.description` describes the two 7-position 1F blocks as "H〜K列相当4列" and "B〜E列相当4列", whereas the charts show them lettered H–N and A–G over exactly two seat rows (列9 and 列10), which is what the issuer's own 「2列撤去…24席減少」 states. No source, range, inventory, batch, fingerprint, or generated artifact changed in this remediation; the only change is this file.

The `KANTO RELEASE-CRITICAL >=1000 MUST-COVERAGE AUDIT` (2026-08-27) ledger is [`data/venue-coverage/kanto-must-coverage-audit-2026-08-27.json`](../data/venue-coverage/kanto-must-coverage-audit-2026-08-27.json): **154** current facility-space records (Tokyo 91, Kanagawa 18, Chiba 13, Saitama 12, Ibaraki 5, Tochigi 5, Gunma 10), now 54 production, 1 ADOPT / implementation-ready, 57 formal HOLD/NEED-EVIDENCE/DEFER, and 42 inventory-unresearched; `missingFromInventory` is **0**. `kanagawa-pia-arena-mm` moved to **production** on 2026-08-27 and `ibaraki-mito-civic-main` moved to **production** on 2026-08-29; the ledger `nextMacro` now names only the one remaining ADOPT, `saitama-city-culture-main`. The new [`KANTO >=1000 HIGH-IMPACT UNRESEARCHED PREFLIGHT`](../data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-2026-08-27.json) formally triaged only the initial 45 unresearched records and stopped after three first-party ADOPTs: `kanagawa-pia-arena-mm`, `ibaraki-mito-civic-main`, and `saitama-city-culture-main`. It corrected the ledger-only alias `tochigi-tochigi-culture-main` to the existing canonical inventory `tochigi-ken-sogo-bunka-center-main`, without adding an inventory record. The ledger's stale hard-case `nextMacro` is now retained as `supersededNextMacro`, while its current `nextMacro` matches this ADOPT implementation action. No source JSON, range, generated artifact, fingerprint, or production data changed by the preflight itself. The corrected release rule is to prioritize formal triage of unresearched release-critical >=1,000 venues and finding implementation-ready candidates before rereading known unresolved hard cases. `TACHIKAWA STAGE GARDEN` remains distinct `tokyo-tachikawa-stage-garden`; SGC HALL ARIAKE remains `tokyo-coverage-sgc-hall-ariake` SOURCE/POLICY HOLD; and `tokyo-official-1017` アリーナ立川立飛 remains a distinct NEED EVIDENCE hard case, reopening only if the issuer publishes new numbered-seat material resolving the missing row/seat labels and 526-versus-528 movable-bank arithmetic. The existing formal blockers for `saitama-kaikan-main` and `omiya-sonic-city-large` were retained without rereading.

The `TOKYO-OFFICIAL-0278 GLOBE TOKYO BOUNDED ISSUER-EVIDENCE JUDGMENT AUDIT` (2026-08-27, [`data/venue-reports/tokyo-official-0278-globe-tokyo-judgment-audit-2026-08-27.json`](../data/venue-reports/tokyo-official-0278-globe-tokyo-judgment-audit-2026-08-27.json)) is complete and its disposition was **ADOPT**, reversing the record's HARD-CASE DEFER. It was an audit-only wave: **no venue source, seat range, generated artifact, or fingerprint was touched**, and production was unchanged at 134 / 136 / 181,827 by it. Only that report, the `tokyo-official-0278` inventory record, and this file changed. It did **not** authorize an implementation; the separately authorized implementation is TOKYO-WAVE-34 below, and `tokyo-official-0278` is now PRODUCTION.

The `TOKYO PRIORITY D BOUNDED PORTFOLIO / LANE AUDIT` (2026-08-27, `data/venue-reports/tokyo-priority-d-portfolio-lane-audit-2026-08-27.json`) is complete. It is an audit-only wave: **no venue data, inventory disposition, generated artifact, or fingerprint was touched**, and production is unchanged at 134 / 136 / 181,827. Its decision is **CONTINUE TOKYO ORDINARY RESEARCH** under a corrected research-ordering rule; see the section below.

The authorized `VENUE-SOURCE TRANSPORT POLICY IMPLEMENTATION` is complete. It is a policy-only change: **no venue data, fingerprint, or generated artifact was touched**, and production was unchanged by it at the **132 venues / 134 selectable configurations / 181,441 configuration-seat records** current in that wave.

The new contract replaces the former unconditional `sources[].url` HTTPS requirement. A recorded URL is provenance and audit metadata only — the repository audit confirmed that no build step and no runtime path dereferences it, that the generated catalog and the 134 runtime payloads contain no URL at all, and that no runtime type carries a `sources` field — so transport is now judged as evidence-record quality rather than as a user-facing link control. HTTPS stays mandatory wherever the issuer serves it. A current first-party official issuer that serves no working HTTPS endpoint may now be recorded at its real `http://` URL under an explicit, machine-checkable `httpOnlyOfficial` waiver holding `issuerDomain` (must match the URL host or a host below it), `httpsCheckedAt` (valid, non-future), and `httpsUnavailableEvidence`. Waiverless HTTP, non-official HTTP, incomplete waivers, issuer-domain mismatch, malformed URLs, and a waiver left on an HTTPS URL are all still errors; an accepted HTTP source raises a standing warning, and the waiver expires into a recheck warning after `WARNING_LIMITS.httpsRecheckDays` (180). Rewriting an HTTP issuer URL to an invented HTTPS address stays prohibited, and the waiver relaxes no seat-evidence, currentness, issuer-ownership, or configuration requirement.

The policy has one implementation, [`scripts/venues/source-transport.mjs`](../scripts/venues/source-transport.mjs), shared by all three gates that previously enforced HTTPS separately: the source validator (`scripts/venues/validation.mjs`), the inventory `officialFacilityUrl` gate (`scripts/venues/inventory.mjs`, which applies the same waiver shape to inventory records), and the production database test (`src/data/venue-db/venueDatabase.test.ts`, which now evaluates the shared predicate instead of asserting an `https://` prefix). Changing the contract means changing that one module.

`tokyo-official-0635` ユーロライブ is **PRODUCTION** as canonical `eurolive-standard`. Its former SOURCE/POLICY HOLD is resolved: the transport policy above unblocked it, and TOKYO-WAVE-32 then integrated it at 178 mapped / 178 published / rangeDiff 0 under an explicit `httpOnlyOfficial` waiver, in pushed production commit `6c6778e`.

TOKYO-WAVE-24 ordinary bounded integration is released. Its data commit `9d2a66cd2bdfc3738bd3e0513288d1d52da6fe68` is pushed in main history. The current bounded Priority-D implementation began from clean `main` / `origin/main` `dfbd52132ee4bd0daa009c88cafce1f54d80d09f`; no reset or revert was performed.

`TOKYO-WAVE-27 PRIORITY D HIGH-YIELD ADOPT IMPLEMENTATION` is complete with **3 PRODUCTION / 0 HOLD**. [`data/venue-reports/tokyo-priority-d-adopt-implementation-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-adopt-implementation-2026-08-25.json) records the independent current-chart transcriptions: RISURU小ホール is 246 mapped / 246 published / rangeDiff 0 with all 110 removable printed positions retained; 杉並公会堂小ホール is 190 mapped / 194 published / rangeDiff -4 with four unnumbered wheelchair spaces only as metadata; and 大泉学園ゆめりあホール is 176 mapped / 176 published / rangeDiff 0, including the issuer-labelled wheelchair IDs 1-6. The source, inventory, batch/readiness, report, production fingerprint, catalog and lazy runtime artifacts are synchronized at **125 venues / 127 selectable configurations / 179,940 configuration-seat records** in production commit `66fc2c6`. Canonical verification passed (213 unit tests, 17 E2E tests); its managed Workers Build completed successfully, and the public UI searched all three at 246 / 190 / 176 seats, completed one RISURU draw, and had no console warning or error. No new candidate research was started.

The authorized `TOKYO PRIORITY D RANKED FOLLOW-UP PREFLIGHT` is complete with **3 ADOPT / 0 DEFER / 0 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-d-ranked-followup-preflight-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-ranked-followup-preflight-2026-08-25.json) checked only the prescribed research order and stopped after rank 3: `1036` 吉祥寺シアター basic-stage 189, `0133` 赤坂RED/THEATER basic-stage 176, and `0282` 矢来能楽堂 published diagram 237 each have current/current-linked official complete numbered evidence for a separately authorized two-pass candidate. `0647` セルリアンタワー能楽堂 and `0238` 音楽の友ホール were not opened or adjudicated after the stop condition. No source JSON, range, generated artifact, fingerprint, production data, or production total changed.

`TOKYO-WAVE-28 PRIORITY D RANKED ADOPT IMPLEMENTATION` is complete with **3 PRODUCTION / 0 HOLD**. [`data/venue-reports/tokyo-priority-d-ranked-adopt-implementation-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-ranked-adopt-implementation-2026-08-25.json) records direct independent current-chart transcriptions: 吉祥寺シアター〔劇場〕 basic stage is 189 mapped / 189 published / rangeDiff 0 (A-I, BL/BR, J); 赤坂RED/THEATER basic stage is 176 / 176 / 0 (A/B movable-chair notation, unnumbered E-column wheelchair space, and printed F12-14 movable-chair context retained without inferred replacement); 矢来能楽堂 is 237 / 237 / 0 (正面64, 座敷正面32, 脇正面49, 中正面52, 座敷中正面40; tatami annotations retained). Source, inventory, batch/readiness, reviewed fingerprints, catalog, and lazy runtime are synchronized at **128 venues / 130 selectable configurations / 180,542 configuration-seat records** in pushed production commit `d2dc595`. Canonical `npm run verify:venues` passed (213 unit tests, 17 E2E tests). Its managed Workers Build `c8a1eda6-2dc7-41a9-bf1c-ddc5ee1759c2` completed successfully; the public UI searched all three at 189 / 176 / 237 seats, completed one draw each, and had no console warning or error.

`TOKYO PRIORITY D NEXT HIGH-YIELD PREFLIGHT` is complete with **2 ADOPT / 2 DEFER / 1 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-d-next-high-yield-preflight-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-next-high-yield-preflight-2026-08-25.json) first checked the mandated carry-overs: `0647` セルリアンタワー能楽堂 is **ADOPT** from its current 201-seat official chart (正面87 / 脇正面58 / 中正面56), while `0238` 音楽の友ホール is **DEFER** because the active operator defines only a variable maximum208 movable-chair capacity and no current complete numbered representative set. Metadata-only reranking then checked `0565` シアタートラム (**DEFER**: its basic225 chart has unnumbered intermediate boxes), `0278` 東京グローブ座 (**HARD-CASE DEFER**: complete 707-seat floor charts but an unreconciled unnumbered-wheelchair/subtotal relation), and `0222` 紀伊國屋ホール (**ADOPT**: current-linked complete 427-seat A-V chart, with printed removable/possible-wheelchair I/J positions retained). Research stopped at the five-candidate ceiling, not at three ADOPTs. Only report/inventory/HANDOFF changed; production remains **128 venues / 130 selectable configurations / 180,542 configuration-seat records**.

`TOKYO-WAVE-29 PRIORITY D NEXT ADOPT IMPLEMENTATION` is complete with **2 PRODUCTION / 0 HOLD**. [`data/venue-reports/tokyo-priority-d-next-adopt-implementation-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-next-adopt-implementation-2026-08-25.json) records the direct comparisons. セルリアンタワー能楽堂 is **201 mapped / 201 published / rangeDiff 0**: 正面87, 脇正面58, 中正面56; the operator's removable-seat use remains outside the standard fixed-seat configuration and no accessibility relation was inferred. 紀伊國屋ホール is **427 / 427 / 0**: all A-V printed IDs and official drawn left/center/right seat blocks remain mapped; I/J 1・2・19・20 remain selectable printed positions, with the official removable/possible-wheelchair-use note retained as metadata and no replacement inferred. Source, inventory, Wave29 batch/readiness, reviewed fingerprints, catalog, and lazy runtime are synchronized at **130 venues / 132 selectable configurations / 181,170 configuration-seat records** in pushed production commit `bcc0ff6`. Canonical `npm run verify:venues` passed (213 unit tests, 17 E2E tests). Its managed Workers Build `07036eae-7a48-41ef-8cd2-2c081b3175cf` completed successfully; the public UI searched both venues at 201 / 427 seats, completed one draw each, and had no console warning or error.

`TOKYO-WAVE-30 PRIORITY D RANKED NEXT ADOPT IMPLEMENTATION` is complete with **1 PRODUCTION / 0 HOLD**. [`data/venue-reports/tokyo-priority-d-ranked-next-adopt-implementation-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-ranked-next-adopt-implementation-2026-08-25.json) records two independent current-chart transcriptions of the single authorized ADOPT: シアターグリーン〔BIG TREE THEATER〕 is **167 mapped / 167 published / rangeDiff 0** (A-F列 each 1-17 across three issuer blocks; G列 5-15 with no left block; H-J列 each 3-14; K列 5-14; L列 6-13). Pass 1 read top-down left-to-right, pass 2 independently reread bottom-up right-to-left before any comparison, and both matched on the complete printed set, row/block ownership, ranges/gaps, the issuer's 167 subtotal, and the selected configuration; the current chart carries no wheelchair, removable, or accessibility marking to reflect. Source, inventory, Wave30 batch/readiness, fingerprint, catalog, and lazy runtime are synchronized at **131 venues / 133 selectable configurations / 181,337 configuration-seat records** in pushed production commit `ffea65d`. Canonical `npm run verify:venues` passed (213 unit tests, 17 E2E tests); its managed Workers Build completed successfully, and the public UI found シアターグリーン〔BIG TREE THEATER〕 displaying 167 seats, completed one draw (`D列 6番`), and had no console warning or error.

The authorized `TOKYO PRIORITY D RANKED NEXT PREFLIGHT` is complete with **1 ADOPT / 3 DEFER / 1 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-d-ranked-next-preflight-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-ranked-next-preflight-2026-08-25.json) reranked the remaining unresearched Priority-D inventory from repository metadata alone under the established performance-oriented rule (822 records with `venueSourceId` null → 289 with a direct URL → 39 with a dedicated performance identity) and checked ranks 1-5 in order. `0859` シアターグリーン〔BIG TREE THEATER〕 is **ADOPT**: the current operator page and its direct 客席図面 define the fixed 167-seat configuration with a complete printed set (A-F 1-4/5-14/15-17, G 5-14 and 15, H-J 3-14, K 5-14, L 6-13) totalling 167, with no wheelchair or removable marking to infer. `0586` 本多劇場 is **HARD-CASE DEFER**: the current-linked official 座席表 prints a complete A-Q set of 390 positions in issuer left/centre/right blocks while the operator states 客席数386席, and the four printed positions M1, M24, Q8 and Q17 carry an unexplained diagonal mark with no legend, so neither retaining nor deleting them is supportable. `0115` 自由劇場 is **DEFER** because the operator publishes only per-production charts whose printed numbered sets differ between productions plus an approximate 約500席. `0476` 中目黒キンケロ・シアター is **DEFER** because its current 図面一式20260702版 and site publish no seat IDs and no 客席数. `0867` 萬劇場 is **DEFER** because its current-linked 座席表 numbers only rows E-J while A, B, C, D and K are drawn unnumbered and capacity is published as variable. Research stopped at the five-candidate ceiling, not at three ADOPTs. The mandated carry-over dispositions were excluded from the pool and untouched. Only report/inventory/HANDOFF changed; production remains **130 venues / 132 selectable configurations / 181,170 configuration-seat records**.

TOKYO-WAVE-25 preflight is formally closed with **0 ADOPT / 6 DEFERRED**. [`data/venue-reports/tokyo-wave-25-2026-08-24.json`](../data/venue-reports/tokyo-wave-25-2026-08-24.json) preserves the completed current first-party evidence for `0726`, `0993`, `0989`, `0561`, `0426`, and `0708`; their inventory records are `blocked` so they cannot re-enter ordinary candidate discovery. No venue source, range, catalog, runtime artifact, fingerprint, or production total changed.

The authorized `NEXT TERRA PREFLIGHT` is now formally closed with **0 ADOPT / 4 DEFERRED / 2 HARD-CASE DEFER**. [`data/venue-reports/tokyo-next-terra-preflight-2026-08-24.json`](../data/venue-reports/tokyo-next-terra-preflight-2026-08-24.json) preserves the completed first-party evidence for `0373`, `0425`, `0709`, `0611`, `1017`, and `1058`; their inventory records are `blocked` so they cannot re-enter ordinary discovery. None is permanently rejected.

The bounded `JUDGMENT` hard-case audit of `0611` and `1017` is complete and both are **NEED EVIDENCE**. For 駒沢体育館, the issuer map labels the row in each visible block (A-K), but prints no individual seat numbers; its 2,354-seat map also does not reconcile to the current guide's fixed 2,238 + wheelchair 24 + amenity 12 + temporary maximum 1,152 scope without choosing an unsupported generation/scope. For アリーナ立川立飛, the current-linked official layout defines 1F/2F and movable/court-side/wheelchair/media/camera subtotals, but prints no seat IDs or row labels; its movable-bank arithmetic is 108 x 2 + 155 x 2 = 526 against the printed 528 movable subtotal. Geometry or capacity fitting cannot resolve either venue. No source JSON or implementation was created.

The legacy `TYPE-ONLY A/B REPRESENTATIVE` lane is now **closed**: 9 original eligible records result in **1 PRODUCTION / 8 DEFER / 0 HARD-CASE DEFER** under the current representative-layout policy. [`data/venue-reports/tokyo-type-only-representative-preflight-2026-08-25.json`](../data/venue-reports/tokyo-type-only-representative-preflight-2026-08-25.json) records the initial six checks; [`data/venue-reports/tokyo-type-only-ab-lane-closure-2026-08-25.json`](../data/venue-reports/tokyo-type-only-ab-lane-closure-2026-08-25.json) closes the remaining `0359`, `0070`, and `0434`. All eight DEFER records are `blocked`, so they cannot re-enter ordinary discovery. `0359` has a current official fixed-1,200-seat statement but no issuer-numbered chart/list; `0070` and `0434` have no current issuer-numbered set. Outdoor use, movability, and multiple layouts were not treated as rejection reasons.

The one-record `TOKYO A/B SOURCE-LOCATED` lane is **closed** with **0 ADOPT / 1 DEFER**. [`data/venue-reports/tokyo-source-located-lane-0374-next-selection-2026-08-25.json`](../data/venue-reports/tokyo-source-located-lane-0374-next-selection-2026-08-25.json) reuses the current first-party material from sibling `0373` and records `0374` (すみだ産業会館〔タイプB〕): the B page has only movable chair-count capacities and the linked A/B/C plan's numbers are grid/equipment references, not issuer seat IDs. No Type-B-only numbered representative set exists in the current material, so `0374` is `blocked` and cannot re-enter ordinary discovery. The report also records the machine aggregation: unresolved A/B is now only 2 A and 1 B not-started records, while the next high-yield lane is the two independent C source-located halls `0950` and `1186`; no Web research was started for them.

The bounded `TOKYO PRIORITY C SOURCE-LOCATED INDEPENDENT-HALL` preflight is formally closed with **0 ADOPT / 3 DEFER / 0 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-c-source-located-preflight-2026-08-25.json`](../data/venue-reports/tokyo-priority-c-source-located-preflight-2026-08-25.json) first closes sibling `0375` from the already-confirmed 0373/0374 A/B/C material: no Type-C-specific issuer-numbered set exists. `0950` has current official movable-use/chair-count material but no issuer-numbered seat set. `1186` has a current city page linking the operator, but the operator site and legacy PDF URL could not be safely verified as current/current-linked; no legacy or third-party chart was adopted. All three inventory records are `blocked`, none is permanently rejected, and no production data changed.

The authorized `TOKYO PRIORITY C TRUE-UNPROCESSED` bounded preflight is formally closed with **3 ADOPT / 0 DEFER / 0 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-c-true-unprocessed-preflight-2026-08-25.json`](../data/venue-reports/tokyo-priority-c-true-unprocessed-preflight-2026-08-25.json) ranks eight of the 18 true-unprocessed records from repository metadata and checks only `0646`, `1115`, and `0101`; research stopped at the three ADOPTs.

`TOKYO-WAVE-26` has completed the separately authorized bounded two-pass implementation for all three ADOPTs. [`data/venue-reports/tokyo-priority-c-adopt-implementation-2026-08-25.json`](../data/venue-reports/tokyo-priority-c-adopt-implementation-2026-08-25.json) records independent range transcriptions, comparison results, current sources, and decisions: `tokyo-wave3-0646` 伝承ホール is **PRODUCTION** at 339 mapped / 345 published / rangeDiff -6 (fixed 125 + movable 122 + zashiki 92; unnumbered parent 4 and wheelchair 2 remain metadata), `tokyo-wave3-1115` ルネこだいら中ホール is **PRODUCTION** at normal-use 398 mapped / 401 published / rangeDiff -3 (three wheelchair spaces and dotted additions remain outside the mapped normal scope), and `tokyo-wave3-0101` 浜離宮朝日ホール小ホール is **PRODUCTION** at 398 mapped / published / rangeDiff 0 (movable 238 + rollback 160). The independent two-pass comparisons remain MATCH fields, not rangeDiff. Source, inventory, batch/review/report, generated artifacts, and fingerprints are synchronized. Priority C true-unprocessed is now **15**.

The authorized `TOKYO PRIORITY C RANKED PREFLIGHT` is formally closed with **0 ADOPT / 5 DEFER / 0 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-c-ranked-preflight-2026-08-25.json`](../data/venue-reports/tokyo-priority-c-ranked-preflight-2026-08-25.json) preserves current first-party checks for `0135`, `0331`, `0908`, `0559`, and `0870`; all five inventory records are now `blocked`, so they cannot return to ordinary discovery. `0135`'s current page links a seat-chart URL that returned PAGE NOT FOUND; `0331` publishes 393 fixed plus two wheelchair spaces but no current numbered chart; `0908`, `0559`, and `0870` publish variable-layout capacities/plans, not complete issuer-numbered audience-seat sets. No production data changed. Priority C true-unprocessed is now **10**.

`TOKYO PRIORITY C METADATA CONSISTENCY CLEANUP` is complete. [`data/venue-reports/tokyo-priority-c-metadata-consistency-cleanup-2026-08-25.json`](../data/venue-reports/tokyo-priority-c-metadata-consistency-cleanup-2026-08-25.json) normalizes stale `draft-created` inventory states for `0008` 神田明神文化交流館・神田明神ホール and `0030` 二松学舎大学 中洲記念講堂 to `blocked`, preserving their existing repository-evidenced **SOURCE HOLD** dispositions. The canonical Priority-C true-unprocessed count is therefore 8 before the new preflight. It also corrects seven current source `rangeDiff` fields mechanically to `mapped - published` (`+10`, `-14`, `+4`, `+67`, `-6`, `+1`, `-9`), and the canonical validator now treats a stored semantic mismatch as an error rather than a warning. Seat ranges, mapped/published counts, scopes, generated data, and production totals remain unchanged.

The bounded `TOKYO PRIORITY C REMAINING PREFLIGHT` is formally closed with **0 ADOPT / 5 DEFER / 0 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-c-remaining-preflight-2026-08-25.json`](../data/venue-reports/tokyo-priority-c-remaining-preflight-2026-08-25.json) records current first-party checks for `0693`, `0042`, `0367`, `0415`, and `0427`; each is now `blocked` and cannot return to ordinary discovery. The direct materials establish capacities or layouts but no candidate supplies a current issuer-numbered representative set with complete ownership/subtotals. No production data changed. Priority C true-unprocessed is now **3**: `0398`, `0407`, and `0518`; no implementation batch is recommended.

`TOKYO PRIORITY C CLOSURE` is complete with **0 ADOPT / 3 DEFER / 0 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-c-closure-2026-08-25.json`](../data/venue-reports/tokyo-priority-c-closure-2026-08-25.json) formalizes `0398` 森下文化センター第1レクホール (maximum chair count only), `0407` 総合区民センターレクホール (chair count and event space only), and `0518` 池上会館集会室 (official long closure through 2027-03-31). All three are `blocked`; **Priority C true-unprocessed is now 0 and the lane is CLOSED**. No production data changed.

The first bounded `TOKYO PRIORITY D HIGH-YIELD PREFLIGHT` is complete with **3 ADOPT / 2 DEFER / 0 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-d-high-yield-preflight-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-high-yield-preflight-2026-08-25.json) reduces the 835 outstanding D records to a 90-record URL/capacity screen and a ten-record dedicated-hall shortlist, then checks only ranks 1-5. `1007` RISURU小ホール (246), `0797` 杉並公会堂小ホール (190 numbered + wheelchair4), and `0930` ゆめりあホール (170 fixed + six issuer-labelled wheelchair positions) are **ADOPT** for separately authorized two-pass transcription only. `0492` パーシモン小ホール is DEFER because its current chart leaves intervening rows unnumbered; `0508` アプリコ小ホール is DEFER because it publishes user-arranged chair capacities, not issuer IDs. Neither is a Sol candidate; no checked venue has a complete numbered map whose only remaining blocker is reconciliation. No production data changed.

The separately authorized, one-venue follow-through for `tokyo-official-0121` (品川プリンスホテル ステラボール) is **SAFE TO INTEGRATE / PRODUCTION**. [`data/venue-reports/tokyo-stellar-ball-representative-2026-08-25.json`](../data/venue-reports/tokyo-stellar-ball-representative-2026-08-25.json) records two independent direct visual transcriptions of the current official/current-linked chart. Both produce the same issuer-labelled set: 1F 750 + 2F 126 = 876, mapped 876 / published 876 / rangeDiff 0 / representative confidence. The 1F movable-chair disclosure and the absence of a separately numbered wheelchair/companion set remain metadata; no seat ID or ownership was invented. Source, inventory, batch/readiness/report, fingerprint, catalog and lazy runtime were synchronized in pushed production commit `a277a5a`; `npm run verify:venues`, its managed Workers Build, and the public UI smoke test all passed.

| Item | State |
| --- | --- |
| Wave24 promotions | 3 (`tokyo-wave3-1170`, `tokyo-wave3-0645`, `tokyo-wave3-0330`) |
| Wave24 HOLD | 1 (`tokyo-wave3-1114`) |
| Authoring production target | 133 venues / 135 selectable configurations / 181,619 configuration-seat records |
| Expected release coverage | Tokyo 48/76 · MUST 29/44 · SHOULD 19/28 · RELEASE READY yes |
| Validation / generated artifacts | Wave31 source, catalog/runtime, and fingerprint are synchronized; canonical `npm run verify:venues` passed; semantic rangeDiff validation remains an error on mismatch. Wave32 source, catalog/runtime, and fingerprint are also synchronized; its canonical `npm run verify:venues` passed (224 unit tests, 17 E2E tests, 0 errors) |
| Commit / push / Workers / public UI | Wave31 `a9f9e76` is pushed; its managed Workers Build completed successfully and one-venue public UI smoke passed. Wave32 `6c6778e` is pushed; its managed Workers Build (`708d8af8-f636-4098-bade-d3028e23fb64`) completed successfully and the public UI found ユーロライブ at 178 seats, completed one draw (`B列 12番`), and had no console warning or error |
| Legacy type-only A/B lane | Closed: 1 production / 8 DEFER / 0 hard-case DEFER; production totals unchanged at 119 / 121 / 178,193 |
| Tokyo A/B source-located lane | Closed: `0374` DEFER; production totals unchanged at 119 / 121 / 178,193 |
| Tokyo priority C source-located preflight | Closed: `0375` / `0950` / `1186` DEFER; 0 ADOPT |
| Tokyo priority C true-unprocessed | CLOSED: `0398` / `0407` / `0518` DEFER; true-unprocessed C = 0 |
| Tokyo priority D high-yield | Preflight: 835 outstanding → 90 URL/capacity screen → 10 dedicated-hall shortlist; `0492` / `0508` DEFER. Wave27: `1007` / `0797` / `0930` are all PRODUCTION, adding 612 mapped seats. |
| Tokyo priority D ranked follow-up | Wave28 promoted all 3 ADOPTs: `1036` 189, `0133` 176, `0282` 237. Production totals are 128 / 130 / 180,542. |
| Tokyo priority D next high-yield | Wave29 promotes `0647` セルリアンタワー能楽堂 201 and `0222` 紀伊國屋ホール 427 after MATCH two-pass review. `0238` / `0565` remain blocked DEFER; `0278` remains blocked HARD-CASE DEFER. |
| Tokyo priority D ranked next | Wave30 promotes `0859` シアターグリーン BIG TREE THEATER 167 after MATCH two-pass review. `0115` / `0476` / `0867` remain blocked DEFER; `0586` 本多劇場 remains blocked HARD-CASE DEFER. Production totals are 131 / 133 / 181,337. |
| Tokyo priority D ranked next preflight 2 | Preflight: 1 ADOPT `0860` シアターグリーン BOX in BOX THEATER 104. `0366` / `0266` / `0816` are blocked DEFER; `0641` シアター代官山 is blocked HARD-CASE DEFER (126 printed vs stated 123). |
| Tokyo priority D ranked next 2 implementation | Wave31 promotes `0860` シアターグリーン BOX in BOX THEATER 104 after MATCH two-pass review. Production totals are 132 / 134 / 181,441. |
| Tokyo priority D ranked next preflight 3 | Preflight: 1 ADOPT `0635` ユーロライブ 178 (current A-M 座席表 image). `0861` / `0893` / `0818` / `0813` are blocked DEFER. No production change; totals remain 132 / 134 / 181,441. |
| Tokyo priority D ranked next preflight 3 implementation attempt | `0635` ユーロライブ two-pass MATCHED (178/178, rangeDiff 0) but closes **SOURCE/POLICY HOLD**: no current HTTPS endpoint at eurolive.jp, and the schema validator requires HTTPS unconditionally. No source authored; production unchanged at 132 / 134 / 181,441. |
| Venue-source transport policy | Policy-only change: HTTP-only current official issuers are allowed under an explicit machine-checkable `httpOnlyOfficial` waiver; HTTPS stays mandatory where it exists. One shared implementation across source validator, inventory validator, and the production test. No venue data, fingerprint, or generated artifact changed; totals remain 132 / 134 / 181,441. |

## TOKYO-WAVE-24 decisions

- **武蔵村山市民会館 さくらホール〔大ホール〕** (`tokyo-official-1170` → existing canonical `tokyo-wave3-1170`) — **PRODUCTION**: 1,027 printed / 1,032 published / rangeDiff -5 / representative. Fixed 909, movable 123 and five unnumbered wheelchair spaces are source metadata; all printed movable numbers remain in scope.
- **渋谷区文化総合センター大和田〔さくらホール〕** (`tokyo-official-0645` → existing canonical `tokyo-wave3-0645`) — **PRODUCTION**: 1F 535 + 2F 172 + balcony 22 = 729 printed / 735 published / rangeDiff -6 / representative. Fixed 407 (including 20 back-reclining seats) and movable 128 remain in scope; parent seats 4 and wheelchair seats 2 are unnumbered metadata.
- **東京藝術大学奏楽堂** (`tokyo-official-0330` → `tokyo-wave3-0330`) — **PRODUCTION**: front 958 + BL/BR 144 = 1,102 printed / 1,100 published / rangeDiff +2 / representative. The eight wheelchair-compatible printed seats stay in the normal set.
- **ルネこだいら 大ホール** (`tokyo-official-1114` → `tokyo-wave3-1114`) — **SOURCE HOLD**: the current map's 2F set independently totals 466, but its visible 1F numbered positions cannot be reduced to issuer subtotal 754 without deciding the relation of wheelchair-labelled positions. No ID was invented or deleted. Next evidence: issuer-defined 1F seat list or arithmetic that resolves the 754-seat total.

## Stellar Ball production decision

- **品川プリンスホテル ステラボール** (`tokyo-official-0121` → `stellar-ball-standard`) — **PRODUCTION**: current official seat-style 1F 750 + 2F 126 = **876** printed and published seats. First and second pass agree on every issuer-labelled row/area, range, and gap. 1F K-M exclude only the printed 21-40 center interval; 2F B/C retain their printed gaps; VIP ROOM-A/B each retain 1-6. The facility's movable 1F seating and unnumbered accessibility operations are disclosures, not range transformations.

### Historical ID reconciliation

`tokyo-official-1170`/`tokyo-wave3-1170` and `tokyo-official-0645`/`tokyo-wave3-0645` are each the same venue. Both prior Wave3 records were incomplete drafts, not REJECT/HOLD dispositions; each was **reopened under current policy/evidence** and updated in place. Future candidate exclusion must match by canonical `venueSourceId` and facility-space identity, not only by inventory-ID prefix, so a previously investigated venue cannot re-enter through an `official-*` versus `wave3-*` ID difference.

## Tokyo Priority D research priority

This is a candidate-ranking rule only; it does not alter the evidence/adoption policy or exclude a venue from production. Rank unresearched Tokyo Priority-D metadata primarily by expected simulator use: (1) dedicated theater, concert hall, live-event hall, or auditorium with continuing stage, theatre, live, or concert use and likely assigned-ticket sales; (2) civic/public halls with explicit performance-use large, medium, or small halls; then (3) recreation, multipurpose, meeting, and conference spaces. No wave establishes a fixed future order. The next high-yield preflight used this rule only after its two mandated carry-overs; its two ADOPTs are セルリアンタワー能楽堂 and 紀伊國屋ホール, while 音楽の友ホール and シアタートラム are blocked DEFER and 東京グローブ座 is blocked HARD-CASE DEFER. The ranked-next preflight then applied the same rule to metadata alone, adding a tie-break within tier 1 that prefers `source-located` plus `likely-eligible` plus a direct operator URL plus a recorded capacity over `not-started` plus `needs-research` plus a null capacity; its single ADOPT is シアターグリーン BIG TREE THEATER. Live-house brand records, sub-100-seat studio theatres, and 国立能楽堂〔研修能舞台〕 as a training stage were ranked below the selected five and left unopened. The ranked-next preflight 2 reapplied the same tier-1 tie-break and added recorded capacity (descending) as a secondary tie-break within the source-located/likely-eligible group; its single ADOPT is シアターグリーン BOX in BOX THEATER. The ranked-next preflight 3 applied the same rule to the remaining pool, including previously-unopened siblings シアターグリーン BASE THEATER and シアター・バビロンの流れのほとりにて in the top five; its single ADOPT is ユーロライブ, while BASE THEATER, シアター・バビロンの流れのほとりにて, 西巣鴨音楽堂, and 杉並能楽堂 are blocked DEFER (no current issuer-numbered seat set). It also identified `tokyo-official-0460` シアターH as a duplicate facility-space of already-produced `theater-h-standard` and excluded it from the pool before ranking, without editing its inventory record. The ranked-next preflight 4 then checked the next five metadata-ranked tier-1 candidates (`0704` 代々木能舞台, `0087` 銀座タクト, `1155` グスタフスタジオ, `0691`/`0692` LUMINE 0 スタジオ1/2); all five are blocked DEFER for the reasons in that report. The remaining tier-1 needs-research/not-started records (`0688` 国立能楽堂〔研修能舞台〕, `1147` くにたち市民芸術小ホール〔スタジオ〕, `0210` シアターウィング, `0800` Live Spot Terra 西荻窪, and further null-capacity records: `0342`, `0535`, `0589`, `0590`, `0718`, `0742`, `0764`, `0765`, `0769`, `0881`, `1002`) remained ranked below and unopened as of that wave. The `TOKYO PRIORITY D CROSS-TIER HIGH-YIELD PREFLIGHT` then revised this rule's application: instead of exhausting the dedicated-theater/auditorium keyword screen before ever considering a civic 大ホール/中ホール/小ホール-designated space, it cross-ranked both groups together at the same base priority and let the existing source-located/likely-eligible/URL/capacity tie-break (with a yield penalty for studio/training/live-spot/multipurpose/sub-100-seat wording) decide order across both. This surfaced academic/museum 講堂 (`0249`, `0337`, `0464`, `0338`) and a civic 小ホール (`1102`) ahead of the still-unopened `0688`/`1147`/`0210`/`0800`; its single ADOPT was 早稲田奉仕園〔スコットホール・講堂〕, since reversed to HARD-CASE DEFER by the `TOKYO-OFFICIAL-0249 SCOTT HALL BOUNDED JUDGMENT AUDIT`. This cross-tier tie-break, not the old single-screen exhaustion order, is now the current ranking approach for any further Priority-D preflight.

## Tokyo Priority D portfolio / lane audit (2026-08-27)

Research ordering only. This changes no evidence, adoption, production-gate, or transport contract, reclassifies no inventory `priority`, and reopens no blocked record.

**Finding.** Tokyo Priority-D high-yield research is **not** exhausted. `priority` in `data/venue-inventory/tokyo.json` is a capacity band, and every capacity-unknown record fell to D by default: null capacity by priority is A 3 / B 0 / C 0 / **D 717** of 969. Priority D is therefore the capacity-unknown bucket, not a small-venue tail. Of the 787 outstanding Priority-D records (confirmed live by `npm run venues:inventory:report`), metadata segmentation gives **109 high-potential / 100 medium-potential / 578 low-potential**, and **102 of the 109 high-potential records have a null capacity**, so no wave has ever scored them. A repository-wide grep of `docs/`, `data/venue-reports/`, and `data/venue-batches/` returns **zero** matches for パルコ劇場/PARCO, 三越劇場, 第一生命ホール, 博品館, サザンシアター, 宝生, 鈴本, 俳優座, 日本橋劇場, せんがわ and 兼松, and none of them is production under any other identity.

**Ranking-rule defects.** (1) Null capacity is penalised instead of treated as absent evidence — 84 of the 90 records that pass the current tier-1/tier-2 name screen are null-capacity, and 44 of the 45 candidates ever opened had a recorded capacity. (2) The vocabulary screen misses proper-noun halls — 273 outstanding records carry a hall name without matching 劇場/シアター/大・中・小ホール, including 第一生命ホール, 品川インターシティホール, ヤクルトホール, 草月ホール, 後楽園ホール. (3) `source-located`/`likely-eligible` outranks venue identity, which in PREFLIGHT 3 put a 75-seat 講堂 above a 130-seat civic 大ホール and both above every dedicated theatre. (4) A missing `officialFacilityUrl` is read as a yield signal, though 533 of 787 records simply have no URL in the discovery row.

**Yield history** (preflight-stage ADOPT separated from eventual production, because the `tokyo-official-0249` スコットホール ADOPT was reversed to HARD-CASE DEFER): 48 candidates checked across ten preflights, 13 initial ADOPT, **12 eventual production**, 2,499 mapped seats. Waves 1–3 ran 8/13 = **61.5%**; waves 4–6 ran 3/15 = **20.0%**; waves 7–10 ran 2/20 initial = 10.0% and **1/20 = 5.0%** to production; the last two waves produced **0/10**. Over the same run the opened candidates' recorded capacity fell from a 176–237 band to 20–159. The decline tracks the ranking rule walking down the capacity-recorded civic-room list, not the exhaustion of Tokyo's performance venues.

**Corrected research-ordering rule** for the next Tokyo Priority-D wave, applied to live metadata:

1. Fixed-numbered-seat likelihood and performance-hall identity — **1a** dedicated ticketed identity (劇場/シアター/theater/theatre/演芸場/能楽堂/音楽堂/コンサートホール/オーディトリアム **and proper-noun single-purpose hall names**); **1b** municipal/public hall with an explicit 大/中/小/主/メインホール designation; **2** 講堂, academic auditorium, 能舞台, 小劇場; **3** 地区センター〔ホール〕, 公民館ホール, 学習館〔講堂〕, レクリエーションホール, 区民ホール; **4** excluded from preflight — studio, rehearsal/training, live house, live spot, multipurpose, meeting, free-seating, movable-chair rental.
2. Assigned-ticket likelihood: producing or commercially programmed operator over rental-only room.
3. Issuer precedent: prefer an issuer already in production with a validated chart convention; demote a record whose sibling space at the same issuer already returned DEFER (`1127` after `1126`, `0444` after `0443`).
4. Recorded capacity descending, with **null neutral, never penalised**; apply the sub-100 penalty only to a *recorded* capacity under 100.
5. Direct issuer URL, then `source-located`/`likely-eligible`, as final tie-breaks only.

**Reference shortlist** (metadata only, recompute before use): `tokyo-official-0690` 紀伊國屋サザンシアターTAKASHIMAYA, `0740` PARCO劇場, `0105` 三越劇場, `1068` 調布市せんがわ劇場, `0103` 日本橋公会堂(日本橋劇場), `0098` 第一生命ホール, `0094` 博品館劇場, `0316` 宝生能楽堂, `1157`/`1158` 東大和ハミングホール 大/小ホール, `0890`/`0891` 北区赤羽会館 大/小ホール. The previous reference set `0688`/`1127`/`0210`/`0444`/`1147`/`0800` is **explicitly deprioritised**: all six rank highly only because a capacity number is present, and four have an adverse sibling or identity signal already recorded here.

**Paths not chosen.** Hard-case judgment: of 34 Tokyo hard-case/HOLD records only **2** are actionable without a new issuer publication — `tokyo-official-0278` 東京グローブ座 (707) and `tokyo-official-0058` セルバンテス東京 (159; an approximate-figure policy call). *(Superseded for `0278`: this audit predicted the relation would turn on whether an independent transcription sums to 463/158/86. It does not — the printed set is 455/158/86 — and the relation was instead settled by an issuer annotation printed on the 1F chart itself. See the `0278` section below.)* `0249`, `0586`, `0641`, `0007`, `0286` and `0017` cannot be resolved by re-reading the same material. Regional expansion: 93% of production is Tokyo (東京 125, 神奈川 3, 大阪 2, 千葉 1, 京都 1, 埼玉 1, 栃木 1), but **all 41** non-Tokyo inventory records outside production are already blocked, mostly on DENSE/JUDGMENT contradictions in 2,000-seat halls, and no non-Tokyo discovery universe exists. The recorded follow-on macro is 神奈川 → 千葉 → 埼玉, entered through the nine records blocked only on an unreachable registered URL, and it requires a discovery-source normalization macro first.

## tokyo-official-0278 東京グローブ座 judgment audit (2026-08-27)

Bounded JUDGMENT audit of one blocked hard case. Disposition **HARD-CASE DEFER → ADOPT**. Audit-only: no venue source, seat range, generated artifact, fingerprint, or production total changed; production stayed **134 / 136 / 181,827**. No other candidate, hard case, or blocked record was opened, `tokyo-official-0058` included. The 100,091-byte catalog soft-limit warning was out of scope and untouched.

**Question.** Whether the published 707 = 1F 463 / 2F 158 / 3F 86 and the unnumbered wheelchair-space icons on the current official charts stand in an issuer-defined relation: outside the subtotal, inside it and replacing numbered seats, or still undecidable.

**Answer: inside, replacing eight numbered seats.** The blocker was an incomplete reading of the chart in the earlier preflight, not a gap in the issuer's evidence. The current official 1F chart prints, beside each wheelchair area, the label 車いす席 with an arrow and the text **「一般席とした場合 14〜17番」** (left gallery, beside 2扉) and **「一般席とした場合 50〜53番」** (right gallery, beside 5扉). Both areas sit on gallery row **O**; each is four dashed slots holding two pictograms. The preflight's statement that "the issuer does not state whether those icons are included in, replace, or are additional to the 707-seat subtotal" is retracted.

**Fresh independent two-pass transcription (MATCH).** Pass 1 read 1F→2F→3F in the ordinary direction; the three chart assets were then re-fetched (byte-identical, sha256 verified) and Pass 2 re-read them 3F→2F→1F, blocks from the opposite side, every row and column from its high-numbered end backwards, using different crop windows throughout. The passes agree on the complete printed ID set, row and block ownership, every range and gap, both wheelchair-icon locations, and all three floor subtotals. Printed numbered identifiers only:

| Floor | Printed IDs | Issuer published | Difference |
| --- | --- | --- | --- |
| 1F 平土間 (rows A–L) | **307** | 307 | 0 |
| 1F ギャラリー (rows M/N/O) | **148** | 156 | 8 |
| 1F total | **455** | 463 | 8 |
| 2F (rows A/B/C) | **158** | 158 | 0 |
| 3F (rows A/B) | **86** | 86 | 0 |
| **Grand** | **699** | 707 | **8** |

The printed set does **not** reproduce 463/158/86 on its own, so the "printed IDs already equal 707" branch does not apply and was not argued. The 8-seat difference was not filled in by the repository; it is issuer-explained three independent ways: the issuer's own finer 1F split 平土間307／ギャラリー156 reconciles the 平土間 exactly at 307 with no wheelchair position involved; the gallery gives 148 printed + 8 drawn wheelchair positions = 156 exactly; and gallery row O's own numbering runs continuously 1–66 omitting from print exactly the eight identifiers 14–17 and 50–53 that the annotation names, while rows M and N contain no wheelchair position and row N runs 1–60 with no gap at all. Therefore 463 = 307 + 148 + 8 and 707 = 699 + 8.

**Exact independent ranges** (recorded for checking a future implementation, not for copying — a fresh two-pass must re-derive them). 1F 平土間 A 2–33, B 1–33, C 1–34, D 2–33, E 3–31, F 3–32, G 4–30, H 6–29, I 8–26, J 10–25, K 10–24, L 10–25. 1F ギャラリー M 6–20 and 41–55 (identifiers 21–40 are not issued on row M, which has no rear-centre section), N 1–60, O 1–66 `excluded: [14,15,16,17,50,51,52,53]`. 2F A 1–56, B 2–57, C 7–52. 3F A 1–42, B 1–44.

**Wheelchair semantics.** The eight positions are 1F gallery row O identifiers 14–17 and 50–53, included in the published 707 but not printed as seats in the standard configuration. The representative mapped set is therefore the **699 printed identifiers**; `expectedSeatCount` stays **707** and the resulting 8-seat difference is documented quality metadata that must be left as a validator warning. Creating seat IDs for the eight from the annotation is prohibited — that would adopt the issuer's alternative 一般席とした場合 arrangement instead of the standard chart, which is a configuration mix.

**Precedent.** Verified against the repository, not taken from the prior audit's claim: of the 134 production sources, 125 mention 車いす/車椅子 and **75** explicitly record wheelchair spaces as outside the mapped numbered set. `musashino-civic-main-standard` is the closest analogue (expectedSeatCount 1,256, mapped 1,252, 番号を印字しないためseat IDを作成しない); `akigawa-kirara-hall-standard` (702 含車椅子席2席 → 700 mapped) and `chofu-green-main-standard` (1,301 車いす用6席含 → 1,295 mapped) are the same pattern, and `kinokuniya-hall-standard` exercises the converse rule by keeping printed convertible IDs rather than deleting them. `venues:validate` already carries 24 "official total differs from mapped seat count" warnings, which `docs/VENUE_DATA_GUIDE.md` classifies as confidence information, not a hard gate. The precedent did not decide the venue — the venue's own three-way arithmetic did — and then fixed how the settled relation is represented.

**Configuration scope.** The issuer publishes exactly one set of standard 1F/2F/3F numbered charts and no alternative numbered chart. The page footnote ※公演によって、座席の配置やステージの形状が異なる場合がございます is a per-production variation notice of the kind already accepted at `tokyo-official-0647` and `tokyo-official-0222`; it defines no second configuration.

**Implementation is not authorized by this audit.** If one is separately authorized it must begin with a currentness re-check of the facility overview and the three chart assets, and must perform a **fresh independent two-pass** — the two passes here were run by one agent in one context and are recorded as a reverse-direction re-derivation, not a cold second reading. Lane for that work is **DENSE** (three multi-block floors, a wrap-around gallery numbering scheme, roughly 60 ranges). Floors must be separate areas: all three reuse row letters from A, and 1F reuses M/N/O for the gallery, so NFKC row-duplicate detection would otherwise collide. Block names are not issuer-published and any repository-side naming must follow the display-name rule in `docs/VENUE_DATA_GUIDE.md`.

## TOKYO-WAVE-34 東京グローブ座 production implementation (2026-08-27)

`TOKYO-WAVE-34 GLOBE TOKYO ADOPT IMPLEMENTATION` is complete with **1 PRODUCTION / 0 HOLD**, in pushed production commit `5a53b6c`. Batch: [`data/venue-batches/tokyo-wave-34-globe-tokyo-adopt-implementation.json`](../data/venue-batches/tokyo-wave-34-globe-tokyo-adopt-implementation.json). Lane **DENSE**. Bounded to `tokyo-official-0278` only: no other candidate research, no DEFER reopening, no `tokyo-official-0058` judgment, and no Kanto >=1000 audit was started.

**Currentness re-check (first).** The current official 施設概要 still publishes 客席 **707席** / 【1F】**463席**（平土間**307席**／ギャラリー**156席**）/ 【2F】**158席** / 【3F】**86席**. The 座席表 page still carries the 1階席/2階席/3階席 charts and the same two footnotes. The three current-linked chart assets were re-fetched and re-hashed **unchanged** from the audit: 1F `.../66086573958a2a2c38cae633c87a8-01.png` sha256 `cbb7bab6d907e3127a7e6a290a99a53574320b17bfa55e535c8cf547d11d90d8`, 2F `...-02.jpg` sha256 `3e35951a693e7575cb925072d66a4acff415e042ddcb0c94a20fb75466aba989`, 3F `...-03.jpg` sha256 `e2a147f7a45ce5b14d4a63790e17b2b9e152641e6281fd142bb2500f483322d4`. The 車いす申請 page still requires an already-held S 席 ticket and its 階／列／番. No asset changed, so no past-range assumption had to be revisited.

**Fresh independent two-pass (MATCH).** The audit's recorded range table was **not** used as an answer key. Pass 1 read 1F → 2F → 3F in the ordinary direction from magnified crops. The three charts were then re-fetched and confirmed byte-identical, and Pass 2 re-read 3F → 2F → 1F, taking every block from its high-numbered end with new crop windows and magnifications. The two passes were compared only after both were complete, and they agree on the complete printed ID set, floor ownership, row ownership, every range and gap, both wheelchair areas, all floor printed subtotals and the grand printed subtotal.

| Floor / area | Printed IDs | Issuer published | Difference |
| --- | --- | --- | --- |
| 1F 平土間 (rows A–L) | **307** | 307 | 0 |
| 1F ギャラリー (rows M/N/O) | **148** | 156 | 8 |
| 1F total | **455** | 463 | 8 |
| 2F (rows A/B/C) | **158** | 158 | 0 |
| 3F (rows A/B) | **86** | 86 | 0 |
| **Grand** | **699** | 707 | **8** |

Arithmetic was derived from the fresh transcription, not fitted to the audit: **mapped 699 / expected 707 / rangeDiff −8**. The freshly derived figures happen to equal the audit's, which is recorded as a consistency check only; no data was adjusted toward it.

**Wheelchair semantics, re-confirmed on the fresh chart.** The current 1F chart still prints, beside each of the two gallery row **O** wheelchair areas, 「車いす席」 with an arrow and 「一般席とした場合14〜17番」 (left gallery, by 2扉) and 「一般席とした場合50〜53番」 (right gallery, by 5扉). Each area is exactly four dashed seat slots carrying two pictograms; both passes counted four slots in each. Those eight positions lie inside the published 707 but are **not printed seat IDs** on the standard chart, so the mapped set is the **699 printed identifiers only**: no seat ID was created for the eight, `expectedSeatCount` stays **707**, and the resulting −8 is kept as the intended validator warning. The 一般席とした場合 alternative arrangement is recorded as metadata and is **not** mixed into the representative configuration.

**Production source.** Schema v2 `globe-tokyo-standard`, configuration `current-official-printed-seat-map`, `status: production`, `selectable: true`, `scope.kind: official-variant`, `confidence: representative`, `wheelchairSemantics.status: resolved` with `accessibilityConversionNotReflected: true`, verification `verified` / `seatStructure: matched` / `seatCount: mismatched` / `rangeDiff: -8`. Six official HTTPS sources (施設概要, 座席表 page, the three floor charts, 車いす申請); no `httpOnlyOfficial` waiver was needed. Because all three floors reuse row letters from A, the floors are **separate areas**: `first-floor-hiradoma` 1階平土間, `first-floor-gallery` 1階ギャラリー, `second-floor` 2階席, `third-floor` 3階席. Both 1F area names are the issuer's own 平土間／ギャラリー, so no repository-invented block name was introduced. Gallery row M is stored as two ranges 6–20 and 41–55 because it has no rear-centre section and the issuer never issues 21–40; row O is `from 1 to 66` with `excluded: [14,15,16,17,50,51,52,53]`. No printed identifier was deleted, no gap closed, no row renumbered.

The inventory record `tokyo-official-0278` moved from `researchStatus: source-located` / `eligibility: likely-eligible` / ADOPT `blockingReason` to `researchStatus: production` / `eligibility: eligible` / `venueSourceId: globe-tokyo-standard`, with the `blockingReason` removed.

**Validation.** Canonical `npm run verify:venues` passed: inventory/readiness/release-coverage reports, `venues:build`, `venues:check` (135 venues synchronized), `venues:validate`, `venues:report`, lint, typecheck, **224 unit tests**, production build, docs verification, `git diff --check`, and **17 E2E tests**. Production is **135 venues / 137 selectable configurations / 182,526 configuration-seat records**. Release coverage gate is still PASS / RELEASE READY yes (the Tokyo coverage universe contract is a fixed 2026-08-11 list that does not include this venue, so its 48/76 figures are unchanged by design). The generated catalog diff is a pure addition — 25 added lines, **0 deletions** — so no prior production fingerprint, catalog entry, or runtime payload changed.

**Warnings.** Warning count went 41 → 42. Exactly **one new warning**: `globe-tokyo-standard.json: configuration 0 official total 707 differs from mapped seat count 699`, which is the intended, issuer-explained wheelchair difference and is confidence metadata, not a gate. All other warnings are pre-existing (the three eurolive `httpOnlyOfficial` transport warnings plus its inventory warning, the 24 other official-vs-mapped warnings, the Kyocera Dome fragmentation warning). The known **catalog soft-limit warning** moved from **100,091 to 101,782 bytes** (+1,691) purely from this one venue's addition; per the wave's explicit scope the threshold was **not** changed and **no** compaction or refactor was done to silence it.

**Release.** Production commit `5a53b6c` is pushed to `origin/main`. The GitHub check run **Workers Builds: seat-lottery-simulator** for `5a53b6c` completed **successfully** (build `740c77fb-52f9-49b5-a291-e28c7b75ba91`). Public UI smoke at `https://seat-lottery-simulator.studiotomo.workers.dev/`: 東京グローブ座 is found by full name and by the alias グローブ座, displays **抽選対象 699席** (matching the mapped count exactly), and lazy-loads its runtime payload with HTTP 200. Eight draws completed across all four areas — `2階席 B列15番`, `1階平土間 G列22番`, `1階ギャラリー O列32番`, `1階ギャラリー N列35番`, `2階席 B列43番`, `3階席 A列33番`, `3階席 B列21番`, `3階席 A列20番` — with the floor-qualified area label shown on every result, so the row letters A and B reused across 1F/2F/3F never collide in the UI. The deployed runtime payload was additionally enumerated in full against the shipped sampler logic: 699 total, per-area 307/148/158/86, row O yields 58 seats over 1–66 and **never** 14–17 or 50–53, row M yields exactly 6–20 and 41–55 and **never** 21–40, and there are **0** duplicate area/row/number keys. The browser console had **no warning and no error**.

## kanagawa-pia-arena-mm ぴあアリーナMM production implementation (2026-08-27)

`KANAGAWA-PIA-ARENA-MM STAND-FLOOR ADOPT IMPLEMENTATION` is complete with **1 PRODUCTION / 0 HOLD**. Lane **JUDGMENT**: the configuration question — which of the four published floors is a fixed numbered-seat set, and what the 公称 12,141 actually measures — had to be resolved from issuer evidence before any transcription. Bounded to `kanagawa-pia-arena-mm` only: no other candidate research, no work on `ibaraki-mito-civic-main` or `saitama-city-culture-main`, no Kanto unresearched-candidate research, and no known HARD-CASE re-audit was started.

**Currentness and current first-party evidence.** All decision evidence is current first-party material published by the operator ぴあ株式会社 on `pia-arena-mm.jp` and checked 2026-08-27: the 座席表 page and its 1F–4F charts (`https://pia-arena-mm.jp/seatmap/`, all HTTPS), the 施設概要 PDF (`/assets/images/facility/facility_summary.pdf`), the 建築概要 page stating 収容人数 最大12,141人 (`/about/`), and the フロアガイド (`/floor/`). No `httpOnlyOfficial` waiver was needed and no secondary source was used for seat evidence.

**Configuration assessment — 1F is not a numbered-seat floor.** The フロアガイド labels **1F ARENA FLOOR アリーナフロア** and **2F / 3F / 4F STAND FLOOR スタンドフロア**. The 1F chart draws only the arena slab plus the eight 扉 block names L01–L03 / C04 / C05 / R06–R08, with **no rows and no seat numbers**, and the 座席表 page publishes per-block numbered detail figures for 2F (7), 3F (7) and 4F (6) but **none for 1F**. The 施設概要 counts the arena separately as 着席時 **3,980** / 立見時 **6,064**. The 1F arena floor is therefore issuer-defined event-dependent seating with no published numbered set, and it is excluded — this is a defined exclusion, not missing evidence for a fixed floor.

**Fresh independent two-pass (MATCH).** No range was copied from the preflight, and 12,141 was never used as a target. Pass 1 read the current charts 1F → 2F → 3F → 4F in the ordinary direction. All 20 numbered detail assets were then re-fetched with cache-busting and confirmed **byte-identical** (SHA-256 unchanged), and Pass 2 re-read 4F → 3F → 2F, taking each bar from its high-numbered end with new crop windows, rotations and magnifications, without consulting the Pass 1 table. The two passes were compared only after both were complete, by machine-diffing the fully expanded seat-ID sets. They agree exactly on the complete printed ID set, floor ownership, block/bar ownership, row ownership, every range, every gap, every wheelchair marking, every per-floor subtotal, and the grand subtotal.

| Floor | Rows | Printed IDs | Issuer published 客席数 | Issuer-stated non-numbered content | Difference |
| --- | --- | --- | --- | --- | --- |
| 2F STAND | 1–9 | **2,700** | 3,150 | 立見最大440名 + 車イス10席 | 440 + 10 |
| 3F STAND | 1–6 | **1,670** | 1,680 | 車イス10席 | 10 |
| 4F STAND | 1–5 | **1,203** | 1,247 | VIP用44席 | 44 |
| **Stand total** | | **5,573** | **6,077** | | **504** |
| 1F ARENA FLOOR | — | **0** (no printed IDs) | 着席時3,980 / 立見時6,064 | event-dependent arena floor | excluded |

**Critical reconciliation of the 公称 12,141.** The 施設概要 states 合計 着席時 **10,057** and 立見時 **12,141**. Both are reproduced exactly from the issuer's own published rows: 3,980 + 6,077 = 10,057 and 6,064 + 6,077 = 12,141. The 12,141 is therefore the **maximum standing capacity including the arena floor**, not a fixed-seat total, and it has no direct relation to a numbered-seat set. Each floor's difference is likewise fully explained by the issuer without inference: 3,150 − 440 − 10 = **2,700**, 1,680 − 10 = **1,670**, 1,247 − 44 = **1,203**, all three matching the freshly transcribed printed totals exactly. No seat ID was added, deleted, extended, or geometry-completed, no missing number was filled in, and no event-specific arena-floor seat entered the set.

**Accessibility, removable and event-dependent semantics.** The current charts draw four wheelchair blocks — 2F at L09–L10 and R29–R30 covering the 8・9列 positions, 3F at L09–L10 and R29–R30 covering the 4・5列 positions — each marked only with the wheelchair pictogram and carrying **no row or seat number**. The 施設概要 states 2階 車イス10席 and 3階 車イス10席 but publishes no per-position numbering and no conversion relation to ordinary seats, so `wheelchairSemantics.status` is **`not-reflected`** with `accessibilityConversionNotReflected: true` and no seat ID was created for those positions. The 2F **Standing 立見** bands behind row 9 (issuer: 最大440名) are unnumbered and excluded. The 4F **VIP用44席** are inside the published 1,247 but are not printed on the public chart — the corresponding 4F block is the Suite A–F / Suiteラウンジ area the 施設概要 lists among the 4F 諸室 — so they are excluded and recorded as such. The 3・4Ｆ最前列 立見厳禁 notice is a conduct rule, not a seating difference.

**Structural gaps retained, not smoothed.** The rear row of each stand floor is cut by the 扉 openings, so 3F row 6 and 4F row 5 carry many issuer-printed gaps (for example 3F row 6 runs 2–12, 20–30, 38–48, …), and the row ends step in and out block by block on every floor (for example 2F row 9 in the C section ends at 146, resumes at 148, and skips 184). All of these are stored as separate ranges at exactly the printed extents; none was closed, extended, or interpolated. The 121 stored ranges expand to 5,573 seats with a maximum of 18 ranges in any one row, below the 25-range fragmentation warning threshold.

**One recorded issuer inconsistency.** The 3F detail figure `f3_4` carries a navigation label reading `seat number 195-320`, while every other 3F label on the same current material (`195-318`, `236-318`, `283-318`) and every printed 3F bar (rows 1–5 `301…318`, row 6 `307…317`) give 318 as the maximum. This is treated as an issuer label inconsistency; 319 and 320 are **not** registered because no printed bar contains them. It is recorded in `verification.unresolvedIssues`.

**Production source.** Schema v2 `pia-arena-mm` (`data/venue-sources/pia-arena-mm.json`), configuration **`stand-floors-fixed`** 「2F〜4F固定スタンド番号席・代表配置」, `status: production`, `selectable: true`, `scope.kind: fixed-only` with `excludesDynamicAreas: true`, `maximumCapacity: false`, five explicit `excludedAreas`, and `exactSubtotal: 5573`; `definitionAuthority: representative-evidence`, `confidence: representative`, `capacityFitting: false`, `repositoryInventedDifferences: false`; verification `reviewed` / `seatStructure: matched` / `seatCount: mismatched` / **`rangeDiff: -504`** against `expectedSeatCount: 6077`. Four official HTTPS sources. Because rows are numbered 1..n and seat numbers restart on every floor, the floors are **separate areas** to prevent runtime key collision: `2f-stand` 2階スタンド, `3f-stand` 3階スタンド, `4f-stand` 4階スタンド. These are the issuer's own floor identities (2F/3F/4F STAND FLOOR); the 扉 block names L01–L15 / C16–C23 / R24–R38 were deliberately **not** adopted as area ownership because the printed number bars do not align with the 扉 boundaries everywhere, and inventing block ownership from the drawing is prohibited.

The inventory record `kanagawa-pia-arena-mm` in [`data/venue-inventory/kanto-release-critical-2026-08-27.json`](../data/venue-inventory/kanto-release-critical-2026-08-27.json) moved from `researchStatus: source-located` / `eligibility: likely-eligible` to `researchStatus: production` / `eligibility: eligible` / `venueSourceId: pia-arena-mm`. The Kanto >=1000 ledger record moved to `currentStatus: production` with the reconciliation recorded, its `byStatus` counts moved to 53 production / 2 ADOPT, and its `nextMacro` now names only `ibaraki-mito-civic-main` and `saitama-city-culture-main`.

**Validation.** Canonical `npm run verify:venues` passed end to end: inventory/readiness/release-coverage reports, `venues:build`, `venues:check` (136 venues synchronized), `venues:validate`, `venues:report`, lint, typecheck, **224 unit tests**, production build, docs verification, `git diff --check`, and **17 E2E tests**. Production is **136 venues / 138 selectable configurations / 188,099 configuration-seat records**. Release coverage gate is still PASS / RELEASE READY yes. The generated catalog diff is a pure addition — 25 added lines, **0 deletions** — so no prior production fingerprint, catalog entry, or runtime payload changed. The range set was additionally enumerated exhaustively against the shipped sampler: all 5,573 offsets resolve to **5,573 unique** area/row/number keys with **0** collisions, and the drawable set equals the transcribed printed set exactly, so no gap or excluded position can ever be drawn.

**Warnings.** Warning count went 42 → 43. Exactly **one new warning**: `pia-arena-mm.json: configuration 0 official total 6077 differs from mapped seat count 5573`, which is the intended, fully issuer-explained 440 + 10 + 10 + 44 difference and is confidence metadata, not a gate. All other warnings are pre-existing. The known **catalog soft-limit warning** moved from **101,782 to 102,874 bytes** (+1,092) purely from this one venue's addition; per the wave's scope the threshold was **not** changed and **no** compaction was done to silence it.

## Release coverage priority (from 2026-08-27)

This is a coverage-ordering rule for future venue work. It does **not** relax any evidence, adoption, production-gate, or transport requirement, and it reclassifies no existing disposition.

Future venue expansion is ordered by expected release usage:

1. **関東** venues used for ライブ／演劇／コンサート／大型イベント — 東京都, 神奈川県, 千葉県, 埼玉県, 茨城県, 栃木県, 群馬県.
2. Within that, a **公称キャパシティ 1,000 以上** Kanto venue is **release-critical MUST-COVERAGE**.
3. Other major 東京 performance venues.
4. Major venues in large cities such as 大阪 and 福岡.
5. Nationwide coverage after that.

**What MUST-COVERAGE means.** It does **not** mean every such venue must be forced into production. It means that by release, each Kanto venue with a stated capacity of 1,000 or more must be accounted for under an explicit formal status — **PRODUCTION**, **ADOPT / implementation-ready**, **HARD-CASE / JUDGMENT**, **HOLD**, **NEED EVIDENCE**, or awaiting human judgment — and must be visible in the inventory and reports. "Absent from the inventory and therefore unrecognised" is **not** an acceptable state. Evidence policy for large venues is **not** relaxed to reach coverage: a venue with no current issuer-numbered seat set stays formally blocked rather than being approximated.

## Unchanged deferrals

- `tokyo-official-0048` 国立劇場 大劇場 and `tokyo-official-0049` 小劇場 are **CLOSED / CURRENTNESS HOLD** from the coverage universe: both have been closed since 2023 and await replacement-facility material.
- `tokyo-official-1060` FOSTERホール is **RENOVATION HOLD** through 2026-12-01.
- `tokyo-official-1020` 国立音楽大学 講堂小ホール is **SOURCE HOLD** because no current complete numbered map is registered.
- `tokyo-official-1071` 京王アリーナTOKYO メインアリーナ is the same facility-space as canonical `musashino-forest-sport-plaza-standard`; retain its **SOURCE/POLICY/SCHEMA HOLD**, rather than rediscovering it under the official-list ID.
- `tokyo-official-1090` remains without a formal disposition in repository evidence. It is outside the current scope; do not infer or research a disposition from this handoff.
- `tokyo-official-0373`, `tokyo-official-0425`, `tokyo-official-0709`, and `tokyo-official-1058` are formally deferred, while `tokyo-official-0611` and `tokyo-official-1017` are hard-case deferred with the current audit result **NEED EVIDENCE**. Reopen only under the evidence conditions in the formal report and inventory blockers.
- `tokyo-official-0115` 自由劇場, `tokyo-official-0476` 中目黒キンケロ・シアター, and `tokyo-official-0867` 萬劇場 are formally deferred, and `tokyo-official-0586` 本多劇場 is hard-case deferred, by the ranked-next preflight. Reopen only under the evidence conditions in that report and their inventory blockers. `tokyo-official-0115` is a distinct facility-space from the Shiki `shiki-haru-standard`, `shiki-aki-standard`, `shiki-umi-standard`, and `ariake-shiki-standard` records; do not merge them.
- `tokyo-official-0366` すみだパークシアター倉, `tokyo-official-0266` シアター風姿花伝, and `tokyo-official-0816` 雑司が谷音楽堂 are formally deferred, and `tokyo-official-0641` シアター代官山 is hard-case deferred, by the ranked-next preflight 2. Reopen only under the evidence conditions in that report and their inventory blockers. `tokyo-official-0860` シアターグリーン〔BOX in BOX THEATER〕 is now **PRODUCTION** (`theater-green-box-in-box-standard`) via TOKYO-WAVE-31; it is a distinct facility-space from produced sibling `theater-green-big-tree-standard` (`tokyo-official-0859`).
- `tokyo-official-0861` シアターグリーン〔BASE THEATER〕, `tokyo-official-0893` シアター・バビロンの流れのほとりにて, `tokyo-official-0818` 西巣鴨音楽堂, and `tokyo-official-0813` 杉並能楽堂 are formally deferred by the ranked-next preflight 3. `tokyo-official-0861` remains a distinct facility-space from produced siblings `theater-green-big-tree-standard` and `theater-green-box-in-box-standard`; do not merge them or infer its disposition from theirs. `tokyo-official-0460` シアターH was identified as a duplicate facility-space of already-produced `theater-h-standard` (`tokyo-coverage-theater-h`) and was excluded from candidate ranking; its inventory record was not edited and it carries no new disposition.
- `tokyo-official-0635` ユーロライブ is **PRODUCTION** (`eurolive-standard`, TOKYO-WAVE-32); it no longer holds a DEFER/HOLD disposition and must not be re-researched or reopened as a candidate.
- `tokyo-official-0704` 代々木能舞台, `tokyo-official-0087` 銀座タクト, `tokyo-official-1155` グスタフスタジオ〔シアターグスタフ〕, `tokyo-official-0691` LUMINE 0〔スタジオ1〕, and `tokyo-official-0692` LUMINE 0〔スタジオ2〕 are formally deferred by the ranked-next preflight 4. Reopen only under the evidence conditions in that report and their inventory blockers (a genuinely numbered individual-seat chart, not a table-numbered layout or a variable/maximum headcount statement).
- `tokyo-official-0337` 独立行政法人国立科学博物館〔日本館講堂〕, `tokyo-official-1102` 小金井 宮地楽器ホール〔小ホール〕, `tokyo-official-0464` 日本近代文学館〔講堂〕, and `tokyo-official-0338` 東京都美術館〔講堂〕 are formally deferred by the `TOKYO PRIORITY D CROSS-TIER HIGH-YIELD PREFLIGHT`. Reopen only under the evidence conditions in that report and their inventory blockers (a current issuer-numbered seat chart; an exact, non-approximate seat count alone is not sufficient). `tokyo-official-1102` 小ホール is a distinct facility-space from the same complex's 大ホール draft `koganei-miyaji-main-standard` (`tokyo-koganei-miyaji-main`), which remains an out-of-scope future hard-case candidate and was not touched by this DEFER. `tokyo-official-0249` 早稲田奉仕園〔スコットホール・講堂〕 was that same wave's single ADOPT, but the `TOKYO-OFFICIAL-0249 SCOTT HALL BOUNDED JUDGMENT AUDIT` reversed it to **HARD-CASE DEFER**; its `researchStatus` is now `blocked` and its two-pass implementation is cancelled.
- Future hard-case candidates (`tokyo-theatre-1010`, `tokyo-koganei-miyaji-main`, `tokyo-coverage-ex-theater-ariake`, `aichi-arts-center-main`, `takasaki-city-theatre-main`) are outside this ordinary wave.

## Release verification

- The GitHub check run **Workers Builds: seat-lottery-simulator** for KANAGAWA-K-ARENA-YOKOHAMA production commit `8d295e4` completed successfully. At `https://seat-lottery-simulator.studiotomo.workers.dev/`, searching `Kアリーナ` returned exactly one result, Kアリーナ横浜 (神奈川県 横浜市), displaying **抽選対象 15,929席** — identical to the mapped subtotal. The runtime detail lazy-loaded over HTTP 200 and six draws completed across all three levels with floor-qualified labels: `5階ミドルスタンド 3列4番`, `7階アッパースタンド 25列218番`, `5階ミドルスタンド 19列27番`, `5階ミドルスタンド 6列57番`, `7階アッパースタンド 5列40番`, `3階ロアースタンド 4列128番`. No browser console warning or error was produced.
- The deployed runtime payload was machine-checked in the browser: 15,929 expanded seats, 15,929 unique `(area, row, seat)` keys, **0 duplicates**, per-area totals 3階 4,978 / 5階バルコニー 235 / 5階ミドル 4,137 / 7階 6,579, and only those four registered upper-level areas — **0** excluded 1F or VIP identifiers. Gap probes confirmed the issuer's own omissions are not drawable: 3階4列 117 and 118 absent while 116 is present, 3階29列 9 and 13 absent while 14 is present, 3階29列 37 absent at GATE 301, 3階28列 100 absent behind the 車イス席 platform, 5階ミドル 4列 absent (the centre has no rows 4-5) and 5階バルコニー 4列 absent, and 7階 1列 exists only in the centre. The runtime JSON leaks no source, checked-at, verification, or URL field.
- Canonical `npm run verify:venues` passed: venue/inventory/release coverage, generated-artifact sync, fingerprints, lint, typecheck, 212 unit tests, production build, 17 E2E tests, docs verification, and diff check.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for `9d2a66c` completed successfully.
- At `https://seat-lottery-simulator.studiotomo.workers.dev/`, the venue selector found the three promoted venues with 1,027 / 729 / 1,102 displayed seats. One draw was started for each; no console errors were observed.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for Stellar Ball production commit `a277a5a` completed successfully. The public runtime searched `ステラボール` to one result, displayed 876 seats, completed one draw (`1F シートフロア D列24番`), and reported no console warning or error.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for TOKYO-WAVE-26 production commit `672c275` completed successfully. The public runtime found 伝承ホール at 339 seats, ルネこだいら中ホール at 398 seats, and 浜離宮朝日ホール小ホール at 398 seats; one draw completed for each and browser console errors were absent.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for Wave27 production commit `66fc2c6` completed successfully (build `8028170a-a184-415e-b874-0475e5df7fdf`). The public runtime found RISURU小ホール at 246 seats, 杉並公会堂小ホール at 190 seats, and 大泉学園ゆめりあホール at 176 seats; one RISURU draw completed (`5列 6番`) and the browser console had no warning or error.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for Wave28 production commit `d2dc595` completed successfully (build `c8a1eda6-2dc7-41a9-bf1c-ddc5ee1759c2`). The public runtime found 吉祥寺シアター at 189 seats, 赤坂RED/THEATER at 176 seats, and 矢来能楽堂 at 237 seats; one draw completed for each (`客席 C列5番`, `客席 B列8番`, `座敷正面席 こ列4番`) and the browser console had no warning or error.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for Wave29 production commit `bcc0ff6` completed successfully (build `07036eae-7a48-41ef-8cd2-2c081b3175cf`). The public runtime found セルリアンタワー能楽堂 at 201 seats and completed a draw (`脇正面 1列8番`); it found 紀伊國屋ホール at 427 seats and completed a draw (`中央ブロック T列8番`). The browser console had no warning or error.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for Wave30 production commit `ffea65d` completed successfully (build `5e828ed0-d878-447a-a192-a3d2750370fd`). The public runtime found シアターグリーン〔BIG TREE THEATER〕 at 167 seats and completed a draw (`D列 6番`). The browser console had no warning or error.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for Wave31 production commit `a9f9e76` completed successfully (build `1be3fc8f-5a84-43cb-a4b0-bad3a1c67a2a`). The public runtime found シアターグリーン〔BOX in BOX THEATER〕 at 104 seats and completed a draw (`A列 14番`). The browser console had no warning or error.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for Wave32 production commit `6c6778e` completed successfully (build `708d8af8-f636-4098-bade-d3028e23fb64`). The public runtime found ユーロライブ at 178 seats and completed a draw (`B列 12番`). The browser console had no warning or error.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for TOKYO-WAVE-34 production commit `5a53b6c` completed successfully (build `740c77fb-52f9-49b5-a291-e28c7b75ba91`). The public runtime found 東京グローブ座 by name and by the alias グローブ座 at 699 seats and completed eight draws across all four areas (`2階席 B列15番`, `1階平土間 G列22番`, `1階ギャラリー O列32番`, `1階ギャラリー N列35番`, `2階席 B列43番`, `3階席 A列33番`, `3階席 B列21番`, `3階席 A列20番`). A full enumeration of the deployed runtime payload confirmed 699 seats, per-area 307/148/158/86, no row-O 14–17 or 50–53, no row-M 21–40, and no duplicate area/row/number key. The browser console had no warning or error.
- Canonical `npm run verify:venues` passed for the SAITAMA-CITY-CULTURE-MAIN wave (137 production venues, 139 selectable configurations, 190,099 configuration-seat records, 43 warnings, 17 E2E tests). The warning count is identical to the pre-change baseline, so the wave introduced none; the catalog/database 500-venue soft-limit projections remain among them and no threshold, compaction, or refactor was applied to silence them.
- The GitHub check run **Workers Builds: seat-lottery-simulator** for the saitama-city-culture-main HOLD commit `7ad4ad1` completed successfully (build `22157fe3-1232-4d09-9087-c8d86ddc6975`). This wave added no venue, so the public smoke verified non-regression and correct absence: searching さいたま市文化センター returns 絞り込み結果 0件, `/venue-db/venues/saitama-city-culture-main--standard.json` serves only the SPA HTML fallback rather than runtime data, and the produced 水戸市民会館〔グロービスホール〕 still loads real runtime JSON, displays 2,000席, and completed one draw (`1階 15列 36番`). The browser console had no warning or error.

The authorized `TOKYO PRIORITY D RANKED NEXT PREFLIGHT 2` is complete with **1 ADOPT / 3 DEFER / 1 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-d-ranked-next-preflight-2-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-ranked-next-preflight-2-2026-08-25.json) reranked the remaining unresearched Tokyo Priority-D inventory from repository metadata alone under the established performance-oriented rule (817 records with `venueSourceId` null → 284 with a direct URL → 28 with a dedicated performance identity, ordered by the existing source-located/likely-eligible/URL/capacity tie-break with capacity as a secondary tie-break) and checked the top five in order. `0860` シアターグリーン〔BOX in BOX THEATER〕 is **ADOPT**: its current operator space page and current-linked 客席図面 define the fixed 104-seat configuration with a complete printed set (A-D 1-4/5-14/15-17, E-F 1-4/5-14, G 6-13) whose independently summed total matches the issuer's own 104 exactly, with no wheelchair or removable marking to infer. `0366` すみだパークシアター倉 is **DEFER** because the current operator site states only 常設151席 with no seat chart, PDF, or numbered diagram of any kind. `0266` シアター風姿花伝 is **DEFER** because its current-linked 平面図 is only an unnumbered movable-chair「90席設営例」riser diagram. `0816` 雑司が谷音楽堂 is **DEFER** because its current facility/system pages publish only an unnumbered 見取り図 image and a maximum-capacity figure (最大席数70). `0641` シアター代官山 is **HARD-CASE DEFER**: the current-linked issuer 座席案内 prints a complete A-I set (14 seats per row in three blocks) totalling 126 positions, while the operator states 客席数「最大123席」, with no legend or mark distinguishing which 3 of the 126 differ. Research stopped at the five-candidate ceiling, not at three ADOPTs. The mandated-exclusion DEFER/HARD-CASE-DEFER records from the prior wave were excluded from the pool and untouched. Only report/inventory/HANDOFF changed; production remains **131 venues / 133 selectable configurations / 181,337 configuration-seat records**.

`TOKYO-WAVE-31 PRIORITY D RANKED NEXT 2 ADOPT IMPLEMENTATION` is complete with **1 PRODUCTION / 0 HOLD**. [`data/venue-reports/tokyo-priority-d-ranked-next-2-adopt-implementation-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-ranked-next-2-adopt-implementation-2026-08-25.json) records two independent current-chart transcriptions of the single authorized ADOPT: シアターグリーン〔BOX in BOX THEATER〕 is **104 mapped / 104 published / rangeDiff 0** (A-D列 each 1-17 across three issuer blocks with no internal gap; E-F列 1-14 with no right block; G列 6-13 with no left or right block). Pass 1 read top-down left-to-right, pass 2 independently reread bottom-up right-to-left before any comparison, and both matched on the complete printed set, row/block ownership, ranges/gaps, the issuer's 104 subtotal, and the selected configuration; the current chart carries no wheelchair, removable, or accessibility marking to reflect, and the open-ended 「104席〜」 addition plus the stage/sound/lighting technical drawings remain metadata only, not mixed into the configuration. Source, inventory, Wave31 batch, fingerprint, catalog, and lazy runtime are synchronized at **132 venues / 134 selectable configurations / 181,441 configuration-seat records** in pushed production commit `a9f9e76`. Canonical `npm run verify:venues` passed (213 unit tests, 17 E2E tests); its managed Workers Build completed successfully, and the public UI found シアターグリーン〔BOX in BOX THEATER〕 displaying 104 seats, completed one draw (`A列 14番`), and had no console warning or error.

The authorized `TOKYO PRIORITY D RANKED NEXT PREFLIGHT 3` is complete with **1 ADOPT / 4 DEFER / 0 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-d-ranked-next-preflight-3-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-ranked-next-preflight-3-2026-08-25.json) reranked the remaining unresearched Tokyo Priority-D inventory from repository metadata alone under the established performance-oriented rule (812 records with `venueSourceId` null → 279 with a direct URL → 20 with a dedicated performance identity, minus `0460` シアターH excluded before ranking as a duplicate facility-space of already-produced `theater-h-standard` → 19 genuine tier-1 candidates), then checked the top five in order, including `0861` シアターグリーン〔BASE THEATER〕 as an ordinary pool member per explicit instruction rather than excluding it for its produced siblings. `0861` is **DEFER**: its current BASE THEATER facility page states 座席数「70席〜」 and explicitly discloses 「現在、席番表示はありません」 (no seat-number display at present), and its linked 客席配置図 draws only unnumbered tiered chair blocks, a step-height scale, and an unnumbered 車椅子席 area totalling 70. `0893` シアター・バビロンの流れのほとりにて is **DEFER**: its own domain fails TLS verification, and current-linked secondary sources describe a flat free-use space with no permanent seating and conflicting capacity figures (48 vs 50). `0818` 西巣鴨音楽堂 is **DEFER**: the current site is a piano-salon rental space with only a variable occupancy limit (20-30 people), no seat chart. `0635` ユーロライブ is **ADOPT**: its current official 座席表 image (fetched via curl after a WebFetch TLS handshake failure) defines a complete A-M row/block-numbered 178-seat chart matching the issuer's own 178 total exactly, with M-13/M-14 retained as printed IDs under a 2023-07 補助席 disclosure. `0813` 杉並能楽堂 is **DEFER**: its current site (guidance/access/history/performances pages, also fetched via curl) publishes a rental fee schedule and a performance archive through 2019 but no seat chart, row label, or capacity figure at all. Research stopped at the five-candidate ceiling, not at three ADOPTs. The mandated-exclusion DEFER/HARD-CASE-DEFER records from prior waves were excluded from the pool and untouched. Only report/inventory/HANDOFF changed; production remains **132 venues / 134 selectable configurations / 181,441 configuration-seat records**.

The authorized bounded two-pass implementation attempt for `TOKYO PRIORITY D RANKED NEXT PREFLIGHT 3`'s single ADOPT is complete with **0 PRODUCTION / 1 SOURCE/POLICY HOLD**. [`data/venue-reports/tokyo-priority-d-ranked-next-preflight-3-implementation-attempt-2026-08-25.json`](../data/venue-reports/tokyo-priority-d-ranked-next-preflight-3-implementation-attempt-2026-08-25.json) first checked the repository's source-URL transport policy: `scripts/venues/validation.mjs:182` requires an HTTPS `sources[].url` unconditionally, for every source status (draft and production alike, confirmed by its unconditional call site at line 577), with no exception in `VENUE_DATA_GUIDE.md` or the validator test suite. Independent, repeated TLS-layer checks against `eurolive.jp:443` (and its parent domain `eurospace.co.jp`, same hosting) on two separate client stacks (curl/schannel and OpenSSL 3.5.5), across TLS 1.2 and TLS 1.3, all failed identically with a server-originated `tlsv1 alert internal error` before any certificate exchange — confirming no current official HTTPS endpoint exists for this venue, not a transient or client-side issue. Per that policy conclusion, the report still performed the independent two-pass visual transcription to preserve the evidence: pass 1 read top-down A-M left-to-right, pass 2 independently reread bottom-up M-A right-to-left from a separately fresh-fetched (byte-identical, MD5-confirmed) copy of the same current 座席表 image, and both MATCHED exactly on the complete printed set (A-K: left 1-4/centre 5-12/right 13-15 each 15 seats; L: centre 5-12/right 13-15, no left block, 11 seats; M: right 13-14 only, 2 seats; total 178), row/block ownership, the issuer's own 178 subtotal, the single configuration, and the M-13/M-14 補助席 disclosure. Because the seat evidence is complete but the transport-policy gate cannot currently be satisfied without fabricating an HTTPS URL (prohibited), no source JSON was authored and the venue closes as **SOURCE/POLICY HOLD**, matching the established pattern for `tokyo-official-1071` and `tokyo-official-1020`. Only report/inventory/HANDOFF changed; production remains **132 venues / 134 selectable configurations / 181,441 configuration-seat records**.

`TOKYO-WAVE-32 EUROLIVE ADOPT IMPLEMENTATION` is complete with **1 PRODUCTION**. [`data/venue-reports/tokyo-wave-32-eurolive-adopt-implementation-2026-08-26.json`](../data/venue-reports/tokyo-wave-32-eurolive-adopt-implementation-2026-08-26.json) re-verified `tokyo-official-0635` ユーロライブ from fresh 2026-08-26 evidence rather than reusing the preserved 2026-08-25 result. HTTPS was independently rechecked against `eurolive.jp:443` with curl/schannel and OpenSSL 3.5.5 across TLS 1.2/1.3 and remains unavailable (identical server-originated `tlsv1 alert internal error`), so HTTP-only stays current; each of the three official HTTP sources (ACCESS page, current 座席表 image, RENT page) carries its own explicit `httpOnlyOfficial` waiver rather than one waiver reused across sources. The 座席表 image was independently re-fetched twice (byte-identical, MD5 `44b3ced97231dae49c72968d3ece98a7`, unchanged from 2026-08-25) and re-transcribed twice in reversed row/block order, MATCHING exactly on the complete A-M printed ID set (A-K: left 1-4/centre 5-12/right 13-15, 15 each; L: centre 5-12/right 13-15, no left block, 11; M: right 13-14 only, 2; total 178) and the M-13/M-14 補助席 disclosure, which is retained as ordinary selectable printed IDs, not deleted. Schema v2 source `eurolive-standard` (configuration `standard-178-seat-map`) is **PRODUCTION**: 178 mapped / 178 published / rangeDiff 0. The inventory record moved from `blocked`/`needs-research` to `researchStatus: production` / `eligibility: eligible` with `venueSourceId: eurolive-standard` and the same waiver shape on `officialFacilityUrl`. Source, inventory, new batch `tokyo-wave-32-eurolive-adopt-implementation`, production fingerprint, catalog, and lazy runtime are synchronized at **133 venues / 135 selectable configurations / 181,619 configuration-seat records**. Canonical `npm run verify:venues` passed (224 unit tests, 17 E2E tests, 0 errors); the only new warnings are the three expected `httpOnlyOfficial` transport warnings plus the corresponding inventory warning.

The authorized `TOKYO PRIORITY D RANKED NEXT PREFLIGHT 4` is complete with **0 ADOPT / 5 DEFER / 0 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-d-ranked-next-preflight-4-2026-08-26.json`](../data/venue-reports/tokyo-priority-d-ranked-next-preflight-4-2026-08-26.json) reranked the remaining unresearched Tokyo Priority-D inventory from repository metadata alone under the established performance-oriented rule (969 records → 807 with `researchStatus` not-started/source-located and `venueSourceId` null, 5 fewer than the prior preflight's 812 → 274 with an officialFacilityUrl or discoveryUrlRaw → 21 with a dedicated performance identity, minus `0460` シアターH excluded before ranking as the same facility-space as already-produced `theater-h-standard` → 20 genuine tier-1 candidates), then checked the top five in order. `0704` 代々木能舞台 is **DEFER**: its official HTTPS endpoint fails certificate hostname validation (a *.lekumo.jp certificate, not the venue's own host) but the plain-HTTP page and a Wikipedia secondary both describe only 座布団敷き (tatami/zabuton floor seating) with approximate totals (約160席／約80席), no numbered chart. `0087` 銀座タクト is **DEFER**: its current official page (working HTTPS) links a real 座席表 image, but the image numbers only 23 tables, not individual chairs, and states 客席は最大80席 as a ceiling, not an enumerated seat set. `1155` グスタフスタジオ〔シアターグスタフ〕 is **DEFER**: the current official page (working HTTPS) states only 最大130名収容可能 with no seat chart, and a secondary source gives a conflicting approximate figure (約90席). `0691` and `0692` LUMINE 0〔スタジオ1／スタジオ2〕 are both **DEFER**: the shared current official page (working HTTPS, though the inventory's recorded `discoveryUrlRaw` is HTTP) describes only a flexible movable/bleacher-style multipurpose event space with format-dependent capacity for a related main hall, and publishes no per-studio seat chart. One candidate (`0704`) had an HTTPS certificate mismatch and the LUMINE studios had a stale recorded HTTP `discoveryUrlRaw` even though HTTPS works; per the current transport policy neither fact was treated as a DEFER reason by itself, and no candidate reached ADOPT so no `httpOnlyOfficial` waiver was needed. Research stopped at the five-candidate ceiling without reaching any ADOPT. The twelve prior DEFER, three HARD-CASE DEFER, and two production-duplicate records were excluded from the pool and untouched. Only report/inventory/HANDOFF changed; production remains **133 venues / 135 selectable configurations / 181,619 configuration-seat records**.

| Item | State |
| --- | --- |
| Tokyo-Wave-32 (ユーロライブ) | 1 PRODUCTION. Production totals are 133 venues / 135 selectable configurations / 181,619 configuration-seat records. |
| Tokyo priority D ranked next preflight 4 | Preflight: 0 ADOPT / 5 DEFER (`0704` 代々木能舞台, `0087` 銀座タクト, `1155` グスタフスタジオ, `0691`/`0692` LUMINE 0 スタジオ1/2). No production change; totals remain 133 / 135 / 181,619. |

The authorized `TOKYO PRIORITY D CROSS-TIER HIGH-YIELD PREFLIGHT` is complete with **1 ADOPT / 4 DEFER / 0 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-d-cross-tier-high-yield-preflight-2026-08-26.json`](../data/venue-reports/tokyo-priority-d-cross-tier-high-yield-preflight-2026-08-26.json) re-ranked the remaining unresearched Tokyo Priority-D pool (969 records → 801 with `researchStatus` not-started/source-located and `venueSourceId` null, after excluding duplicate `0460` シアターH) by cross-ranking dedicated theater/auditorium identities together with civic 大ホール/中ホール/小ホール-designated spaces on equal footing, rather than exhausting the dedicated-identity screen first, then breaking ties by source-located/likely-eligible/URL/capacity signals with a yield penalty for studio/training/live-spot/multipurpose/sub-100-seat wording. This surfaced academic/museum 講堂 and a civic 小ホール ahead of the previously-queued `0688`/`1147`/`0210`/`0800`, none of which were opened. `0249` 早稲田奉仕園〔スコットホール・講堂〕 is **ADOPT**: its current official facility page directly links a current 座席表 PDF defining a complete 200-seat configuration (1F rows 1-10, 2F rows 11-15, each row split into left/right blocks, with printed pillar-obstruction exclusions at row 9/10 position 5 and 10) whose independently summed total (136 + 64 = 200) matches the issuer's own printed subtotal exactly. `0337` 国立科学博物館〔日本館講堂〕 is **DEFER**: the current facility page shows no seating figures at all, and the one externally indexed rental-guide PDF is now a dead link. `1102` 小金井 宮地楽器ホール〔小ホール〕 is **DEFER**: the current facility page explicitly describes a flat-floor (平土間式) room with user-assembled stacking chairs and a maximum-only capacity (最大150席), no fixed seat chart; this is a distinct facility-space from the same complex's out-of-scope future hard-case draft `koganei-miyaji-main-standard` (大ホール), which was not touched. `0464` 日本近代文学館〔講堂〕 is **DEFER**: the current page states only an approximate 座席数100前後, no chart. `0338` 東京都美術館〔講堂〕 is **DEFER**: the current page and its full linked usage-guide PDF state only 定員230名（車いすスペース5台分含む）, no chart. Research stopped at the five-candidate ceiling with 1 ADOPT, short of the three-ADOPT stop condition. All seventeen prior DEFER, three HARD-CASE DEFER, two production-duplicate records, and the out-of-scope future hard-case draft were excluded from the pool and untouched. Only report/inventory/HANDOFF changed; production remains **133 venues / 135 selectable configurations / 181,619 configuration-seat records**.

The authorized `TOKYO-OFFICIAL-0249 SCOTT HALL BOUNDED JUDGMENT AUDIT` is complete and **reverses that preflight's single ADOPT to HARD-CASE DEFER**. [`data/venue-reports/tokyo-official-0249-scott-hall-judgment-audit-2026-08-26.json`](../data/venue-reports/tokyo-official-0249-scott-hall-judgment-audit-2026-08-26.json) re-fetched the current facility page and every current-linked first-party document of the same issuer. The two 座席表PDF links on the page (`_src/63662848/` and `_src/63494111/`) are byte-identical, so they carry no revision conflict. Direct re-reading of that chart confirms a complete numbered set of 200 printed identifiers — 1F rows 1-8 left 1-7 / right 8-14, rows 9-10 the same 14 drawn boxes but only 12 printed identifiers each, 2F row 11 1-12, rows 12-13 1-16, row 14 1-14, row 15 1-6 — reproducing its own printed footnote `1階136席・2階64席｜計200席` exactly. **The four X positions are settled**: each is a drawn seat box carrying no printed number, with an X of two corner-to-corner diagonals drawn over the empty box, and the neighbouring numbering is not renumbered (left prints 1,2,3,4,6,7 and right prints 8,9,11,12,13,14), so identifiers 5 and 10 exist in the row space but are unissued in rows 9 and 10; arithmetic confirms they are outside the chart's own 136 (8x14 + 2x12 = 136). The audit also found two current-linked **2025.10 講堂案内図** documents (`2510_講堂案内図`, linked as 見取図PDF from the same table cell and from 音楽利用; `202510_講堂案内図（撮影用）`, linked from 撮影利用) that both draw exactly 140 1F seat cells, both state 客席 **1階140席・2階60席**, and mark the same four positions with the legend 「柱のため舞台全体が見えない席」. The **1F relation is therefore issuer-explained** (140 physical seats minus the 4 unnumbered pillar-obstructed positions = the chart's 136), but the **2F relation is not**: the chart draws and numbers 64 2F seats while three current issuer statements say 60, no 2F seat drawing exists to check the 60 against, and the difference runs opposite in sign to the 1F convention. No issuer correction, errata, or generation/configuration distinction exists anywhere on the issuer domain. The preflight's description of the page's 1F140/2F60 split as a "coarser, rounder disclosure" is **retracted as unsupported**. The matching 200 grand total was not accepted as evidence, and no seat was deleted, added, or moved between floors. Only report/inventory/HANDOFF changed; production remains **133 venues / 135 selectable configurations / 181,619 configuration-seat records**.

| Item | State |
| --- | --- |
| Tokyo priority D cross-tier high-yield preflight | Preflight: 1 ADOPT `0249` 早稲田奉仕園〔スコットホール・講堂〕 200 (current 座席表 PDF, 136+64=200 exact). `0337`/`1102`/`0464`/`0338` are blocked DEFER. No production change; totals remain 133 / 135 / 181,619. |
| Tokyo official 0249 Scott Hall judgment audit | That preflight's ADOPT is superseded: `0249` is now blocked **HARD-CASE DEFER** (complete numbered chart exists, issuer 2F subtotal 60 vs charted/numbered 64 unresolved; 1F 140 vs 136 explained by the 4 pillar-obstructed unnumbered positions). Four X positions settled as drawn-but-unnumbered. No production change; totals remain 133 / 135 / 181,619. |
| Tokyo priority D cross-tier high-yield preflight 2 | Preflight: 1 ADOPT `1122` 日野市民会館〔小ホール〕 208 (current 座席表 PDF, 11+19+168+10=208 exact, cross-checked against city page/FAQ/accessibility page with no conflict). `0235`/`0045`/`1145` are blocked DEFER; `0395` ティアラこうとう〔小ホール〕 is blocked **SOURCE/POLICY HOLD** (facility-wide renovation closure through 2027-10, mirroring sibling 大ホール). No production change; totals remain 133 / 135 / 181,619. |
| TOKYO-WAVE-33 (ひの煉瓦ホール 小ホール) | 1 PRODUCTION. Production totals are 134 venues / 136 selectable configurations / 181,827 configuration-seat records. |

The authorized `TOKYO PRIORITY D CROSS-TIER HIGH-YIELD PREFLIGHT 2` is complete with **1 ADOPT / 3 DEFER / 1 SOURCE/POLICY HOLD**. [`data/venue-reports/tokyo-priority-d-cross-tier-high-yield-preflight-2-2026-08-26.json`](../data/venue-reports/tokyo-priority-d-cross-tier-high-yield-preflight-2-2026-08-26.json) recomputed the same cross-tier rule live against current repository metadata (969 Tokyo records → 797 Priority-D records with `researchStatus` not-started/source-located and `venueSourceId` null → 796 after excluding the already-identified duplicate `0460` → 96 matching the unified Tier-1/Tier-2 keyword screen) rather than reusing the fixed reference order from the prior handoff, and the recomputation reproduced that same reference order at the top: `0235` (210), `1122` (208), `0045` (207), `1145` (150), `0395` (140). `0235` 新宿区立新宿文化センター〔小ホール〕 is **DEFER**: the current official facility/hall pages and a current (2025.07) 舞台平面図PDF all state 210席（移動椅子）with only generic unlabeled chair-block icons, no row or seat numbering. `1122` 日野市民会館(ひの煉瓦ホール)〔小ホール〕 is **ADOPT**: the current official 座席表 PDF (2019.04.01) prints a complete 11-row, left/center/right-block numbered set whose independent sum (11+19+168+10=208) matches the issuer's own 208 exactly; cross-checked against the current city facility page, FAQ, and accessibility page, all agree on 208 with no conflicting subtotal, and the accessibility page explicitly directs wheelchair-seat inquiries to the *large* hall's own chart rather than marking a space in the small hall, so the absence of a wheelchair marking on this chart is issuer-explained, not an unresolved discrepancy (two non-seat toilet-legend icons on the chart sit outside the seating-block boundary and are not seat markings). The inventory's recorded `officialFacilityUrl` (a malformed `www.hino-_kaikan.jp`) was corrected to the verified working host. `0045` 千代田区立日比谷図書文化館〔日比谷コンベンションホール(大ホール)〕 is **DEFER**: the current facility page states 207名 but its current-linked 全体平面図PDF draws only generic unlabeled chair icons in the fan-shaped auditorium, no numbering. `1145` くにたち福祉会館〔大ホール〕 is **DEFER**: the current facility page states only an approximate maximum "〜150人程度" with 机・椅子 furnishing, no seat chart. `0395` 江東公会堂ティアラこうとう〔小ホール〕 is **SOURCE/POLICY HOLD**: the current official page states the entire facility is closed for a large-scale renovation (including stage audio/lighting digitalization) through 2027年9月ごろ, reopening 2027年10月; this mirrors the already-blocked sibling `tokyo-tiara-koto-main` 大ホール record for the identical closure, so no pre-renovation seat evidence was evaluated. Research stopped at the five-candidate ceiling with 1 ADOPT, short of the three-ADOPT stop condition. All prior DEFER, HARD-CASE DEFER, production, and duplicate records (including `0249`, `0278`, `0586`, `0641`, `0635`, `0460`) were excluded from the pool and untouched. Only report/inventory/HANDOFF changed; production remains **133 venues / 135 selectable configurations / 181,619 configuration-seat records**.

`TOKYO-WAVE-33 HINO BRICK HALL SMALL HALL ADOPT IMPLEMENTATION` is complete with **1 PRODUCTION**. Fresh official evidence was re-confirmed at implementation time (not the preflight's transcription): the current 小ホール page, its directly linked 座席表 PDF, the FAQ, the facilities/accessibility page, and the city's independent facility page all matched the preflight's findings with no change. The current 座席表 PDF (20190401_日野_小ホール座席表_第1版) was independently re-fetched and transcribed twice — pass 1 read 1列→11列, left→center→right; pass 2 independently re-read 11列→1列, right→center→left from a separately fresh-fetched copy — and both MATCHED exactly on the complete printed set (1列: center only 6-16, 11 seats; 2列: left 2-5/center 6-16/right 17-20, 19 seats; 3-10列: left 1-5/center 6-16/right 17-21, 21 seats each ×8 rows = 168; 11列: left 1-5/right 17-21 only, no center block, 10 seats; total 208), row/block ownership, and the issuer's own 208 total exactly (rangeDiff 0). Cross-document reconciliation was re-checked at implementation time and found no new conflict: the seating chart, the current hall page, and the city page all state 208 with no discrepancy. The two facility-legend icons (car/men's/women's toilet symbols) printed near the corridor/stairs area outside the seating-block boundary were re-confirmed on both passes to be non-seat facility markers, not seat or accessibility markings. Schema v2 source `hino-brick-small-standard` (configuration `standard-208-seat-map`) is **PRODUCTION**: 208 mapped / 208 published / rangeDiff 0. `wheelchairSemantics.status` is recorded as `not-applicable` (the same value already used by `cerulean-tower-noh-standard`, `eurolive-standard`, `suginami-kokaido-small-standard`, and both シアターグリーン sources for "the chart defines no conversion/marking relationship") with a `description` that precisely states the current-evidenced fact — no fixed wheelchair seat ID/space is defined for this hall's numbered chart, small-hall accommodation is handled by direct inquiry per the current FAQ, and wheelchair-seat guidance is instead directed to the large hall's own chart — rather than the false claim that no wheelchair accommodation exists. The inventory record moved from `researchStatus: source-located`/`blockingReason: ADOPT...` to `researchStatus: production`/`eligibility: eligible` with `venueSourceId: hino-brick-small-standard`, keeping the corrected `officialFacilityUrl` (`https://www.hino-kaikan.jp/about/hall_small/`) from the preflight rather than restoring the old malformed URL. Source, inventory, new batch `tokyo-wave-33-hino-brick-small-adopt-implementation`, production fingerprint, catalog, and lazy runtime are synchronized at **134 venues / 136 selectable configurations / 181,827 configuration-seat records** in commit `7294177`. Canonical `npm run verify:venues` passed (224 unit tests, 17 E2E tests, 0 errors); the only warnings are the pre-existing ones (the three `httpOnlyOfficial` eurolive warnings and the pre-existing official-vs-mapped-count warnings on unrelated venues) plus one new warning — the production catalog crossed its 100,000-byte soft limit (100,091 bytes) purely from organic growth, which is a review-only capacity warning, not an error.

The authorized `TOKYO PRIORITY D CROSS-TIER HIGH-YIELD PREFLIGHT 3` is complete with **0 ADOPT / 4 DEFER / 1 HARD-CASE DEFER**. [`data/venue-reports/tokyo-priority-d-cross-tier-high-yield-preflight-3-2026-08-26.json`](../data/venue-reports/tokyo-priority-d-cross-tier-high-yield-preflight-3-2026-08-26.json) recomputed the same cross-tier rule live against current repository metadata (969 Tokyo records → 792 Priority-D records with `researchStatus` not-started/source-located and `venueSourceId` null, matching `venues:inventory:report`'s live figure → 791 after excluding the already-identified duplicate `0460` → 94 matching the unified Tier-1/Tier-2 keyword screen) rather than reusing the fixed reference order from the prior handoff. This recomputation did **not** reproduce that reference order at the top: it surfaced two `source-located`/`likely-eligible` records with direct operator URLs — `0058` (159) and `0297` (75) — ahead of the previously-queued `0443`/`1008`/`1126` trio, because the established tie-break weighs `researchStatus`/`eligibility` above capacity. `0058` インスティトゥト・セルバンテス東京〔オーディトリアム〕 is **HARD-CASE DEFER**: its current official facility page links a seating-chart image defining a complete 159-seat configuration (rows A-L, independently summed 78+70+11=159) matching the same page's own printed 159 exactly, but a separate current official about-us page states the same building's hall accommodates approximately 200 people, with no issuer-published relation between the two figures — per the `tokyo-official-0249` Scott Hall precedent, this unexplained cross-document gap blocks ADOPT rather than being dismissed as benign rounding. `0297` 東京都助産師会館〔講堂〕 is **DEFER**: the current official room-rental page states only 最大72名 with hourly pricing and rentable equipment, no seat chart. `0443` 小山台会館〔大ホール〕 is **DEFER**: the current facility page explicitly states clients set up and remove their own chairs and tables, and its linked floor-plan PDF is an architectural drawing with unnumbered chair-hatching. `1008` 幸学習館〔講堂〕 is **DEFER**: the only currently reachable official page publishes a photo, equipment list, and capacity 130 with no seat chart; two indexed pages that might otherwise carry a floor plan both currently 404. `1126` 平山交流センター〔大ホール〕 is **DEFER**: the current facility page describes an explicitly flat vinyl-floor room furnished with 24 movable tables and 100 movable chairs, no fixed seating. Research stopped at the five-candidate ceiling with 0 ADOPT, short of the three-ADOPT stop condition. All prior DEFER, HARD-CASE DEFER, SOURCE/POLICY HOLD, production, and duplicate records were excluded from the pool and untouched. Only report/inventory/HANDOFF changed; production remains **134 venues / 136 selectable configurations / 181,827 configuration-seat records**. The production catalog's known 100,000-byte soft-limit warning is unchanged at 100,091 bytes (no catalog/runtime generation was run by this preflight).

| Item | State |
| --- | --- |
| Tokyo priority D cross-tier high-yield preflight 3 | Preflight: 0 ADOPT / 4 DEFER / 1 HARD-CASE DEFER. `0058` インスティトゥト・セルバンテス東京〔オーディトリアム〕 is blocked **HARD-CASE DEFER** (complete 159-seat chart vs. a separate current page's approximate ~200-person figure, unreconciled). `0297`/`0443`/`1008`/`1126` are blocked DEFER (no numbered seat chart in current material). No production change; totals remain 134 / 136 / 181,827. |
| tokyo-official-0278 東京グローブ座 judgment audit | Audit only. **HARD-CASE DEFER → ADOPT.** Fresh two-pass MATCH gives printed IDs 1F 455 (平土間307 + ギャラリー148) / 2F 158 / 3F 86 = 699 against the published 463 / 158 / 86 = 707. The current 1F chart itself annotates its two wheelchair areas 車いす席／一般席とした場合 14〜17番 and 50〜53番, both on gallery row O, so the 8 unnumbered positions are **inside** 707 and replace 8 numbered seats: 148 + 8 = ギャラリー156 and 699 + 8 = 707. Map the 699 printed IDs only, keep `expectedSeatCount` 707. That audit did not authorize an implementation; **superseded by TOKYO-WAVE-34**, which performed the separately authorized fresh two-pass and promoted the venue. |
| TOKYO-WAVE-34 (東京グローブ座) | 1 PRODUCTION. `tokyo-official-0278` → `globe-tokyo-standard` / `current-official-printed-seat-map`, 699 mapped / 707 published / rangeDiff −8, after a currentness re-check (charts byte-unchanged) and a fresh independent two-pass MATCH. Production totals are **135 venues / 137 selectable configurations / 182,526 configuration-seat records** in pushed commit `5a53b6c`. Catalog soft-limit warning moves 100,091 → 101,782 bytes with no threshold change. |
| KANAGAWA-PIA-ARENA-MM (ぴあアリーナMM) | 1 PRODUCTION. `kanagawa-pia-arena-mm` → `pia-arena-mm` / `stand-floors-fixed`, **5,573 mapped** (2F 2,700 / 3F 1,670 / 4F 1,203) / 6,077 published stand 客席数 / rangeDiff −504, after a fresh independent two-pass MATCH on byte-unchanged current charts. The 公称 12,141 is the issuer's 立見時 maximum (arena floor 6,064 + stand 6,077) and is not a fixed-seat total; the −504 decomposes exactly as 立見440 + 車イス10 + 車イス10 + VIP44. `scope.kind: fixed-only`; 1F ARENA FLOOR is excluded as issuer-defined event-dependent seating with no printed IDs. Production totals are **136 venues / 138 selectable configurations / 188,099 configuration-seat records**. Catalog soft-limit warning moves 101,782 → 102,874 bytes with no threshold change. |
| Tokyo priority D bounded portfolio / lane audit | Audit only, no research. Decision: **CONTINUE TOKYO ORDINARY RESEARCH** with a corrected research-ordering rule. Priority D is the capacity-unknown bucket (717 of 969 null capacity), 787 outstanding records segment to 109 high / 100 medium / 578 low potential, and 102 of the 109 high-potential records were never scored because capacity is null. Yield history: 48 checked, 13 initial ADOPT, 12 eventual production, 2,499 seats; 61.5% in waves 1-3 down to 0/10 in the last two. Only 2 of 34 hard cases are actionable without new issuer evidence. No production change; totals remain 134 / 136 / 181,827. |

## Exact next action

The one exact next action is **`CODEX: KANTO >=1000 HIGH-IMPACT UNRESEARCHED PREFLIGHT 4`** — a separately authorized bounded preflight over the **34 remaining Kanto >=1,000 inventory-unresearched records**, carrying each to a formal disposition (production candidate ADOPT, or a formal blocked/HOLD status with a blocker and reopen condition) without relaxing large-venue evidence policy. It must recompute its own ledger counts from `records[]` in [`data/venue-coverage/kanto-must-coverage-audit-2026-08-27.json`](../data/venue-coverage/kanto-must-coverage-audit-2026-08-27.json) rather than trusting any figure in this file, and it must not reopen any record already carrying a production, formal blocked or HOLD disposition. `gunma-kiryu-culture-main` 美喜仁桐生文化会館〔シルクホール〕 is **no longer in this queue**: it reached PRODUCTION on 2026-09-04 as `kiryu-culture-silk-hall` / `proscenium` (1,517 mapped seats) and must not be re-researched or reopened as a candidate; reopen it only if the issuer publishes a chart superseding SHA-256 `8d8c70a83160534e075700fecf2f7d61209d3884d386d1484ce458e0e0d853d2`, defines a seat-ID replacement relation or per-position numbering for the ten wheelchair spaces, or publishes a numbered chart for the オーケストラピット使用時 or スラストステージ stage forms. **With that, the PREFLIGHT 3 ADOPT implementation queue is empty** (`kanagawa-kaat-hall`, `chiba-ichihara-culture-main` and `gunma-kiryu-culture-main` are all production). The **RaiBoC re-implementation is held as a separate queue** and is not part of this next action: `saitama-raiboc-main` RaiBoC Hall〔大ホール〕 stays blocked pending a corrected re-transcription and must not be combined with Kanto research. **Tokyo Priority D stays ranked after this** while Kanto >=1,000 inventory-unresearched records remain. `chiba-ichihara-culture-main` 市原市市民会館〔大ホール〕 is **no longer in this queue**: it reached PRODUCTION on 2026-09-02 as `ichihara-civic-large-hall` / `standard` (1,527 mapped seats) and must not be re-researched or reopened as a candidate. Once 桐生 is closed, return to the next Codex `KANTO >=1000 HIGH-IMPACT UNRESEARCHED PREFLIGHT`. `kanagawa-kaat-hall` is **no longer in this queue**: it reached PRODUCTION on 2026-09-01 and must not be re-researched or reopened as a candidate. That implementation must treat its `implementationVerificationPlan` in [`data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-3-2026-09-01.json`](../data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-3-2026-09-01.json) as a mandatory gate: fresh independent human visual two-pass, machine-expanded range verification, issuer subtotal reconciliation where available, independent machine structural checksum where feasible, and a production stop/return-to-chart on any mismatch. RaiBoC re-implementation remains a **separate queue** and must not be combined with these new ADOPTs. Tokyo Priority D remains lower priority while Kanto inventory-unresearched records remain. Every implementation action named later in this section or in the narrative sections above is historical unless it is repeated here.

`tokyo-official-1122` 日野市民会館(ひの煉瓦ホール)〔小ホール〕 is now **PRODUCTION** (`hino-brick-small-standard`, TOKYO-WAVE-33); it no longer holds a preflight-stage ADOPT disposition and must not be re-researched or reopened as a candidate.

No work on `tokyo-official-0249` 早稲田奉仕園〔スコットホール・講堂〕 is authorized. Its previously queued two-pass implementation is **cancelled**: the `TOKYO-OFFICIAL-0249 SCOTT HALL BOUNDED JUDGMENT AUDIT` reversed it to blocked HARD-CASE DEFER because the issuer's own current documents assign 4 of the 200 printed identifiers to different floors (chart `1階136席・2階64席`, facility page and both 2025.10 講堂案内図 `1階140席・2階60席`) and only the 1F half of that relation is issuer-explained. Reopen it only under a separately authorized bounded judgment audit, and only once the issuer publishes a current statement of the 2F numbered-seat set, a current 2F seat drawing, or a correction explaining the 136/64 versus 140/60 relation; relative document age and the matching 200 grand total are explicitly not sufficient. If it is ever reopened and reaches implementation, `excluded: [5, 10]` on 1F rows 9 and 10 is the correct notation for the four X positions and those four positions must never be mapped as seats.

`tokyo-official-0278` 東京グローブ座 is now **PRODUCTION** (`globe-tokyo-standard`, TOKYO-WAVE-34, pushed commit `5a53b6c`, Workers Build `740c77fb-52f9-49b5-a291-e28c7b75ba91` successful, public UI smoke passed). It no longer holds an ADOPT, HARD-CASE, or DEFER disposition and must not be re-researched or reopened as a candidate.

The `KANTO RELEASE-CRITICAL >=1000 MUST-COVERAGE AUDIT` is **complete**; its ledger is [`data/venue-coverage/kanto-must-coverage-audit-2026-08-27.json`](../data/venue-coverage/kanto-must-coverage-audit-2026-08-27.json) and it must not be re-run. Both identity questions it was asked are answered there: 立川ステージガーデン is the distinct record `tokyo-tachikawa-stage-garden` (formal HOLD / NEED EVIDENCE, and **not** the same venue as `tokyo-official-1017` アリーナ立川立飛), and SGCホール有明 is `tokyo-coverage-sgc-hall-ariake` with its recorded SOURCE/POLICY HOLD blocker and reopen condition preserved.

`kanagawa-pia-arena-mm` ぴあアリーナMM is now **PRODUCTION** (`pia-arena-mm` / `stand-floors-fixed`, 5,573 mapped seats). It no longer holds an ADOPT disposition and must not be re-researched or reopened. Reopen it only if the operator publishes a numbered seat set for the 1F ARENA FLOOR, per-position numbering for the 2F/3F 車イス spaces, or a numbered 4F VIP set — each of which would extend the excluded scope rather than change the 5,573 printed identifiers already mapped.

**Superseded history (not current scope).** An earlier revision of this file named `separately authorized bounded visual two-pass implementation of saitama-raiboc-main RaiBoC Hall〔大ホール〕` as the one exact next action, with `chiba-pref-culture-main` held after it. Both of those implementations were subsequently performed and are closed; the paragraph is retained only as a record of that wave’s entry points (`https://saitama-culture.jp/raibochall/`, `https://saitama-culture.jp/raibochall/hall/`, `https://saitama-culture.jp/sculwp/wp-content/uploads/omiya-civichall_zaseki01.pdf`) and must not be selected as a current action. RaiBoC did reach production and was then returned to blocked by the `RAIBOC + CHIBA PRODUCTION DISPOSITION RECONCILIATION AUDIT` (2026-08-31); reopening it requires the separate authorization and the reopen condition recorded in the current-state section above, not this superseded paragraph.

`KANTO >=1000 HIGH-IMPACT UNRESEARCHED PREFLIGHT 2` (2026-08-30) is **complete** and must not be re-run; its report is [`data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-2-2026-08-30.json`](../data/venue-reports/kanto-ge1000-high-impact-unresearched-preflight-2-2026-08-30.json). Because **39** Kanto >=1,000 records remain inventory-unresearched, the macro after the two remaining ADOPTs is the next Codex Kanto high-impact unresearched preflight, still ahead of Tokyo Priority D.

`kanagawa-k-arena-yokohama` Kアリーナ横浜 is now **PRODUCTION** (`k-arena-yokohama` / `upper-stands-fixed`, `scope.kind: fixed-only`, 15,929 mapped seats: 3階 4,978 / 5階 4,372 / 7階 6,579). It no longer holds an ADOPT, HARD-CASE, or DEFER disposition and must not be re-researched or reopened as a candidate. The 2026-08-31 `KANAGAWA-K-ARENA-YOKOHAMA BOUNDED VISUAL TWO-PASS IMPLEMENTATION` record is preserved as **historical fact**: its blocker was human-only transcription scalability over roughly 1,100 printed row-ranges, not missing issuer evidence, and it was reopened on an explicit user decision that this venue is release-critical, under the new narrow `DENSE-VECTOR verification` exception in [`VENUE_DATA_GUIDE.md`](VENUE_DATA_GUIDE.md). Reopen it only if the issuer publishes a seat map superseding SHA-256 `24a838884437b225bf19f6dbe4f40bf95e80204278f48b0528da4c7b603967ee`, a numbered set for the performance-dependent LEVEL1 アリーナ, per-position numbering for the 3F 車イス席 platform, or a numbered VIP set — each of which would extend the excluded scope rather than change the 15,929 printed identifiers already mapped. Never fit to 20,033: it is the whole-venue figure and includes LEVEL1.

`saitama-city-culture-main` さいたま市文化センター〔大ホール〕 is **no longer an ADOPT candidate and no longer the next action**. Its 2026-08-29 bounded implementation closed it as a formal **RENOVATION / CURRENTNESS HOLD** on the issuer's announced 2027-06-01 facility-wide 休館 for an approximately two-year 中規模修繕工事, with `recheckNotBefore` 2029-06-01. Do not re-research or reopen it for production before the facility reopens **and** the issuer publishes a post-renovation current 座席表（大ホール）restating the representative configuration and the 固定席/移動席(オケピ)/車椅子席 split. The pre-renovation `culturecenter_zaseki01.pdf`, its exact 2,006 arithmetic, and the hall remaining open for the next nine months are explicitly **not** sufficient to reopen it. Its A仕様 configuration assessment is already recorded in the wave report and does not need to be redone.

The **`TOKYO PRIORITY D PERFORMANCE-IDENTITY PREFLIGHT`** described below is **lower priority than this Kanto work and is not the next action**. Under the release coverage priority, the 39 remaining Kanto >=1,000 inventory-unresearched records must be carried to a formal disposition first; Tokyo Priority D resumes only after that MUST-COVERAGE triage is complete or is separately re-authorized ahead of it. Its recorded method below remains valid for when it is reached.

`ibaraki-mito-civic-main` 水戸市民会館〔グロービスホール〕 is now **PRODUCTION** (`mito-civic-globis-hall` / `standard`, 2,000 mapped seats) and its production two-pass compliance remediation (2026-08-29) is closed. It no longer holds an ADOPT disposition, is no longer a next action anywhere in this file, and must not be re-researched or reopened as a candidate; the narrative sections above that still describe it as upcoming are historical records of earlier waves. `saitama-city-culture-main` is likewise no longer outstanding: it closed on 2026-08-29 as a formal RENOVATION / CURRENTNESS HOLD. Do not reopen a known HARD-CASE as ordinary scope; the authorized Kanto continuation is the bounded unresearched preflight named as the exact next action above. The Kanto release priority in "Release coverage priority (from 2026-08-27)" above is unchanged: 関東 ライブ／演劇／コンサート／大型イベント venues first, with 公称 capacity >= 1,000 as release-critical MUST-COVERAGE.

Do not relax large-venue evidence policy to close coverage: a venue with no current issuer-numbered seat set is recorded under a formal blocked status, not approximated.

Ranked **after** the Kanto >=1,000 MUST-COVERAGE triage above, the remaining Tokyo research macro is a **`TOKYO PRIORITY D PERFORMANCE-IDENTITY PREFLIGHT`**, which also requires separate authorization and is not the next action while 39 Kanto >=1,000 records remain inventory-unresearched. Recompute the corrected research-ordering rule in "Tokyo Priority D portfolio / lane audit (2026-08-27)" live against `data/venue-inventory/tokyo.json`, open at most five tier-1a/1b candidates in the recomputed order, and stop at three ADOPTs or the five-candidate ceiling. Treat the shortlist in that section as reference only and recompute before use; the previously listed `tokyo-official-0688`/`1127`/`0210`/`0444`/`1147`/`0800` set is deprioritised and must not be carried forward as a lead group. Where a record has no `officialFacilityUrl`, locating the current official issuer page is part of the preflight and is not a reason to down-rank it. Do not start any two-pass implementation, further candidate research, or hard-case audit without that authorization.

Keep `tokyo-official-0238` 音楽の友ホール, `tokyo-official-0565` シアタートラム, `tokyo-official-0115` 自由劇場, `tokyo-official-0476` 中目黒キンケロ・シアター, `tokyo-official-0867` 萬劇場, `tokyo-official-0366` すみだパークシアター倉, `tokyo-official-0266` シアター風姿花伝, `tokyo-official-0816` 雑司が谷音楽堂, `tokyo-official-0861` シアターグリーン〔BASE THEATER〕, `tokyo-official-0893` シアター・バビロンの流れのほとりにて, `tokyo-official-0818` 西巣鴨音楽堂, `tokyo-official-0813` 杉並能楽堂, `tokyo-official-0704` 代々木能舞台, `tokyo-official-0087` 銀座タクト, `tokyo-official-1155` グスタフスタジオ, `tokyo-official-0691` LUMINE 0〔スタジオ1〕, `tokyo-official-0692` LUMINE 0〔スタジオ2〕, `tokyo-official-0337` 国立科学博物館〔日本館講堂〕, `tokyo-official-1102` 小金井 宮地楽器ホール〔小ホール〕, `tokyo-official-0464` 日本近代文学館〔講堂〕, `tokyo-official-0338` 東京都美術館〔講堂〕, `tokyo-official-0235` 新宿区立新宿文化センター〔小ホール〕, `tokyo-official-0045` 千代田区立日比谷図書文化館〔日比谷コンベンションホール(大ホール)〕, `tokyo-official-1145` くにたち福祉会館〔大ホール〕, `tokyo-official-0297` 東京都助産師会館〔講堂〕, `tokyo-official-0443` 小山台会館〔大ホール〕, `tokyo-official-1008` 幸学習館〔講堂〕, and `tokyo-official-1126` 平山交流センター〔大ホール〕 as DEFER. Keep `tokyo-official-0395` 江東公会堂ティアラこうとう〔小ホール〕 as **SOURCE/POLICY HOLD** (facility-wide renovation closure, `recheckNotBefore` 2027-10-01) — do not reopen before reopening, and only then with a confirmed post-renovation current seat chart. `tokyo-official-0278` 東京グローブ座 is **PRODUCTION** (`globe-tokyo-standard`, TOKYO-WAVE-34): the 2026-08-27 judgment audit resolved its unnumbered wheelchair-space/707-seat-subtotal relation from the issuer's own chart annotation, and the separately authorized implementation then promoted it at 699 mapped / 707 published / rangeDiff −8. Do not re-research or reopen it. Keep `tokyo-official-0586` 本多劇場 as HARD-CASE DEFER until the issuer states what its diagonally marked M1/M24/Q8/Q17 positions are and whether they fall inside the published 386-seat count, `tokyo-official-0641` シアター代官山 as HARD-CASE DEFER until the issuer states which 3 of its 126 printed positions fall outside the published 123-seat maximum, `tokyo-official-0249` 早稲田奉仕園〔スコットホール・講堂〕 as HARD-CASE DEFER until the issuer resolves its 2階60席-versus-charted-64 relation, and `tokyo-official-0058` インスティトゥト・セルバンテス東京〔オーディトリアム〕 as HARD-CASE DEFER until the issuer publishes a current statement reconciling its dedicated facility page's exact 159/145-seat chart against its about-us page's approximate ~200-person figure. `tokyo-koganei-miyaji-main` (小金井 宮地楽器ホール 大ホール) remains an out-of-scope future hard-case candidate, unaffected by this wave's DEFER records.
