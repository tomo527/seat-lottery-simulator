# Current handoff

Updated: 2026-08-13 (Asia/Tokyo)

## Current state

- `TOKYO-ADDRESSABLE-CONVERSION-1B` was committed as `7f533819ae5745ae826cba660c9cd6912f80a815` (`Close Tokyo addressable conversion 1B`) and pushed to `origin/main`; `main == origin/main` and the working tree was clean before the adequacy review began. 1A remains `d82b59c94098a49ac4bf9b6ffa7ad4e287f95f70`.
- Production: **62 venues / 63 selectable configurations / 85,237 configuration-seat records**. The 60 schema-v1 venues remain unchanged at 81,515 seats; 明治座 adds two schema-v2 configurations totaling 2,816 records and NNTT中劇場 adds one 906-seat schema-v2 configuration.
- Coverage: Tokyo **53**, Kanagawa **3**, Chiba **1**, Saitama **1**, Ibaraki **0**, Tochigi **1**, Gunma **0**; Kyoto **1**, Osaka **2**. Kanto total: **59**.
- 2026-08-11 Tokyo coverage universe audit replaced the old 59-venue/curated-target release direction with a separate canonical audit inventory at `data/venue-coverage/tokyo-coverage-universe-2026-08-11.json`: **76 candidates**, **MUST 44 / SHOULD 28 / OPTIONAL 4**, and current audit states **PRODUCTION 10 / HOLD 60 / CONTRADICTION 2 / 未調査 4**. MUST未調査は0、SHOULD未調査は0、OPTIONAL未調査は4。
- `TOKYO-DOME COMPLEX` closed `tokyo-dome` as MUST/**SOURCE/POLICY/SCHEMA HOLD** before range transcription. The public numbered stand map is explicitly for baseball; concert arena/floor seating is event-dependent and unpublished. Its **historical pre-Meijiza snapshot** was **44/44 dispositions: PRODUCTION 4 / HOLD 38 / CONTRADICTION 2 / 未調査 0**; it is not the current state.
- `RELEASE-COVERAGE SCHEMA AUDIT` separated formal research from user-visible coverage. Research completeness is **72/76 = 94.7%** overall, **44/44 = 100.0% MUST**, and **28/28 = 100.0% SHOULD**. User-visible production coverage is **10/76 = 13.2%** overall, **6/44 = 13.6% MUST**, and **4/28 = 14.3% SHOULD**. Formal disposition 100% is not coverage 100%.
- Confirmed schema-addressable coverage is **10/76 = 13.2%**, all already production. MUST is **6/44 = 13.6%**; SHOULD is **4/28 = 14.3%**. Historical MUST nonproduction-40 taxonomy remains **A12 / B21 / C4 / D3**; the current MUST nonproduction cohort is **38 = A0 / B27 / C8 / D3**.
- Current Tokyo audit state is **PRODUCTION 10 / HOLD 60 / CONTRADICTION 2 / 未調査 4**. Taxonomy-level `A_SCHEMA_UNLOCKABLE` is **0**; B/C taxonomy remains separate from the formal inventory-state labels.
- `TOKYO-CONFIG-A-CURRENT-1` independently reviewed only `tokyo-international-forum-a` and `nntt-opera`. Forum A remains a **POLICY HOLD** and NNTT Opera a **SOURCE HOLD**; fresh issuer evidence proves that schema alone cannot resolve either, so both moved from `A_SCHEMA_UNLOCKABLE` to `B_SOURCE_LIMITED`. No gate was lowered and no production artifact changed. See `data/venue-reports/tokyo-config-a-current-1-2026-08-12.json`.
- `TOKYO-CONFIG-A-ARENA-1` independently re-reviewed only `yoyogi-1`, `yoyogi-2`, and `tokyo-gymnasium` with Sol high. Each issuer independently defines a fixed-seat subset and separates dynamic arena/floor scope, but all three fail exact reconciliation: 第一体育館 **8,636 expected / 8,655 calculated / rangeDiff 0 (+19)**、第二体育館 **2,803 / 2,818 / 0 (+15)** after excluding lettered movable rows、東京体育館 **5,178 expected / full calculated null / rangeDiff 0 at the first conflicting block**, where 2F north C is **316 printed / 317 numbered (+1)** in both the current north PDF and independent current accessibility block guide. All moved A→`C_CONTRADICTION`; production/selectable increase is 0. See `data/venue-reports/tokyo-config-a-arena-1-2026-08-12.json`.
- `TOKYO-CONFIG-A-ARENA-2` independently re-reviewed only `ariake-arena`, `kanadevia-hall`, and `sgc-hall-ariake` with Sol high. All moved A→`B_SOURCE_LIMITED`; no gate was lowered. 有明はfixed subtotal **11,232**を公式確認したが全固定番号とwheelchair／随行者／amenity番号対応が未公開。Kanadeviaは3固定バルコニー **1,365**の公式番号図があるが、issuer-defined balcony-only configuration、車いす／同伴、認証後Seat Plan generationが不足。SGCは1F 1,720＋上層fixed scope 2,047＝3,767を確認したが、上層全番号、固定番号席だけのissuer condition、車いす4＋同伴4の恒久番号対応がない。expected/calculated/rangeDiffは順に **11,232/null/null、1,365/null/null、null/null/null**、rangesは全件0、production/selectable増加0。See `data/venue-reports/tokyo-config-a-arena-2-2026-08-12.json`.
- `TOKYO-CONFIG-A-THEATRE-1` independently re-reviewed only `ex-theater-ariake`、`nntt-playhouse`、`galaxy-theatre` with Sol high. EX moved A→`B_SOURCE_LIMITED`: 最大1,546配置のwheelchair／可動前方席／stage variant包含が未公表。NNTT中劇場は公式プロセニアム形式③ A・B号迫り使用 **906/906/rangeDiff 0**をschema v2 production/selectable化し、ABC配置 **788/791/0**は非selectable contradictionへ分離。銀河劇場はA→`C_CONTRADICTION`、通常図 **746/750/0**。productionは1 venue / 1 configuration / 906 records増え、current A cohortは0。See `data/venue-reports/tokyo-config-a-theatre-1-2026-08-12.json`.
- `SOL-SHOULD-1` independently researched only `ex-theater-roppongi`、`zepp-haneda`、`zepp-shinjuku` with Sol high and fresh issuer/operator-owned evidence. All three are `B_SOURCE_LIMITED`: EXは公式着席920を確認したがmap generationとwheelchair包含・置換番号・同伴mappingが未解決、Zepp Hanedaは基本着席 **1,207/1,207/rangeDiff 0**、Zepp ShinjukuはB4F番号席subset **530/530/0**まで2パス一致したが、両館ともwheelchair／同伴の恒久置換番号が催事依存で非公開。production/selectable増加は0。See `data/venue-reports/tokyo-sol-should-1-2026-08-13.json`.
- `SOL-SHOULD-2` independently researched only `toyosu-pit`、`otemachi-mitsui-hall`、`mon-takanawa-box1000` with Sol high and fresh current issuer/operator evidence. All three are `B_SOURCE_LIMITED`: 豊洲PITは公式着席 **1,328/null/null** で完全floor番号図なし、大手町三井ホールは560席配置 **560/560/0** とflat 572席配置 **572/572/0**、MoN BOX1000は公式着席例 **1,048/1,048/0** を全番号2パス一致したが、後二館を含む全件でwheelchair／companionの包含・置換番号が未解決。production/selectable増加0。See `data/venue-reports/tokyo-sol-should-2-2026-08-13.json`.
- `SOL-SHOULD-3` independently researched only `hulic-hall-tokyo`、`forum-b7` with Sol high and fresh current issuer/operator evidence. Both are `B_SOURCE_LIMITED`: ヒューリックは公式固定 **886/886/rangeDiff 0** と車いす転換撤去番号J2・3・32・33（最大2台）まで全番号2パス一致したがcompanion配置が未解決。B7は可変平土間のシアター1,200人例を公式確認したが、目安人数の無番号仮設配置で完全番号席setとwheelchair／companion semanticsがない。production/selectable増加0、SHOULD researchは **28/28** で完了。See `data/venue-reports/tokyo-sol-should-3-2026-08-13.json`.
- `TOKYO-ADDRESSABLE-CONVERSION-1A` re-reviewed only `jcom-hall-hachioji` and `musashino-civic-main` from fresh issuer/operator-owned evidence. Both are **B_SOURCE_LIMITED** and are no longer addressable; neither is production/selectable. J:COM is **1,869/null/null** because its official wheelchair-on/off totals and pit/removable-seat marks lack complete numbered wheelchair/companion/removal mappings. 武蔵野 is **1,252/null/null** because its separate current technical plan marks wheelchair/removable/backrest semantics without seat-number correspondence, so independent full-ID range diff cannot be established. No seat numbers, exclusions, or configurations were invented. See `data/venue-reports/tokyo-addressable-conversion-1a-2026-08-13.json`.
- `TOKYO-ADDRESSABLE-CONVERSION-1B` re-reviewed only `national-noh-theatre` and `nissho-hall` with Sol high and fresh issuer/operator-owned evidence. Both are **B_SOURCE_LIMITED** and no longer addressable. 国立能楽堂 is **627/null/null**: the complete numbered map and area totals exist, but wheelchair inclusion/replacement/companion semantics, the relation to six movable chushomen seats, and an independent complete source generation are missing. ニッショーホール full diagram is **999/999/rangeDiff 0** across the current seat map and audio seating plan, with 1F row 8 seat 2 always removed for one wheelchair space; however, row 8 seats 3-4/5-6 removal variants lack issuer-defined conditions/exact totals and wheelchair companion placement. No repository-defined standard or derived 997/995-seat configuration was created. Production/selectable increase is 0. See `data/venue-reports/tokyo-addressable-conversion-1b-2026-08-13.json`.
- `SOL-SHOULD-1` validation passed: JSON parsing、`venues:inventory:report`、`venues:readiness:report`、`venues:release:coverage`、`venues:build/check/validate/report`、lint、typecheck、unit tests（**17 files / 139 tests**）、production build、E2E（**13/13**）、production generated-artifact regression、`git diff --check`。sandbox-only E2Eは既知のWrangler registry `EPERM`でtimeoutし、approved elevated rerunが通過。production追加がないためcommit/push、Workers Build、公開URL確認は実施しない。
- ARENA-2 validation passed: all three `venues:review` runs, JSON parsing, `venues:inventory:report`, `venues:readiness:report`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, lint, typecheck, unit tests (**17 files / 139 tests**), production build, E2E (**12/12**, including both 明治座 configurations), production generated-artifact regression, and `git diff --check`. The sandbox-only E2E start hit the known Wrangler registry `EPERM`; the approved elevated rerun passed. Production artifacts and both 明治座 runtime configurations are unchanged. No Workers Build/public-URL verification was required because production/selectable additions are zero.
- `TOKYO-CONFIG-PILOT-1` converted only `meijiza-standard` to schema v2. `花道あり` is 1,368/1,368 seats and `花道なし` is 1,448/1,448 seats; both independent range passes have diff 0, verified status, and no unresolved issues. The exact 80-seat difference is `1階1～20列7～10番`. Official 2F wheelchair spaces are common unnumbered corners outside the complete 390 numbered-seat set, so they are recorded as a non-selectable official scope and no third configuration is invented. Pilot platform/data result: **PASS/PASS**.
- Pilot validation passed: configuration-level `venues:review`, inventory/readiness/release coverage reports, `venues:build/check/validate/report`, JSON parsing, lint, typecheck, unit tests (**17 files / 139 tests**), production build, E2E (**12/12**, including both 明治座 configurations and console error 0), and `git diff --check`. Existing60 regression is **PASS**: all prior sources and runtime files are byte-for-byte untouched; the legacy catalog projection and all 60 prior fingerprint entries are identical; IDs, dataPath, aliases, search order, and lottery behavior are unchanged. Only the 明治座 v2 source, one additive catalog group, two runtime files, and two configuration fingerprints were added.
- `release-seed-v1` requires 100% MUST/SHOULD research completeness and conversion of every confirmed addressable candidate. Repo-derived raw production floor is MUST **6/44**、SHOULD **4/28**、MUST+SHOULD **10/72**、Tokyo **10/76**. Addressable conversion is MUST **6/6**、SHOULD **4/4**、total **10/10 = 100%**. The adequacy review is complete with **ADEQUACY FAIL**; conversion 100% did not produce adequate major-Tokyo utility.
- `TOKYO RELEASE COVERAGE ADEQUACY REVIEW` found canonical production dome 0 / arena 0 / hall 5 / theater 5 and, using review-only scale bands, large 0 / medium 5 / small 5. All Tokyo production 53 is large 0 / medium 12 / small 41. Concert-hall and commercial-theatre utility is partial, but large live, medium live-house, and major Ariake/Odaiba/Toyosu bayfront coverage are absent. Critical gap IDs are `tokyo-dome`, `nippon-budokan`, `ariake-arena`, `zepp-haneda`, and `tokyo-takarazuka`.
- `TOKYO-CONFIG-SCHEMA-1` implemented additive schema v2 `venue -> configurations[]` support without adding real v2 venue data. Schema v1 sources normalize internally to one configuration while all existing v1 source/catalog/runtime/fingerprint bytes remain unchanged. Schema v2 separates venue research status from configuration `status/selectable`, validates issuer condition/source generation/scope/wheelchair semantics/source references/count/rangeDiff per configuration, groups catalog entries by `venueGroupId`, keys runtime details by `(venueId, configurationId)`, and supports explicit multi-configuration selection plus fixed-only disclosure at selection and result.
- `venues:release:coverage` derives readiness from current coverage candidates, inventory/source/catalog linkage, schema-addressable cohorts, repo-derived raw floors, platform capabilities, source/inventory production validation, universe-gap review, and the required adequacy review. It emits nonproduction schema-addressable IDs, now **none**. Current deterministic result is **Coverage gate FAIL / RELEASE READY NO** because the completed adequacy review has four utility blocker IDs; legacy regional shortfalls remain informational only.
- `TOKYO-CONFIG-SCHEMA-1` local validation passed: schema v1/v2 positive fixtures, multi-configuration and fixed-only fixtures, required negative fixtures, `venues:build/check/validate/report`, inventory/readiness/release coverage reports, lint, typecheck, unit tests (**17 files / 137 tests**), build, E2E (**11 tests**, approved elevated Miniflare run after the known sandbox registry `EPERM`), and `git diff --check`. Existing60 regression is byte-for-byte PASS: no diff under `data/venue-sources`, generated catalog, all runtime details, or production fingerprints; catalog/fingerprint hashes remain `6b2bc4fba9afaa71440e0aa207de6dc9182318ae` / `882ba86a5bc85185405e5ce216640e25a1aff39c`.
- `LUNA-STD-1` completed for exactly `theater-h`, `pia-theater-yaesu`, and `galaxy-theatre`. Production promotion: **0**; formal holds: **3**. `LUNA-STD-2` then completed exactly `theater-g-rosso`, `imm-theater`, and `nissho-hall`: G-ROSSO **PRODUCTION 765 seats**、IMM **SOURCE/SCHEMA HOLD**、ニッショー **SOURCE/POLICY/SCHEMA HOLD**. Production is **60 venues / 81,515 seats**.
- `TERRA-DENSE-1` completed for exactly `tokyo-takarazuka`, `shiki-haru`, and `shiki-aki`: all three are **SOURCE/POLICY/SCHEMA HOLD** before range transcription. 東京宝塚劇場の現行一般公演2,079席は公式確認済みだが車いす観劇スペースの包含・置換番号が未公表。四季［春］は約1,500席、［秋］は約1,200席と現行作品別非販売図・車いす個別予約を公式確認したが、固定されたselectable numbered-seat set・厳密総数・車いす置換番号が定義されない。第1パスと独立第2パスは代表pattern gateまで完了し、rangeはゼロ、range diffは`null`。production昇格は0、baseline 60 production artifactsは不変。
- `TERRA-DENSE-2` completed for exactly `yoyogi-1`, `yoyogi-2`, and `ariake-arena`: all three are **SOURCE/POLICY/SCHEMA HOLD** before range transcription. 第一体育館は固定スタンド8,636席、車いす104席、VIP34席（最大70席）、イベントごとに変わるアリーナ床最大4,124席を別建てで示す。第二体育館は固定2,803席、車いす8席、可動314席、アリーナ最大1,191席を示し用途により内訳が変わる。有明アリーナは約15,000席を仮設席込みの最大とし、1階イベント床と2階可動観覧席full/部分展開を別variantとして示す。いずれもissuer-defined representative selectable numbered-seat setがなく、固定席だけの独自選択、capacity fitting、イベント床席の恒久番号化を行わなかった。第1パスと独立第2パスは代表pattern gateまで完了、rangesはゼロ、expected/calculated/range diffは`null`、production昇格は0、baseline 60 production artifactsは不変。
- `TERRA-DENSE-3` completed for exactly `tokyo-gymnasium`, `musashino-forest-sport-plaza`, and `tokyo-garden-theater`: all three are **SOURCE/POLICY/SCHEMA HOLD** before range transcription. 東京体育館は最大9,308席、固定5,178席、車椅子130席と北側/南側の別座席案内を示す。武蔵野の森は固定約6,000席、ロールバックチェア、仮設席で10,000人超、競技/コンサート/展示会対応を示す。東京ガーデンシアターは最大約8,000人、全席可搬、アリーナA/B、着席/スタンディング、舞台・車いすエリアの公演別変更を明示する。いずれもissuer-defined representative selectable numbered-seat setがなく、固定席だけの独自選択、capacity fitting、イベント床席の恒久番号化を行わなかった。第1パスと独立第2パスは代表pattern gateまで完了、rangesはゼロ、expected/calculated/range diffは`null`、production昇格は0、baseline 60 production artifactsは不変。
- `TERRA-DENSE-4` completed for exactly `shiki-umi`, `ariake-shiki`, and `nihon-seinenkan`: 四季［海］と有明四季劇場は **SOURCE/POLICY/SCHEMA HOLD**。現行資料は各作品の販売図（非販売席・ファミリーゾーン）と個別予約の車いすスペース、約1,200席だけを示し、恒久physical番号席set・厳密総数・車いす置換番号を定義しない。日本青年館ホールは **SCHEMA HOLD**。現行運営者資料は総数1,249、固定番号席1,245、別枠車いす4、1階811・2階434を一致して示すが、分割番号blockのcanonical row/area ownershipを図形位置の補完なしに定義しない。全3件でrangesはゼロ、expected/calculated/range diffは`null`、production昇格は0、baseline 60 production artifactsは不変。Terra active MUST queueは**0**、新規Sol handoffはない。
- `SOL-CPLX-1` completed for exactly `kanadevia-hall`, `sgc-hall-ariake`, and `ex-theater-ariake`: all three are **SOURCE/POLICY/SCHEMA HOLD** before range transcription. Kanadeviaは固定バルコニー1,365席、アリーナ着席990席、立見106人で最大2,471だが、arena恒久番号・車いす/同伴・standard configurationが未定義。SGCは2026-03-27開業後の基本図と最大3,767を確認したが、1F dynamic floorと車いすlayoutが公演依存。EXは2026-04-25開場後の最大1,546番号図を確認したが、中通路前全席撤去、pit/張出し/花道、車いすspaceとstandard/normal configurationが公演依存。3件とも第1/独立第2パスは代表pattern gateまで完了、rangesは0、expected/calculated/range diffは`null`、production昇格は0。
- `TOKYO-DOME COMPLEX` completed for `tokyo-dome` only: **SOURCE/POLICY/SCHEMA HOLD**. Current operator material separates general capacity 55,000 from baseball approximately 43,500; calls the published numbered stand map a baseball-event map; and states concert/event arena seating differs by event and is not published. The baseball wheelchair provision is 30 positions with up to two companions and no position selection, but is explicitly for Giants games. Current concert issuer material independently shows equipment-seat release after lighting layout confirmation plus annotated and stage-side seat sales. No issuer-defined standard concert configuration, permanent floor IDs, stage-specific stand exclusion numbers, concert wheelchair mapping, or suite/premium/special-area inclusion exists. Both passes stopped at the representative-pattern gate; ranges are 0 and expected/calculated/range diff are `null`; production promotion is 0.
- The 2026-08-09 Kanto P1 Sol macro closed `takasaki-city-theatre-main-release-seed` as **CONTRADICTION** and preserved all 56 baseline production fingerprints and catalog entries unchanged.
- The 2026-08-10 bounded Terra escalation left `nissay-theatre` on **SOURCE/SCHEMA HOLD** and handed `line-cube-shibuya` to Sol. The bounded Sol escalation then found current issuer-owned operational evidence identifying all 26 wheelchair-conversion seats and promoted `line-cube-shibuya-standard` as **PRODUCTION**, 1,952 numbered seats. Production is now **57 venues / 76,911 seats**.
- The 2026-08-10 Sol Tokyo COMPLEX macro 1 promoted `tokyo-wave3-0130` (`suntory-hall-main`) as **PRODUCTION**, 2,006 numbered seats. The prior 57 production fingerprints and catalog entries are unchanged; production is now **58 venues / 78,917 seats**.
- **Historical, superseded by the 2026-08-12 pilot:** the 2026-08-10 Sol Tokyo COMPLEX macro 2 closed `meijiza` as **POLICY/SCHEMA HOLD** before range transcription. The later `TOKYO-CONFIG-PILOT-1` conversion is the current disposition; do not treat this historical closure as a current HOLD.
- The 2026-08-10 Sol Tokyo COMPLEX macro 3 closed `kabukiza` as **SCHEMA HOLD** before range transcription. Production remains **58 venues / 78,917 seats**, and all baseline production fingerprints, catalog entries, and runtime details are unchanged.
- The 2026-08-10 Sol Tokyo COMPLEX macro 4 closed `setagaya-public-theatre` as **POLICY/SCHEMA/SOURCE HOLD** before range transcription. Tokyo COMPLEX is complete, bounded Tokyo top-up processing is complete, and production remains **58 venues / 78,917 seats** with every baseline production artifact unchanged.
- The 2026-08-10 Luna high P2 STANDARD macro 1 closed `sapporo-kitara-small-release-seed` as **LUNA BOUNDARY / SCHEMA HOLD** and `rohm-theatre-kyoto-main-release-seed` as **TERRA HANDOFF / SCHEMA-POLICY HOLD** after currentness review. Both remain draft with empty ranges and `expectedSeatCount`/`calculatedSeatCount` null; no first pass, independent second pass, or production promotion was authorized. Production remains **58 venues / 78,917 seats**, with zero baseline production artifact regression.
- The 2026-08-10 Terra high P2 DENSE + escalation macro reviewed exactly `rohm-theatre-kyoto-main-release-seed`, `sapporo-kitara-main-release-seed`, and `kyoto-concert-hall-main-release-seed`. All three are now **SOL HANDOFF** before range entry: ROHM has no issuer-owned numbered wheelchair/pit conversion key for normal 2,005 (including 10 wheelchair seats) versus pit 1,833; Kitara separately states 12 wheelchair spaces beside its 2,008 floor total without an inclusion/replacement rule; Kyoto Concert Hall's January 2026 map separately marks wheelchair space without a relationship to its 1F 980 / total 1,833 counts. All remain draft with empty ranges and null expected/calculated counts. No first/second range pass or production promotion was authorized; production remains **58 venues / 78,917 seats**, with zero baseline artifact regression.
- The 2026-08-11 Sol high P2 evidence-resolution macro closed those three handoffs. `kyoto-concert-hall-main-release-seed` is **PRODUCTION** at 1,833 numbered seats after two official-map passes with zero diff; operator and Kyoto City material independently establish six additional unnumbered wheelchair spaces and 1,839-person capacity. `rohm-theatre-kyoto-main-release-seed` is **SOURCE CONTRADICTION HOLD** because the complete current official-map transcription contains 2,013 printed IDs against the official 2,005 total, despite resolving the 172 pit removals and all ten wheelchair IDs. `sapporo-kitara-main-release-seed` is **SOURCE/SCHEMA HOLD**: issuer sources resolve 2,008 fixed seats plus 12 additional wheelchair-only spaces, but independent dense-map passes did not converge on canonical block-boundary ownership. Production increased by one venue and 1,833 seats to **59 venues / 80,750 seats**; all 58 baseline production artifacts are byte-for-byte unchanged.
- The 2026-08-11 Sol high P2 COMPLEX macro 1 closed `hbg-hall-release-seed` as **SOURCE/SCHEMA HOLD** before range transcription. Current operator material and a Hiroshima City survey confirm the unique normal total of 2,001 seats; the operator map identifies all 20 wheelchair-conversion numbers as 1F row 13 seats 12–17, 18–24, and 25–31. The same official material states 52 seats are inside the orchestra pit and that pit use makes rows 1–4, 82 seats total, unavailable, but publishes no numbered allocation of the 52-seat subset versus the additional 30 seats. Production remains **59 venues / 80,750 seats**, with zero changes to all baseline production artifacts.
- The 2026-08-11 Sol high P2 COMPLEX macro 2 closed `fukuoka-sunpalace-hall-release-seed` as **SOURCE/SCHEMA HOLD** before range transcription. Current operator material establishes the nominal normal layout as 2,316 seats (1F 1,468 including 1,344 fixed and 124 pit; 2F 360; 3F 488), and the official pit close-up directly maps all 124 pit seats to rows 1–4. The operator separately states six rear first-floor wheelchair positions, three on each side, but does not state whether they are included in 2,316 or identify numbered seats removed or replaced for their use. Production remains **59 venues / 80,750 seats**, with zero changes to all baseline production artifacts.
- The 2026-08-11 Sol high P2 COMPLEX macro 3 closed `festival-hall-release-seed` as **SOURCE/SCHEMA HOLD** before range transcription. Current operator material establishes the normal 2,700-seat layout and the 2,524-seat pit-use total, while the current 1/100 stage-plan bundle states that pit and forestage use reduces the audience by 176 seats. No issuer-owned source maps those 176 seats to numbers. The operator confirms a limited-capacity unnumbered wheelchair space on 1F but publishes neither its capacity nor its inclusion in 2,700 or numbered replacements. Production remains **59 venues / 80,750 seats**, with zero changes to all baseline production artifacts.
- The 2026-08-11 Sol high P2 COMPLEX macro 4 closed `aichi-arts-center-main-release-seed` as **SOURCE CONTRADICTION HOLD** before range transcription. Current official material resolves the normal full layout as 2,480 positions: 2,456 fixed numbered seats (including 12 score seats), eight additional unnumbered wheelchair positions, and 16 unnumbered parent-child-room seats. The current map and 2026 guide call pit rows 1–5 a 176-seat reduction, but those rows print 177 IDs; the issuer does not identify the one-seat difference. No first pass, independent second pass, or production gate was started. Production remains **59 venues / 80,750 seats**, with zero changes to all baseline production artifacts.
- P2 processing is complete at **24/24 dispositions**: one production promotion / 1,833 added seats (`kyoto-concert-hall-main-release-seed`) and 23 non-production closures. The 23 split analytically into 19 closures that require new or corrected issuer-owned evidence and four with possible future schema/policy redesign value; they are not active model queues.

## Release goal

Release ready is **NO under the deterministic Tokyo coverage contract**, not because of a production-data defect. The verified application is **62 venues / 63 selectable configurations / 85,237 configuration-seat records**. Tokyo is **10/76 user-visible PRODUCTION**. A conversion is complete at A0, SHOULD research is 28/28, nonproduction addressable is 0, raw floor and conversion are 100%, but the completed adequacy review is **ADEQUACY FAIL** because there is no large/dome/arena production choice, no medium live-house production, a major bayfront gap, and low coverage of high-demand MUST venues. B/C/D exclusions remain visible in the raw denominator.

- `RELEASE-COVERAGE SCHEMA AUDIT` changed only coverage governance, report/docs, and the release coverage reporter. It did not change venue sources, catalog, runtime details, or production fingerprints.
- Validation passed: coverage JSON/classification consistency (40/40 unique MUST nonproduction assignments), `venues:inventory:report`, `venues:readiness:report`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, lint, typecheck, unit tests (16 files / 116 tests), build, E2E (11 tests in the approved elevated Miniflare run), production artifact baseline regression, and `git diff --check`. The sandbox-only E2E attempt hit the known Wrangler registry `EPERM`; the elevated rerun passed.

## 2026-08-11 Tokyo coverage `LUNA-STD-1`

- `theater-h`: **SOURCE/POLICY HOLD**. Current official overview confirms 2024 opening, 747 maximum capacity (1F 581 plus six wheelchair spaces or auxiliary seats; 2F 158), and the official seat map shows numbered seats plus wheelchair icons. The independent second pass confirmed that the issuer does not publish the specific replacement/exclusion numbers for the auxiliary-seat or wheelchair setting. Expected/calculated remain `null`/`null`; ranges remain empty.
- `pia-theater-yaesu`: **SOURCE/SCHEMA HOLD**. Current owner/operator pages confirm the 2026-05 pre-opening, fixed stepped theatre, 1F 593 + 2F 213 = 806 seats, and theatre/musical/live use. The independent second pass confirmed that the public organizer page says detailed seat/stage drawings are available by inquiry; the public event page does not provide a complete canonical row/number map. Expected/calculated remain `null`/`null`; ranges remain empty.
- **Historical, superseded by `TOKYO-CONFIG-A-THEATRE-1`:** `galaxy-theatre` was closed as **POLICY/SCHEMA HOLD** in `LUNA-STD-1`. The fresh issuer-map review is the current disposition: **C_CONTRADICTION**, expected/calculated `746/750`, `rangeDiff: 0`.
- The canonical coverage inventory now records the three as MUST/HOLD with inventory/source links: `tokyo-coverage-theater-h`, `tokyo-coverage-pia-theater-yaesu`, and `tokyo-official-0455`. No production promotion occurred, so production remains **59 / 80,750** and release readiness remains **NO**.
- `LUNA-STD-1` validation passed: JSON parsing, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, inventory/readiness/release coverage reports, lint, typecheck, unit tests (16 files / 116 tests), build, E2E (11 tests passed in the approved elevated Miniflare run), baseline regression, and `git diff --check`. The first sandbox E2E attempt timed out on the known Wrangler registry EPERM; the elevated rerun passed. No deploy was made because production runtime did not change.

## 2026-08-11 Tokyo coverage `LUNA-STD-2`

- `theater-g-rosso`: **PRODUCTION**, 765 numbered seats. Current Tokyo Dome City official material confirms active use, 26 rows A–Z, 765 seats, and three separate wheelchair spaces. First pass transcribed the official numbered map; independent second pass used the operator equipment page and English official seat guide, with zero range diff and expected/calculated `765`/`765`. The source is `data/venue-sources/theater-g-rosso-standard.json`.
- `imm-theater`: **SOURCE/SCHEMA HOLD**. Current operator material confirms 705 seats (general 703, wheelchair 2), but the official seat page says the basic layout varies by performance and wheelchair details are handled per event. First and independent second pass agree that no unique fixed selectable numbered-seat set is issuer-defined; expected/calculated remain `null`/`null`.
- `nissho-hall`: **SOURCE/POLICY/SCHEMA HOLD**. Current operator material confirms 1,000 seats (1F 671 + wheelchair 1, 2F 328) and a current official map. The map explicitly states 1F row 8 seat 2 is always removed and seats 3–4 / 5–6 can be removed in pairs. First and independent second pass agree that no unique normal sales variant is issuer-defined; expected/calculated remain `null`/`null`.
- Batch consistency is recorded in `data/venue-batches/tokyo-luna-std-2.json` for exactly these three inventory IDs; no other Tokyo candidate, existing HOLD/CONTRADICTION, or LUNA-STD-1 target was added.
- Tokyo coverage after the macro: **PRODUCTION 8 / HOLD 38 / CONTRADICTION 2 / 未調査 28**; MUST未調査 **16**; SHOULD未調査 **8**. No existing HOLD/CONTRADICTION or LUNA-STD-1 target was re-investigated.
- `LUNA-STD-2` validation passed: JSON parsing, `venues:build` (60 production venues), `venues:check`, `venues:validate`, `venues:report`, inventory/readiness/release coverage reports, lint, typecheck, unit tests (16 files / 116 tests), build, E2E (11 tests in the approved elevated Miniflare run), baseline regression, and `git diff --check`. Data commit `65e8bde` completed the [Workers Build](https://github.com/tomo527/seat-lottery-simulator/runs/93794395953); the [public URL](https://seat-lottery-simulator.studiotomo.workers.dev/) returned HTTP 200 and its venue selector displayed `シアターGロッソ 通常番号席配置` with `抽選対象 765席`. The final HANDOFF-only follow-up preserves this runtime state.

## 2026-08-12 Tokyo coverage `SOL-CPLX-1`

- `kanadevia-hall`: **SOURCE/POLICY/SCHEMA HOLD**. Current Tokyo Dome City sources establish fixed balconies 447 + 478 + 440 = 1,365 seats, event-dependent seated arena 990, and 106 standing places inside the 2,471 maximum. Wheelchair and suite seats are outside that total. The public arena guide has no permanent seat IDs, detailed seat drawings are authentication-gated, and no issuer-defined standard/normal configuration exists.
- `sgc-hall-ariake`: **SOURCE/POLICY/SCHEMA HOLD**. Current post-opening TV Asahi sources establish the 3,767 maximum as 1F 1,720; 2F 595 + wheelchair 4 + companions 4 + balcony 16; 3F 725 + VIP 8; 4F 687 + VIP 8. The published map is only a basic pattern; 1F uses a dynamic seated/standing floor and the FAQ makes wheelchair layout performance-dependent, so event-floor IDs were not made permanent and fixed upper levels were not selected alone.
- `ex-theater-ariake`: **SOURCE/POLICY/SCHEMA HOLD**. Current post-opening TV Asahi sources publish a detailed maximum 1,546-seat map, but state that all seats before the mid-aisle are removable and that orchestra pit, thrust, hanamichi, wheelchair space, and stage configuration vary by production. The issuer does not define the maximum map as standard/normal or publish the variant number differences. EX THEATER ROPPONGI data was not reused.
- For all three, first and independent second passes stopped at the representative-pattern gate. `expectedSeatCount` and calculated count remain `null`; ranges remain empty; range diff is `null`; no production promotion occurred. The macro report is `data/venue-reports/tokyo-sol-cplx-1-2026-08-12.json`, and the batch is `data/venue-batches/tokyo-sol-cplx-1.json`.
- Tokyo coverage after the macro: **PRODUCTION 8 / HOLD 53 / CONTRADICTION 2 / 未調査 13**; MUST未調査 **1**; SHOULD未調査 **8**; OPTIONAL未調査 **4**. Production remains **60 venues / 81,515 seats**.
- At SOL-CPLX-1 close, `docs/reports/TOKYO_COVERAGE_UNIVERSE_AUDIT_2026-08-11.md` separated historical snapshots from canonical values and named `tokyo-dome` as the then-next bounded macro. TOKYO-DOME COMPLEX has now superseded that historical queue state.

## 2026-08-12 Tokyo coverage `TOKYO-DOME COMPLEX`

- `tokyo-dome`: **SOURCE/POLICY/SCHEMA HOLD**. General capacity 55,000 is not an exact numbered-seat count and is separately stated from baseball capacity approximately 43,500. The live seat page links a baseball-diamond numbered map and a separate 2026 Giants-game category map; the operator FAQ explicitly limits the stand guidance to baseball and says concert arena seats vary by event and are not published.
- Physical/configuration gate: permanent stands exist, but the issuer does not publish the exact subset that remains selectable for every concert stage. Arena/floor seats, stage position, stand closures, movable/temporary/supplementary seats, equipment releases, annotated/obstructed-view sales, concert wheelchair/companion positions, and suite/premium/special-area inclusion vary or lack a permanent concert number key. The baseball-only 30-wheelchair-seat procedure was not transferred to concerts.
- First and independent second passes stopped at the representative-pattern gate. `expectedSeatCount` and calculated count remain `null`; ranges remain empty; range diff is `null`; no production promotion occurred. The report is `data/venue-reports/tokyo-dome-complex-2026-08-12.json`, the source is `data/venue-sources/tokyo-dome-concert.json`, and the batch is `data/venue-batches/tokyo-dome-complex.json`.
- MUST phase final: **44 total = PRODUCTION 4 / HOLD 38 / CONTRADICTION 2 / 未調査 0**. The four production MUSTs are `line-cube-shibuya`, `suntory-hall-main`, `theater-milano-za`, and `sunshine-theatre`; all other MUSTs have formal official-source dispositions, not unresolved research status. The audited universe has no obvious unregistered MUST-class gap.
- Tokyo coverage after the macro: **PRODUCTION 8 / HOLD 54 / CONTRADICTION 2 / 未調査 12**; MUST未調査 **0**; SHOULD未調査 **8**; OPTIONAL未調査 **4**. Production remains **60 venues / 81,515 seats**.

## Kanto macro classification

- **PRODUCTION**: `yokohama-minatomirai-hall-main-release-seed`, `sagamihara-green-hall-main-release-seed`, `culttz-kawasaki-hall-release-seed`, `ichikawa-culture-hall-main-release-seed`, `saitama-arts-theatre-main-release-seed`, `utsunomiya-city-culture-hall-release-seed`.
- **LUNA**: none remaining in Kanto.
- **TERRA**: none remaining in Kanto.
- **SOL**: none remaining in Kanto.
- **HOLD/MISMATCH**: `kanagawa-kenmin-hall-main-release-seed` (SOURCE HOLD), `muza-kawasaki-symphony-hall-release-seed` (SOURCE HOLD), `yokosuka-arts-theatre-release-seed` (POLICY HOLD), `mori-no-hall21-main-release-seed` (SOURCE HOLD), `narashino-culture-hall-release-seed`, `funabashi-civic-culture-hall-release-seed`, `chiba-city-culture-center-hall-release-seed`, `urayasu-culture-hall-release-seed`, `saitama-kaikan-main-release-seed` (CONTRADICTION), `omiya-sonic-city-large-release-seed` (INDEPENDENT REVIEW MISMATCH), `kawaguchi-lilia-main-release-seed`, `tokorozawa-muse-ark-release-seed` (POLICY HOLD), `westa-kawagoe-large-release-seed` (CONTRADICTION), `mito-arts-tower-concert-release-seed`, `ibaraki-kenritsu-bunka-center-large-release-seed`, `tochigi-ken-sogo-bunka-center-main-release-seed`, `gunma-music-center-release-seed`, `takasaki-city-theatre-main-release-seed` (CONTRADICTION).

## 2026-08-09 Kanto P1 Sol macro outcome

- `takasaki-city-theatre-main-release-seed`: **CONTRADICTION**. Expected numbered seats are 2,022 (official 2,027 positions less five nonnumbered wheelchair positions); two complete independent passes both read 2,054 printed numbered seats, with 1F 1,468, 2F 480, and side areas 106. `rangeDiff: 0`, but the same official map states 2F 448, leaving an unexplained 32-seat difference. No ranges, generated detail, catalog entry, or fingerprint were created.
- Production promotions: **0**; production remains **56 venues / 74,959 seats**.

## 2026-08-09 Kanto P0 Sol macro outcomes

- `yokosuka-arts-theatre-release-seed`: **POLICY HOLD**. The M/N local diff is resolved as M 2-31 and N 2-37, but the fixed-seat subtotal and four separately drawn wheelchair positions do not define one numbered selectable pattern.
- `mori-no-hall21-main-release-seed`: **SOURCE HOLD**. The registered official PDF is a third-floor restricted-view photo guide, not a complete numbered-seat map.
- `saitama-arts-theatre-main-release-seed`: **PRODUCTION**, 776 numbered seats; two independent official-PDF passes, zero range diff.
- `saitama-kaikan-main-release-seed`: **CONTRADICTION**. Two passes read 1F 1,071 printed numbers versus the official 1F 1,049 count; the 22-seat difference is undefined.
- `omiya-sonic-city-large-release-seed`: **INDEPENDENT REVIEW MISMATCH**. Two passes read 1,681 printed numbers versus an official count implying 1,677 numbered seats; the four-seat difference is undefined.
- `westa-kawagoe-large-release-seed`: **CONTRADICTION**. Two passes read 3F 594 printed numbers versus the official 3F 576 count; the 18-seat difference is undefined.

## 2026-08-09 Sol macro outcomes

- `sagamihara-green-hall-main-release-seed`: **PRODUCTION**, 1,778 numbered seats; two independent passes, zero range diff.
- `kanagawa-kenmin-hall-main-release-seed`: **SOURCE HOLD**; official capacity/conversion evidence is explicit, but the only public numbered diagram is a low-resolution viewpoint GIF with markers obscuring numbers.
- `yokosuka-arts-theatre-release-seed`: **INDEPENDENT REVIEW MISMATCH**; representative fixed layout is clear, but the independent flat-floor readings differ by four seats around rows M-N beside wheelchair spaces. No guessed ranges were saved.
- `culttz-kawasaki-hall-release-seed`: **PRODUCTION**, 1,995 numbered seats; two independent passes, zero range diff.
- `ichikawa-culture-hall-main-release-seed`: **PRODUCTION**, 1,758 numbered seats; two independent passes, zero range diff.

## 2026-08-09 Terra macro outcomes

- `muza-kawasaki-symphony-hall-release-seed`: **SOURCE HOLD**. The official 2023 map establishes 1,987 numbered general seats plus 10 separately identified wheelchair positions (1,997 total), but neither the map nor the current official seat page publishes a lossless row/number inventory. Do not synthesize row ranges from block totals or invent wheelchair seat numbers.
- `tokorozawa-muse-ark-release-seed`: **POLICY HOLD**. The official map/page agree on maximum 2,002 seats (1F 1,106; 2F 560; 3F 336); the official page also says 13 movable 1F seats can convert to six wheelchair positions. It does not define one fixed representative selectable configuration.

## Tokyo top-up

### 2026-08-10 Sol Tokyo COMPLEX macro 4

- `setagaya-public-theatre`: **POLICY/SCHEMA/SOURCE HOLD**. The current official overview calls 612 seats the basic shape but publishes two operational forms in parallel: proscenium 540–600 seats and open 513–540 seats. The forestage and flat-floor seating may be removed for stage/orchestra-pit use, three actor entrances require partial seat removal, and the official wheelchair notation is four positions/12 seats without individual replacement numbers.
- Sales/currentness resolution: an official performance page separately adds annotated seats, auxiliary chairs, and first-/third-floor standing positions when available, so sale inventory does not establish a normal fixed set. The main theatre is closed from 2026-04-01 through 2027-03-31 (planned) for ceiling and stage-equipment renovation, while the linked numbered map is dated April 2010; no post-renovation current map or conversion schedule is published.
- The representative-pattern/currentness gate failed. Expected/calculated counts remain `null`/`null`, ranges remain empty, and the first pass, independent second pass, and production gate were not started. Do not choose 612 or a range maximum, subtract 12 seats, infer removal numbers from geometry, or create a default-form policy. Reopen only after issuer publication of a post-renovation current numbered map, a default operational form/count, and removable/wheelchair seat-number mapping.
- Production promotions: **0**. All 58 baseline production fingerprints, catalog entries, and runtime details are unchanged; production remains **58 venues / 78,917 seats**.

### Tokyo top-up final aggregation

- Scope: **16 targets / 16 dispositions recorded**. Tokyo COMPLEX is **4/4 complete** and no active Tokyo top-up model queue remains.
- Production: **2 promotions / 3,958 seats added** — `line-cube-shibuya` 1,952 and `suntory-hall-main` 2,006. The top-up moved the application from 56 venues / 74,959 seats to **58 venues / 78,917 seats**.
- Non-production closures: **14** — 12 HOLD/SOURCE/POLICY/SCHEMA closures and two official contradiction/mismatch closures (`tokyo-geigeki-concert`, `tokyo-opera-city-concert`). Closed targets are not a model queue; reopen only on new issuer-owned evidence.
- Tokyo top-up bounded processing is complete. This does not make the release ready because the regional release seed still has unmet P0/P1/P2 coverage.

### 2026-08-10 Sol Tokyo COMPLEX macro 3

- `kabukiza`: **SCHEMA HOLD**. Current issuer-owned sources publish one physical layout with 1F 897 + 2F 441 + 3F 470 = 1,808 seats. The hanamichi is a permanent facility element, and the current map includes individually numbered first-floor sajiki seats. The 96 fourth-floor one-act viewing seats use separate access/sales and are explicitly excluded from the 1,808 total.
- Wheelchair/sales resolution: the current official map draws two unnumbered first-floor wheelchair positions, and Shochiku's current wheelchair guide confirms a dedicated space for at most two seats. Neither source says whether those positions are included in the 1,808 total or identifies numbered seats removed/reinstated for their use. The July 2025 change is a sales-grade change, while selectable/unavailable status varies by reservation stage; neither defines a different normal physical numbered set.
- The physical-layout variants are therefore resolved, but the selectable numbered-seat set is not uniquely defined. Expected/calculated counts remain `null`/`null`, ranges remain empty, and the first pass, independent second pass, and production gate were not started. Do not subtract two from 1,808, infer replacement numbers from the diagram, or create a repository inclusion policy. Reopen only if the issuer publishes the inclusion rule or replacement seat numbers.
- Production promotions: **0**. All 58 baseline production fingerprints, catalog entries, and runtime details are unchanged; production remains **58 venues / 78,917 seats**.

### 2026-08-10 Sol Tokyo COMPLEX macro 2

- Historical closure below is preserved but superseded by the successful 2026-08-12 `TOKYO-CONFIG-PILOT-1` schema-v2 conversion recorded in Current state.
- `meijiza`: **POLICY/SCHEMA HOLD**. The current official seat page and separate official PDFs publish two complete fixed-number configurations: with hanamichi 1F 834 + 2F 390 + 3F 144 = 1,368, and without hanamichi 1F 914 + 2F 390 + 3F 144 = 1,448. The page says hanamichi use varies by performance, and official event pages demonstrate both variants in actual use; no issuer source designates a normal, standard, or default one.
- Variant/sales resolution: the maps directly show the with-hanamichi first floor omits seats 7-10 in rows 1-20, matching the official 80-seat floor-count difference, but this does not authorize choosing the 1,448-seat variant as representative. Wheelchair spaces are at the two second-floor corners and reserved per performance, while no official individual numbers or removal/replace mapping to the 390 numbered second-floor seats is published. Seat-selection availability, annotation seats, auxiliary seats, and standing sales also vary by event or sale stage and do not designate a normal fixed-seat set.
- Representative-pattern gate failed, so expected/calculated counts remain `null`/`null`, ranges remain empty, and the first pass, independent second pass, and production gate were not started. Do not select 1,368 or 1,448 by policy, subtract or fit seats, or invent wheelchair identifiers. Reopen only if the issuer publishes a normal representative configuration and its wheelchair seat-number relationship.
- Production promotions: **0**. All 58 baseline production fingerprints, catalog entries, and runtime details are unchanged; production remains **58 venues / 78,917 seats**.

### 2026-08-10 Sol Tokyo COMPLEX macro 1

- `suntory-hall-main`: **PRODUCTION**, 2,006 numbered seats. The official seat map first pass and a fresh independent pass from the current official rental guide's separately published map both reproduced 1F 858 (rows 1-14 510; rows 15-23 348) and 2F 1,148 across LA 110, LB 74, LC 52, LD 66, C 354, RA 110, RB 74, RC 52, RD 66, and P 190. `rangeDiff: 0`, expected 2,006, calculated 2,006.
- Variant resolution: the official map draws six dedicated wheelchair spaces without individual numbers; current official material separately states up to 14 universal-design compatible seats. The 39-part lift is stage equipment, not an audience-seat numbering alternative. The current rental guide requires the Main Hall to be all-reserved and directs ticket issuance from the 2,006-seat map. This uniquely defines the standard printed numbered-seat set without subtraction, range fitting, or invented policy.
- Production promotion: **1**. All 57 baseline production fingerprints and catalog entries are unchanged; one reviewed source, catalog entry, runtime detail, and fingerprint were added. Production is **58 venues / 78,917 seats**.

### 2026-08-10 Sol LINE CUBE SHIBUYA bounded escalation

- `line-cube-shibuya`: **PRODUCTION**, 1,952 numbered seats. The official capacity remains 1,956 (1F 1,180 including four unnumbered multipurpose-viewing-room seats; 2F 424; 3F 352). A complete official-map first pass and independent second pass both counted 1F 1,176 numbered seats, 2F 424, and 3F 352 with `rangeDiff: 0`.
- New issuer-owned evidence: the current official `LINE CUBE SHIBUYA 利用のご案内` explicitly identifies the wheelchair-conversion targets as 1F row 16 seats 1-13 and 32-44, exactly 26 numbered seats. The official map draws the four multipurpose-room seats separately without individual numbers. This uniquely defines the normal selectable numbered-seat set without subtracting from 1,956 or guessing ranges.
- Production promotion: **1**. All 56 baseline production fingerprints and catalog entries are unchanged; one reviewed source, catalog entry, runtime detail, and fingerprint were added. Production is **57 venues / 76,911 seats**.

### 2026-08-10 Terra bounded escalation

- `nissay-theatre`: **SOURCE/SCHEMA HOLD**. Official PDF and theatre overview confirm 1,334 seats. Terra first pass and an independent second pass both read the labelled 1F central runs, G.C., 2F, and curves; however `1扉` through `16扉` are door labels, and the curved positions repeat printed numbers without an issuer-owned row/area key. No range, expected count, generated detail, catalog entry, or fingerprint was created. Do not invent areas from geometry or fit to 1,334. Reopen only with an issuer-owned seat-number key, ticketing seat specification, or equivalent coordinate-to-row/area mapping.
- `line-cube-shibuya`: **SOL HANDOFF at Terra close**. Both official-source passes confirmed 1,956 total (1F 1,180 including the four-seat multipurpose viewing room; 2F 424; 3F 352), but Terra had not yet located the current user-guide clause identifying the 26 conversion target numbers. This historical blocker was resolved by the later Sol escalation above.
- Production promotions: **0**. All 56 baseline catalog entries and fingerprints remain unchanged.

The 2026-08-09 Luna high STANDARD macro 1 closed exactly three targets with no production promotion:

- `tokyo-bunka-hall-main`: **HOLD**. Official current material shows the large hall is closed during renovation; the pre-renovation map must not be promoted as current.
- `tokyo-geigeki-concert`: **CONTRADICTION/MISMATCH**. Two official-PDF passes read Q-T as 92 printed seats while the same official table states 82; the unexplained difference is 10 seats.
- `tokyo-opera-city-concert`: **CONTRADICTION/MISMATCH**. Two official-PDF passes read 58 numbered seats in 1-LB/1-RB beyond the published 1F/2F/3F subtotal structure; excluding them only to fit 1,632 is prohibited.

No range was corrected to a published total, and no generated catalog/runtime/fingerprint changed. The remaining Tokyo top-up queue is 13 candidates: 4 STANDARD candidates ready for Luna source passes, 4 COMPLEX candidates for later judgment, and 5 HOLD candidates.

The 2026-08-09 Luna high STANDARD macro 2 closed exactly three targets with no production promotion:

- `nissay-theatre`: **TERRA HANDOFF**. Official current sources establish 1,334 seats, but the high-density 1F/G.C./2F and curved-box map needs lossless Terra range transcription.
- `line-cube-shibuya`: **POLICY HOLD / SOL HANDOFF**. Official sources establish 1,956 seats (1F 1,180; 2F 424; 3F 352), but the 4 multipurpose seats and 26-seat wheelchair conversion relationship does not define one fixed selectable set.
- `kioi-hall`: **POLICY HOLD**. Official sources establish 800 seats (1F 522; 2F 278), but the hall is closed through the end of 2026 and the 2025-10 map is not post-renovation current evidence.

No range was corrected to a published total, and no generated catalog/runtime/fingerprint changed. The remaining Tokyo top-up queue is 10 candidates: `kameari-lirio-hall` is the only remaining STANDARD candidate, with 4 COMPLEX candidates and 5 HOLD candidates.

The 2026-08-09 Luna high STANDARD macro 3 closed the final STANDARD candidate:

- `kameari-lirio-hall`: **HOLD / SOURCE HOLD**. Official material states 610 seats including two wheelchair positions, but the official current status is renovation closure through 2026-08-31 with a scheduled 2026-09-01 reopening. The post-renovation current seat map and numbered-seat set are not confirmed, so no pre-renovation range was promoted.

No range was fitted to 610, and no generated catalog/runtime/fingerprint changed. No Tokyo STANDARD candidate remains. The unresolved Tokyo top-up queue remains 10 candidates: 4 COMPLEX and 6 HOLD, including `kameari-lirio-hall`.

## Model queues

- **Tokyo coverage active — Terra**: **0**. `TERRA-DENSE-1` through `TERRA-DENSE-4` are closed formal holds; no Terra→Sol handoff was created.
- **Tokyo coverage active — Sol, MUST 0**: `tokyo-dome` is a closed TOKYO-DOME COMPLEX formal hold. MUST phase is 44/44 formally disposed.
- **Tokyo coverage active — Sol, SHOULD 0**: `SOL-SHOULD-1/2/3` are complete; SHOULD research is **28/28**.
- **Bounded addressable conversion queue**: **0**. `TOKYO-ADDRESSABLE-CONVERSION-1A/1B` removed all four prior candidates as B_SOURCE_LIMITED after fresh evidence; repo-derived nonproduction addressable IDs are none.
- **Adequacy remediation — Sol, bounded 3**: `TOKYO-ADEQUACY-CRITICAL-GAPS-1` for `nippon-budokan`, `nhk-hall`, `zepp-haneda` only, official-source-discovery/evidence-normalization first. No range transcription without newly complete issuer-owned evidence.
- **Record-only / post-release — Sol, OPTIONAL 4 (non-blocking; not an active model queue)**: `ajinomoto-stadium`, `national-theatre-large`, `national-theatre-small`, `teikoku-theatre`.
- **Tokyo coverage Luna**: none. The twelve `TERRA-DENSE-1`/`TERRA-DENSE-2`/`TERRA-DENSE-3`/`TERRA-DENSE-4` targets and all pre-existing Tokyo HOLD/CONTRADICTION are closed, not model queues; reopen only on changed issuer-owned evidence. Active pre-release queue is MUST/SHOULD only. P2 remains closed at 24/24 dispositions and is outside the Tokyo coverage sequence.

## Holds

- **2026-08-11 P2 Sol evidence-resolution closures**: `rohm-theatre-kyoto-main-release-seed` is **SOURCE CONTRADICTION HOLD** at expected/calculated `2,005/2,013`; the issuer identifies wheelchair IDs and the 172 pit removals, but not the eight-ID surplus in its current normal map. `sapporo-kitara-main-release-seed` is **SOURCE/SCHEMA HOLD** at `2,008/null`; the fixed-seat/wheelchair relationship is resolved, but the two dense-map passes did not converge and no range fitting is permitted. `kyoto-concert-hall-main-release-seed` passed at `1,833/1,833`. The report is `data/venue-reports/p2-sol-evidence-resolution-macro-2026-08-11.json`. `sapporo-kitara-small-release-seed`, the 16 P2 HOLDs, the four separate Sol targets, and all Kanto/Tokyo records were untouched.
- **2026-08-11 P2 Sol COMPLEX macro 1 closure**: `hbg-hall-release-seed` is **SOURCE/SCHEMA HOLD** at expected/calculated `2,001/null`. Normal count and wheelchair conversion numbers are resolved, but the issuer does not map the stated pit-internal 52 seats within the 82 numbered seats unavailable during pit use. The report is `data/venue-reports/p2-sol-complex-macro-1-hbg-hall-2026-08-11.json`. Reopen only when the issuer publishes the numbered 52/30 allocation; do not infer it from diagram geometry or arithmetic.
- **2026-08-11 P2 Sol COMPLEX macro 2 closure**: `fukuoka-sunpalace-hall-release-seed` is **SOURCE/SCHEMA HOLD** at expected/calculated `null/null`, with nominal capacity 2,316. The pit set is resolved as 1列16–41, 2列14–43, 3列12–45, and 4列12–45, totaling 124 directly shown seats. The issuer does not define whether six unnumbered wheelchair positions are included in 2,316 or which numbered seats are removed or replaced. The report is `data/venue-reports/p2-sol-complex-macro-2-fukuoka-sunpalace-2026-08-11.json`. Reopen only on issuer publication of the inclusion rule and numbered replacement mapping; do not infer row 32 numbers from diagram geometry.
- **2026-08-11 P2 Sol COMPLEX macro 3 closure**: `festival-hall-release-seed` is **SOURCE/SCHEMA HOLD** at expected/calculated `null/null`, with nominal normal capacity 2,700 and pit-use capacity 2,524. The issuer directly states a 176-seat reduction for pit/forestage use but does not identify the seat numbers. Its current map and FAQ confirm an unnumbered 1F wheelchair space but not its capacity, inclusion in 2,700, or numbered replacements. Currentness is resolved by the live official links: the seat map is internally dated 2014-05-20; the stage 1/100 plan is Ver.2016.03.01 and is preferred over the linked 1/200 Ver.2014.01.11 for stage semantics, without mixing geometry into seat numbers. The report is `data/venue-reports/p2-sol-complex-macro-3-festival-hall-2026-08-11.json`. Reopen only on issuer publication of pit/forestage removal numbers and wheelchair inclusion/replacement mapping.
- **2026-08-11 P2 Sol COMPLEX macro 4 closure**: `aichi-arts-center-main-release-seed` is **SOURCE CONTRADICTION HOLD** at expected/calculated `2,456/null`. The 2024 current maps and 2026 current guide resolve the full configuration, wheelchair inclusion, and source generations, but the official pit count of 176 conflicts with 177 printed IDs in rows 1–5. The report is `data/venue-reports/p2-sol-complex-macro-4-aichi-and-final-2026-08-11.json`. Reopen only when the issuer identifies or corrects the one-seat difference; do not fit the rows to 176.

Historical closure records include the superseded `meijiza` policy/schema ambiguity; current `meijiza` is production with two verified configurations. Current holds include `kabukiza` wheelchair inclusion schema ambiguity, `setagaya-public-theatre` configuration/conversion/currentness ambiguity, Sendai Sunplaza policy ambiguity, Sapporo hitaru policy ambiguity, Misonoza policy ambiguity, Umeda date hold, Hakataza source hold, and the ACROS official 1,871 vs 1,885 contradiction. Do not promote any hold by arithmetic or inference.

## Deployment verification

The official URL is `https://seat-lottery-simulator.studiotomo.workers.dev/`. The repository uses the established GitHub-to-Cloudflare Workers Builds flow. Before closing a macro, verify that the final pushed HEAD has a successful `Workers Builds: seat-lottery-simulator` check and that the public URL returns HTTP 200.

- 2026-08-11 P2 Sol COMPLEX macro 4 deployment verification passed: data commit `05db51f` completed `Workers Builds: seat-lottery-simulator` check `https://github.com/tomo527/seat-lottery-simulator/runs/93759339906`; the public URL returned HTTP 200, displayed `絞り込み結果 59件`, returned zero Aichi Arts Center results as required by the hold, and emitted no console errors. The final HANDOFF-only follow-up preserves the same 59-venue / 80,750-seat deployment.

## Validation status

- 2026-08-13 `TOKYO RELEASE COVERAGE ADEQUACY REVIEW` passed its validation while returning **ADEQUACY FAIL**: JSON parsing, `venues:inventory:report`, `venues:readiness:report`, `venues:release:coverage`, `venues:build/check/validate/report`, lint, typecheck, unit tests (**17 files / 140 tests**), production build, E2E (**13/13** in the approved elevated Miniflare run after the expected sandbox registry `EPERM`), production artifact regression against 1B commit `7f533819ae5745ae826cba660c9cd6912f80a815`, and `git diff --check`. Production remains **62 venues / 63 selectable configurations / 85,237 records**; no deploy or release was performed.
- 2026-08-13 `TOKYO-ADDRESSABLE-CONVERSION-1B` validation passed: both target `venues:review` runs, Tokyo wave batch reports, inventory/readiness/release reports, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, lint, typecheck, unit tests (**17 files / 140 tests**), production build, E2E (**13/13** in the approved elevated Miniflare run), production artifact baseline regression, and `git diff --check`. The sandbox-only E2E attempt hit the known Wrangler registry `EPERM`; the elevated rerun passed. Production artifacts remain exactly **62 venues / 63 selectable configurations / 85,237 records**, so no Workers Build or public-URL verification was required.
- 2026-08-13 `TOKYO-ADDRESSABLE-CONVERSION-1A` passed: both target `venues:review` runs, JSON parsing, `venues:inventory:report`, `venues:readiness:report`, `venues:release:coverage`, `venues:build/check/validate/report`, lint, typecheck, unit tests (**17 files / 139 tests**), production build, E2E (**13/13**, approved elevated Miniflare run after the expected sandbox registry `EPERM`), and `git diff --check`. Production catalog/runtime/fingerprints remain exactly **62 venues / 63 selectable configurations / 85,237 records**; no Workers Build or public-URL verification is required because production additions are zero.
- 2026-08-13 `SOL-SHOULD-3` passed: JSON parsing、`venues:inventory:report`、`venues:readiness:report`、`venues:release:coverage`、`venues:build/check/validate/report`、lint、typecheck、unit tests（**17 files / 139 tests**）、production build、E2E（**13/13**、approved elevated Miniflare run after the known sandbox registry wait）、production generated-artifact regression、`git diff --check`。productionは **62 venues / 63 selectable configurations / 85,237 records**、明治座2配置とNNTT中劇場906席を含む全既存production artifactは不変。SHOULD researchは **28/28**。production追加がないためSHOULD-3のcommit/push、Workers Build、公開URL確認は実施しない。
- 2026-08-13 `SOL-SHOULD-2` passed: JSON parsing、`venues:inventory:report`、`venues:readiness:report`、`venues:release:coverage`、`venues:build/check/validate/report`、lint、typecheck、unit tests（**17 files / 139 tests**）、production build、E2E（**13/13**、approved elevated Miniflare run after the known sandbox registry wait）、production generated-artifact regression、`git diff --check`。productionは **62 venues / 63 selectable configurations / 85,237 records**、明治座2配置とNNTT中劇場906席を含む全既存production artifactは不変。production追加がないためcommit/push後のWorkers Build・公開URL確認は実施しない。
- 2026-08-12 `TOKYO-CONFIG-A-THEATRE-1` local validation passed: fresh `venues:review` for all three targets, JSON parsing, `venues:inventory:report`, `venues:readiness:report`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, lint, typecheck, unit tests (**17 files / 139 tests**), production build, E2E (**13/13**, including NNTT中劇場 and both existing 明治座 configurations), existing61 production regression, and `git diff --check`. NNTT中劇場 adds one verified 906-seat configuration; all prior 61 production catalog/fingerprint entries and runtime details remain unchanged. Workers Build/public-URL verification follows the pushed data commit.
- 2026-08-12 TOKYO-DOME COMPLEX passed: `venues:review -- --id tokyo-dome-concert`, `venues:batch:report -- --batch tokyo-dome-complex`, `venues:inventory:report`, `venues:readiness:report`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests in the approved elevated Miniflare run), JSON parsing, baseline artifact comparison, and `git diff --check`. The source remains draft with empty ranges and null expected/calculated counts; all 60 production artifacts remain unchanged at 81,515 seats. No deploy was required because there was no production promotion.
- 2026-08-12 SOL-CPLX-1 passed: `venues:review` for all three targets, `venues:batch:report -- --batch tokyo-sol-cplx-1`, `venues:inventory:report`, `venues:readiness:report`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests in the approved elevated Miniflare run), JSON parsing, baseline artifact comparison, and `git diff --check`. All three sources remain draft with empty ranges and null expected/calculated counts; all 60 production artifacts remain unchanged at 81,515 seats. No deploy was required because there was no production promotion.
- 2026-08-12 TERRA-DENSE-4 passed: `venues:review -- --batch tokyo-terra-dense-4`, `venues:batch:report -- --batch tokyo-terra-dense-4`, `venues:inventory:report`, `venues:readiness:report`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, `lint`, `typecheck`, `test`, `build`, `test:e2e` (11 tests), JSON parsing, baseline artifact comparison, and `git diff --check`. All three sources remain draft with empty ranges and null expected/calculated counts; all 60 production artifacts remain unchanged at 81,515 seats. No deploy was required because there was no production promotion.
- 2026-08-12 TERRA-DENSE-3 passed: `venues:review -- --batch tokyo-terra-dense-3`, `venues:batch:report -- --batch tokyo-terra-dense-3`, `venues:inventory:report`, `venues:readiness:report`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, `lint`, `typecheck`, `test`, `build`, `test:e2e`, JSON parsing, baseline artifact comparison, and `git diff --check`. All three sources remain draft with empty ranges and null expected/calculated counts; all 60 production artifacts remain unchanged at 81,515 seats.
- 2026-08-12 TERRA-DENSE-2 passed: `venues:review -- --batch tokyo-terra-dense-2`, `venues:batch:report -- --batch tokyo-terra-dense-2`, `venues:inventory:report`, `venues:readiness:report`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, `lint`, `typecheck`, `test`, `build`, `test:e2e`, JSON parsing, baseline artifact comparison, and `git diff --check`. All three sources remain draft with empty ranges and null expected/calculated counts; all 60 production artifacts remain unchanged at 81,515 seats.
- 2026-08-11 Sol high P2 COMPLEX macro 4 passed: `venues:review -- --id aichi-arts-center-main-release-seed`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch release-seed-v1`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests, elevated Miniflare run), JSON parsing, baseline artifact comparison, and `git diff --check`. Aichi remains draft with empty ranges; all 59 production fingerprints, catalog entries, source semantics, and runtime details are unchanged at 80,750 seats. P2 is closed at 24/24 dispositions.
- 2026-08-11 Sol high P2 COMPLEX macro 3 passed: `venues:review -- --id festival-hall-release-seed`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch release-seed-v1`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests, elevated Miniflare run), JSON parsing, baseline artifact comparison, and `git diff --check`. Festival Hall remains draft with zero ranges; all 59 production fingerprints, catalog entries, source semantics, and runtime details are unchanged at 80,750 seats.
- 2026-08-11 P2 Sol COMPLEX macro 3 deployment verification passed: data commit `27785a6` completed the `Workers Builds: seat-lottery-simulator` check at `https://dash.cloudflare.com/c7f327eb274991c64d5cabfaee8ab0af/workers/services/view/seat-lottery-simulator/production/builds/7d06b27d-2c8a-4eac-a479-12bc1642f6ed`; the public URL returned HTTP 200, displayed `絞り込み結果 59件`, returned zero Festival Hall results as required by the hold, and emitted no console errors. The final HANDOFF-only follow-up preserves the same 59-venue / 80,750-seat deployment.
- 2026-08-11 P2 Sol COMPLEX macro 2 deployment verification passed: data commit `9139069` completed `Workers Builds: seat-lottery-simulator` check `https://github.com/tomo527/seat-lottery-simulator/runs/93686385722`; the public URL returned HTTP 200, displayed `絞り込み結果 59件`, returned zero Fukuoka Sunpalace results as required by the hold, and emitted no console errors. The final HANDOFF-only follow-up preserves the same 59-venue / 80,750-seat deployment.
- 2026-08-11 Sol high P2 COMPLEX macro 2 passed: `venues:review -- --id fukuoka-sunpalace-hall-release-seed`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch release-seed-v1`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests, elevated Miniflare run), baseline artifact comparison, JSON parsing, and `git diff --check`. Fukuoka Sunpalace remains draft with zero ranges; all 59 production fingerprints, catalog entries, source semantics, and runtime details are unchanged at 80,750 seats.
- 2026-08-11 P2 Sol COMPLEX macro 1 deployment verification passed: data commit `6894ba5` completed `Workers Builds: seat-lottery-simulator` check `https://github.com/tomo527/seat-lottery-simulator/runs/93680487013`; the public URL returned HTTP 200, displayed `絞り込み結果 59件`, returned zero HBG Hall results as required by the hold, and emitted no console errors. The final HANDOFF-only follow-up preserves the same 59-venue / 80,750-seat deployment.
- 2026-08-11 Sol high P2 COMPLEX macro 1 passed: `venues:review -- --id hbg-hall-release-seed`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch release-seed-v1`, `venues:release:coverage`, `venues:build`, `venues:check`, `venues:validate`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests, elevated Miniflare run), baseline artifact comparison, and `git diff --check`. HBG remains draft with zero ranges; all 59 production fingerprints, catalog entries, source semantics, and runtime details are unchanged at 80,750 seats.
- 2026-08-11 Sol high P2 evidence-resolution macro passed: `venues:review -- --id kyoto-concert-hall-main-release-seed`, `venues:build`, `venues:check`, `venues:validate`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch release-seed-v1`, `venues:release:coverage`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests, elevated Miniflare run), baseline artifact comparison, and `git diff --check`. The prior 58 fingerprints, catalog entries, source semantics, and runtime details are unchanged; production is 59 venues / 80,750 seats.
- 2026-08-11 P2 evidence-resolution deployment verification passed: data commit `412d139` completed `Workers Builds: seat-lottery-simulator` check `https://github.com/tomo527/seat-lottery-simulator/runs/93671280163`; the public URL returned HTTP 200, displayed `絞り込み結果 59件`, returned one Kyoto Concert Hall search result, lazy-loaded `抽選対象 1,833席`, and emitted no console errors. The final HANDOFF-only follow-up must preserve this deployment state.
- 2026-08-10 Sol Tokyo COMPLEX macro 4 passed: `venues:build`, `venues:check`, `venues:validate`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch tokyo-wave-1`, `venues:release:coverage`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests), and `git diff --check`. `setagaya-public-theatre-standard` remains draft with no ranges after the representative-pattern/currentness gate failed; all 58 production catalog entries, runtime details, and fingerprints are unchanged at 78,917 seats.
- 2026-08-10 Sol Tokyo COMPLEX macro 3 passed: `venues:build`, `venues:check`, `venues:validate`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch tokyo-wave-1`, `venues:release:coverage`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests), and `git diff --check`. `kabukiza-standard` remains draft with no ranges after the selectable-numbered-set schema gate failed; all 58 production catalog entries, runtime details, and fingerprints are unchanged at 78,917 seats.
- **Historical, superseded by the schema-v2 pilot:** 2026-08-10 Sol Tokyo COMPLEX macro 2 passed its validation while `meijiza-standard` was still draft with no ranges. That 58-venue / 78,917-seat state is not current; 明治座 now has two verified production configurations.
- 2026-08-10 Sol Tokyo COMPLEX macro 1 passed: `venues:review -- --id tokyo-wave3-0130`, `venues:build`, `venues:check`, `venues:validate`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch tokyo-wave-3`, `venues:release:coverage`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests), and `git diff --check`. Existing 57 fingerprints are unchanged; production is 58 venues / 78,917 seats.
- 2026-08-10 bounded Sol LINE CUBE escalation passed: `venues:review -- --id line-cube-shibuya-standard`, `venues:build`, `venues:check`, `venues:validate`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch tokyo-wave-1`, `venues:release:coverage`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests), and `git diff --check`. Existing 56 fingerprints are unchanged; production is 57 venues / 76,911 seats.
- 2026-08-10 bounded Terra escalation passed: `venues:build`, `venues:check`, `venues:validate`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch tokyo-wave-1`, `venues:release:coverage`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests), and `git diff --check`. The 56-production catalog, runtime details, and fingerprints remain unchanged.
- 2026-08-09 Tokyo macro 3 data checks passed: `venues:build`, `venues:check`, `venues:validate`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report`, `venues:release:coverage`, and `venues:report`; source hold recorded without changing production data. Production remains 56 venues / 74,959 seats.
- Full application validation passed: `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests), and `git diff --check`.
- Macro 3 deployment verification passed: `Workers Builds: seat-lottery-simulator` runs `https://github.com/tomo527/seat-lottery-simulator/runs/93256523368` and `https://github.com/tomo527/seat-lottery-simulator/runs/93256713037` succeeded; the public URL returned HTTP 200. The final HANDOFF-only follow-up must preserve this deployment state.
- Sol Tokyo COMPLEX macro 1 deployment verification passed: data commit `f2d3741` completed `Workers Builds: seat-lottery-simulator` check `https://github.com/tomo527/seat-lottery-simulator/runs/93349026437`; the public URL loaded successfully and exposed `サントリーホール〔大ホール〕` with `抽選対象 2,006席`, including successful lazy-loaded selection. The final HANDOFF-only follow-up must preserve this deployment state.
- Historical Sol Tokyo COMPLEX macro 2 deployment verified that the then-HOLD 明治座 was absent. That runtime state was superseded by the later schema-v2 pilot; current 明治座 is production with two selectable configurations.
- Sol Tokyo COMPLEX macro 3 deployment verification passed: data commit `7223f4a` completed `Workers Builds: seat-lottery-simulator` check `https://github.com/tomo527/seat-lottery-simulator/runs/93364063363`; the public URL loaded successfully with `絞り込み結果 58件`, and searching `歌舞伎座` returned 0 results as required by the HOLD. The final HANDOFF-only follow-up must preserve this deployment state.
- Sol Tokyo COMPLEX macro 4 deployment verification passed: data commit `65239d0` completed `Workers Builds: seat-lottery-simulator` check `https://github.com/tomo527/seat-lottery-simulator/runs/93367288322`; the public URL loaded successfully with `絞り込み結果 58件`, and searching `世田谷パブリックシアター` returned 0 results as required by the HOLD. The final HANDOFF-only follow-up must preserve this deployment state.
- Existing warnings remain limited to Kyocera Dome row fragmentation, inventory duplicate-candidate reporting, and 300/500-venue size projections; no database validation errors.
- E2E required the approved elevated run because Miniflare writes the user Wrangler registry outside the workspace; the sandbox-only attempt failed with `EPERM` and was not a repository failure.
- 2026-08-10 Luna high P2 STANDARD macro 1 validation passed: `venues:build`, `venues:check`, `venues:validate`, `venues:inventory:report`, `venues:readiness:report`, `venues:batch:report -- --batch release-seed-v1`, `venues:release:coverage`, `venues:report`, `lint`, `typecheck`, `test` (16 files / 116 tests), `build`, `test:e2e` (11 tests, elevated Miniflare run), and `git diff --check`. Both targets remain non-production holds; all 58 production artifacts remain unchanged at 78,917 seats.

## Exact next action

Run **`TOKYO-ADEQUACY-CRITICAL-GAPS-1`** for `nippon-budokan`, `nhk-hall`, and `zepp-haneda` only, restricted to official-source discovery and evidence normalization. Do not transcribe ranges until complete issuer-defined configuration, wheelchair/companion semantics, exact count, and an independent current source generation are established. Then re-run the adequacy review. Do not release, deploy, broaden research, or reopen 1A/1B without changed issuer-owned evidence.
